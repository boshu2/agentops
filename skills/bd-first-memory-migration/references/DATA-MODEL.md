# Memory data model + type taxonomy

> Scaffold stub — authored by bead ag-5u50.6. Defines how a "real" memory is represented on top of bd memory KEYS (convention + thin wrapper; no bd core change).

Planned: type taxonomy (fact:/feedback:/project:/episodic:/procedural:), per-memory fields
(key, type, body, source-provenance, utility_score, access_count, timestamps, maturity, superseded_by),
and per-type decay/TTL policy. See bead .6 for the authoritative spec.
