{
  "name": "agy-project-worktree-permissions",
  "skill_api_version": 1,
  "form": "A",
  "quality_score": 0.92,
  "summary": "Prove scoped project/worktree isolation on the AGY image — non-overlapping --add-dir scopes, role-matched permission tiers, and the dcg guard — before an AGY tick joins the quorum.",
  "sections": [
    { "id": "title",        "title": "agy-project-worktree-permissions", "type": "intro",       "priority": "required" },
    { "id": "overview",     "title": "Overview / When to Use",           "type": "overview",    "priority": "required" },
    { "id": "constraints",  "title": "⚠️ Critical Constraints",           "type": "constraints", "priority": "required" },
    { "id": "workflow",     "title": "Workflow / Methodology",           "type": "procedure",   "priority": "required" },
    { "id": "output",       "title": "Output Specification",             "type": "contract",    "priority": "required" },
    { "id": "rubric",       "title": "Quality Rubric",                   "type": "rubric",      "priority": "required" },
    { "id": "examples",     "title": "Examples",                         "type": "examples",    "priority": "standard" },
    { "id": "troubleshoot", "title": "Troubleshooting",                  "type": "table",       "priority": "standard" },
    { "id": "seealso",      "title": "See Also / References",            "type": "routing",     "priority": "required" }
  ],
  "references": [],
  "metadata": {
    "tier": "cross-vendor",
    "stability": "experimental",
    "dependencies": ["agy-native", "cc-worktree-isolation", "dcg", "beads-br"],
    "hexagonal_role": "supporting",
    "context_window": "inherit",
    "consumes": ["agy-native"],
    "produces": ["agy-isolation-evidence"],
    "triggers": [
      "agy",
      "worktree",
      "permissions",
      "project",
      "agy --add-dir scope",
      "project/worktree isolation on Antigravity",
      "scoped permission proof",
      "author != judge isolation on AGY",
      "gate cp-c6k.3.4"
    ],
    "token_estimate": {
      "minimal": 70,
      "overview": 300,
      "standard": 1300,
      "full": 2400
    }
  },
  "output_contract": "An AGY isolation-proof artifact: two roles (author, judge) pinned to non-overlapping --add-dir/worktree scopes with role-matched permission tiers, the dcg BeforeTool guard confirmed present, two distinct conversation ids, and a verdict — persisted to <repo>/evidence and/or the AGY brain (userFacing:true). Gates cp-c6k.3.4 before the AGY image counts toward quorum.",
  "evidence": {
    "sources": [
      "~/dev/control-plane/IMAGE-AGY.md (cp-c6k.3.4 project/worktree/scoped-permission child; --add-dir + --sandbox + sandboxed-defaults image rule)",
      "~/dev/agentops/skills/agy-native/SKILL.md (--add-dir write-isolation, permission tiers, dcg BeforeTool guard, author!=judge)",
      "~/dev/agentops/skills/cc-worktree-isolation/skill.spec.json (cross-image worktree-isolation analogue)",
      "~/.gemini/settings.json (dcg BeforeTool hook on run_shell_command — guard state checked by validate.sh proxy)"
    ]
  }
}
