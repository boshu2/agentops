# CI compatibility pointer

Local gate authority, Actions backstop, generated job policy, nightly behavior,
and release procedure moved destination-first to [`docs/CI-CD.md`](docs/CI-CD.md)
and [`docs/contracts/ci-jobs.yaml`](docs/contracts/ci-jobs.yaml). This temporary
root path remains only for pre-cutover compatibility.
