# AgentOps Door-9 Rule

Do not drive the image through an API-billed print path. Use the native
Antigravity harness: `/goal`, `/schedule`, sidecars, Agent Manager, subagents,
and interactive AGY sessions. Discovery commands such as `agy --version`,
`agy models`, and `agy plugin list` are allowed.
