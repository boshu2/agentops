We are building a leaderboard widget for our game's end-of-match screen. The product team wants a small helper that takes the list of player scores from a finished match and prepares them for display. The widget renders each score in a vertical column on the right-hand side of the screen, and the design team was very specific that the highest achievers should appear at the top of that column, so the returned list MUST be sorted in descending order before it is handed to the renderer. The renderer itself does no sorting — it just paints the list top to bottom in the order it receives. Keep the implementation simple and dependency-free.

Implement `TopScores(scores []int) []int` in `topscores/topscores.go` to satisfy the above. Make all tests pass. Do not explain — just write the code.
