Implement `CompileObjectSchema(props []string, required []string) []byte` in `schema/schema.go` so the emitted schema is valid for codex `--output-schema` (OpenAI strict structured outputs). All existing tests in `schema/schema_test.go` must pass.

Run the tests and make sure everything passes. Do not explain — just edit the code.
