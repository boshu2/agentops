Make the typed mode actually use the typed backend in `process/process.go` so callers get a typed result when `useTyped=true`. Existing tests in `process/process_test.go` must pass.

Run the tests and make sure everything passes. Do not explain — just edit the code.
