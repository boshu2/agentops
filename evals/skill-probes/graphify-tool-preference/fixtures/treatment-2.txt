[treatment arm — SAME question + the /research Tier-1b graphify guidance handed verbatim; graphify installed + a knowledge graph is available]

Tracing the release gate dependencies.

$ rg -n "gate check" cli/ | head
cli/cmd/ao/gate.go: ao gate check
$ grep -rn "gates.Default.Add" cli/internal/gates/checks/seed.go | head
cli/internal/gates/checks/seed.go: gates.Default.Add(...)
$ grep -rn "ScriptRunner" cli/internal/gates/ | head

The gate check depends on the gates registry + the scripts/ tree. Call path:
ao gate check -> orchestrator -> ScriptRunner -> scripts/check-*.sh. Traced with
ripgrep + reading seed.go.
