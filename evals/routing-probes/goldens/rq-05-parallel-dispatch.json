{
  "$schema": "../../../schemas/pack-quality-expectations.v1.schema.json",
  "schema_version": 1,
  "id": "rq-05-parallel-dispatch",
  "retrieval_surface": "ao-skills-find",
  "task": "Several queued work items touch disjoint files and the caller wants them run at once without the workers colliding.",
  "query": "dispatch these independent tasks to parallel workers without collisions",
  "top_k": 3,
  "expected_selected_ids": [
    "agent-native"
  ],
  "critical_omitted_ids": [
    "codex-exec"
  ],
  "forbidden_leaks": [
    "^(swarm|fitness|status|reality-check)$"
  ],
  "min_provenance_density": 1.0,
  "max_tokens": 80,
  "rationale": "Agent-native now owns explicit disjoint packet dispatch through the caller-selected runtime. Codex-exec executes one supplied command, while reality-check reports a claim or requested measurement; neither owns a batch of independent work packets.",
  "notes": "Owner migrated from swarm when packet dispatch was consolidated into agent-native. The caller still requests concurrent dispatch with noncolliding write scopes; historical routing measurements retain their original subject."
}
