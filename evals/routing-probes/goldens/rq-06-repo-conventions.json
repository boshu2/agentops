{
  "$schema": "../../../schemas/pack-quality-expectations.v1.schema.json",
  "schema_version": 1,
  "id": "rq-06-repo-conventions",
  "retrieval_surface": "ao-skills-find",
  "task": "The caller is about to write Go in this repository and wants the local rules that govern the change, not generic language advice.",
  "query": "which repository conventions apply to this Go change",
  "top_k": 3,
  "expected_selected_ids": ["standards"],
  "critical_omitted_ids": ["codebase-recon"],
  "forbidden_leaks": ["^automation-shape-routing$"],
  "min_provenance_density": 1.0,
  "max_tokens": 100,
  "rationale": "standards loads only the standards relevant to a supplied change. codebase-recon took rank 1 on the phrasing 'match the existing repository conventions when writing this Go code' in an offline check on 2026-08-26 — reconstructing the repo is the expensive wrong answer when the rules are already declared, so it is pinned out of the pack. automation-shape-routing is a router-shaped decoy that must not hold rank 1.",
  "notes": "Derived from the rt-06-conventions routing template — the one 2026-08-05 live batch row that ROUTED but was contaminated by the agent finding the scenario file on disk."
}
