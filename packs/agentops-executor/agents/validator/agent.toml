scope = "rig"
provider = "codex"
session = "tmux"
wake_mode = "fresh"
lifecycle = "one_shot"
min_active_sessions = 0
max_active_sessions = 1
option_defaults = { permission_mode = "auto-edit", model = "gpt-5.6-sol", effort = "high" }
install_agent_hooks = ["codex"]
inject_assigned_skills = true
