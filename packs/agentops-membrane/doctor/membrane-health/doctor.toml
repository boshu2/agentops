description = "Membrane health: the fail-closed close door is installed, wired, and the importing city has the >=2-family posture the gate requires (a 1-family city can never CONFIRM)"
run = "run.sh"
