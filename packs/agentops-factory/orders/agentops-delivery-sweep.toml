[order]
description = "Snapshot at most eight ready AgentOps delivery leaves and advance each exactly once."
exec = "$PACK_DIR/assets/scripts/delivery-sweep.sh"
scope = "rig"
enabled = true
idempotent = false
trigger = "cooldown"
interval = "30s"
timeout = "60s"
