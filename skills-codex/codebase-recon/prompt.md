# codebase-recon

Reconstruct a repository as cited entry-to-test flows, bounded claims, and a reusable baseline or verified delta. Steer with mode (baseline|delta), view emphasis, domain lens, and depth. Triggers: "build a repository mental model", "trace this codebase", "refresh the prior recon", "codebase-recon --view=audit".

## Instructions

Load and follow the skill instructions from the sibling `SKILL.md` file for this skill.
Then read local files in `references/` and `scripts/` when needed.
