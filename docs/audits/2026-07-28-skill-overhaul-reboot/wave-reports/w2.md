# Wave W2 report — product/campaign

Bead: `age-skill-overhaul-reboot-sjv7v.3`. Branch: `claude/sor-w2-product`.
Skills: `product`, `goals`, `craft-goal`, `automation-shape-routing`.

Every checklist item from `checklists/outcomes-12.md` for these four slugs is
dispositioned below. Each was verified against the live tree first; the
`Verified` line records what the live file actually showed.

## automation-shape-routing

- **P1-ASR-SPEC** `[defect]` — **fixed (deleted `skill.spec.json`).**
  Verified: the sidecar is a stale form-A metadata source that contradicts the
  SKILL.md v2 frontmatter on six fields — declares 9 sections that do not exist
  in SKILL.md, `context_rel` 2 vs 5, `produces` `[]` vs `[automation-shape-verdict]`,
  `user_invocable: false` vs `user-invocable: true`, a divergent trigger set, and
  `evidence.sources` citing out-of-repo `~/dev/agentops-3cat-spike/`. Confirmed
  nothing load-bearing consumes it: `grep` across `scripts/` and `cli/` finds no
  reader; the only consumers are cass's own `validate.sh`, which treats an absent
  sidecar as OK ("skill.spec.json sidecar absent"). No owner command regenerates
  it, so "regenerate under a parity check" is impossible; hand-syncing it would
  just re-create the drift class. Deleted the source (`git rm`); codex-sync
  removed the codex twin projection during regen. SKILL.md is unchanged and
  remains the single canonical metadata source.

- **P2-INVOCABLE** `[defect]` — **fixed (folded into P1-ASR-SPEC).**
  Verified: the `user_invocable` (underscore) vs `user-invocable` (hyphen)
  mismatch was between the spec and SKILL.md; SKILL.md uses the canonical
  hyphenated key (`user-invocable: true`, matching the corpus and schema).
  Deleting the spec removes the mismatch.

- **Spec↔SKILL.md parity check to the corpus gate** `[enhance]` —
  **deferred (out of wave scope).** This is a shared corpus-gate change covering
  all three spec-carrying skills (`automation-shape-routing`, `cass`, `cc-hooks`);
  `cass` and `cc-hooks` are outside W2. Deleting ASR's spec removes ASR from the
  risk set; a durable corpus-wide spec-parity (or spec-retirement) gate belongs
  to a separate bead per the "touches a shared contract / another skill → defer"
  constraint.

## goals

- **P0-EFFECTS** `[defect]` — **fixed** (`effects: []` → `effects: [write_goal_snapshot]`).
  Verified in `cli/internal/goals/commands.go`: `measure` (`SaveSnapshot` at
  :207), `drift` (:399), and `export` (:495/:504) persist a snapshot to the
  default `.agents/ao/goals/baselines` (:171/:389/:484), best-effort (failure
  only warns, does not fail the command — :208-209). `render --out` writes a
  Gherkin spec via `os.WriteFile` (`cli/internal/goals/render.go:47`). Declared
  the snapshot effect and documented both writes in the Boundary as derived-path
  writes that never touch the goals source.

- **P1-GOALS-DRIFT** `[defect]` — **fixed.** Verified eight subcommands
  registered in `cli/internal/commands/goals/module.go:112-119` (measure,
  validate, drift, history, export, meta, scenarios, render); SKILL.md documented
  six. Added `scenarios` and `render` to the command block. `produces:
  [result.json]` was false (no code path writes `result.json`; grep clean) —
  changed to `goal-measurement-report`, matching `output_contract`.

- **State goals source never mutated + command-tree parity check** `[enhance]`
  — **partially applied / partially deferred.** Applied the "goals source is
  never modified" statement, tied to the new write-effect declaration (new
  Boundary bullet + Commands preamble). Deferred the command-tree parity check:
  it belongs as a Go test next to `module.go` asserting the SKILL.md command
  list matches the registered subcommands — a cross-cutting change into the CLI
  test suite, out of skill-file scope. The current 6-vs-8 drift is fixed by
  documentation.

- **Note: `ao goals … --json` REFUTED** — **confirmed, not carried.** Verified
  `cli/cmd/ao/root.go:140` registers a global persistent `--json` flag; the
  documented `--json` invocations are valid and were kept.

## craft-goal

- **P1-CG-VAL** `[enhance]` — **applied (embedded output-shape validator).**
  Verified `scripts/validate.sh` is a 3-line shim to
  `skill-builder/scripts/heal.sh --check --strict` (structural only); the
  `output_contract`'s terminal tokens had no validator. Added an Output validator
  command to the Quality section (mirrors `automation-shape-routing`'s
  precedented Output Specification pattern) asserting a captured decision leads
  with exactly one of `SAFE_TO_CREATE|USE_RPI|UNSAFE_GOAL`. A deeper auto-run
  gate over the fourteen lint dimensions is deferred: they judge prompt content
  that has no persisted artifact at gate time, so there is nothing deterministic
  to check; they remain a documented human rubric. `validate.sh` kept as-is.

- **P1-CG-BOUNDARY** `[enhance]` — **applied.** Added an "Authority boundary"
  paragraph after the opening Insight: the emitted prompt/report are inert
  caller-owned text; crafting creates no goal, starts no runtime, mutates no
  bead, and confers no standing authorization; the prompt drives RPI dispatch
  only when the caller runs it in their own runtime under their own authority,
  inside the non-renewing envelope they set.

- **No `context:` block; bd/br/bv coupling undeclared** `[enhance]` —
  **applied.** Added a `context:` block (`window: inherit`, `intent.mode: task`,
  `intel_scope: topic`) — existing v2 field, no new schema. Made the tracker
  coupling explicit in the Bead graph contract: `bd`/`br`/`bv` is an external,
  caller-owned runtime the emitted goal drives; craft-goal reads live state when
  present but starts nothing and requires no tracker installed to compile a
  prompt. `stability: experimental` kept.

- **Craft-goal strength claims** `[disputed]` — **acknowledged, no action.**
  The audit's craft-goal strength claims (e.g. "highest craft score") were
  flagged unreliable by its own final review and were not relied upon; no
  strength claim was used to justify any edit.

## product

- **Aspiration-laundering detector** `[enhance]` — **applied (contract) /
  detector deferred.** Rewrote steps 3-4 to require two explicit headings in the
  emitted PRODUCT.md: `## Proven` (every claim carries a resolvable citation —
  README, release, test, or evidence source) and `## Assumptions` (unproven
  hopes, evidence gaps, unmeasured success signals). Added a detector rule to the
  failure-mode note: any `## Proven` claim without a resolvable citation is a
  laundered aspiration. The machine detector is deferred — product emits a
  caller-owned document and owns no gate over consumer-repo `PRODUCT.md` files.

- **No `context:` block** `[enhance]` — **applied.** Added a `context:` block
  (`window: inherit`, `intent.mode: task`, `intel_scope: topic`).

- **P1-PROD-BYTE (byte-preservation test)** `[enhance]` — **deferred (no
  deterministic writer).** Verified product has no CLI writer (`no ao product`
  command) and no `scripts/`; PRODUCT.md is authored by the agent. A
  byte-preservation test would exercise the harness/LLM, not a skill boundary.
  Building a deterministic `ao product refine` writer to test against is out of
  wave scope. The byte-for-byte preservation rule stays a documented agent
  contract (existing anti-pattern text).

## Disposition counts

- Fixed: 4 (ASR P1-ASR-SPEC, ASR P2-INVOCABLE, goals P0-EFFECTS, goals P1-GOALS-DRIFT)
- Applied (enhance): 4 full (craft-goal P1-CG-VAL, craft-goal P1-CG-BOUNDARY,
  craft-goal context/coupling, product context) + 2 partial
  (goals source-never-mutated, product aspiration-laundering contract)
- Deferred (enhance): 4 (ASR corpus parity gate, goals command-tree parity check,
  craft-goal deep lint gate, product P1-PROD-BYTE) + product laundering detector
- Rejected: 0
- Acknowledged, no action: 1 (`[disputed]` craft-goal strength claims)
- Confirmed-refuted note not carried: 1 (goals `--json`)

## Gate results

- `bash skills/craft-goal/scripts/validate.sh` → exit 0 (product/goals/ASR carry
  no validate.sh)
- `bash scripts/validate-skill-frontmatter.sh` → 49/49 ok, exit 0 (only
  pre-existing skill-builder `context_rel` warning; all four W2 skills OK)
- `bash scripts/regen-all.sh` then `--check` → "All generated projections are
  current", exit 0
- `bats tests/scripts/skill-validator-liveness.bats tests/scripts/anti-spiral-contract.bats`
  → 15/15 ok, exit 0
