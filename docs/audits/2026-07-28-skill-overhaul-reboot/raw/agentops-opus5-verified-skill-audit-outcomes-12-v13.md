# AgentOps — Corrected Deep Audit v13: Twelve Outcome/Support Skills

**Auditor:** the substantive twelve-skill audit is the Opus 5 lineage (v1–v11); **v12 was a bounded report-integrity attribution mapping correction authored by Fable 5, and v13 is a bounded four-site attribution repair over v12 authored by Opus 5**. Neither changes any substantive finding.
**Date:** 2026-07-28
**Mode:** read-only. No repository file was created, edited, or deleted; nothing generated, committed, merged, or pushed; no tracker record mutated; and no predecessor artifact edited or re-sealed.
**Status: advisory.** **No PASS claimed, and no AgentOps semantic verdict is issued.** This is an audit correction over a repository subject; only an author-distinct fresh context can bind a `PASS | FAIL | NOT_PROVEN` verdict, and this context is not that. **v13 confers no implementation authority**: it authorizes no repository remediation and binds nothing.

**Supersedes v12**, whose bytes at `/tmp/agentops-opus5-verified-skill-audit-outcomes-12-v12.md` are **SHA-256 `8fe38af42a681ae9eafea0af8f776831d7cd03dbf4a0158e90675b3ecbd02bcd`, 1,319 lines, 188,877 bytes** — measured directly from that path in the v13 pass, before the file was read. The **fresh v12 Sol review** (`4522542fe9ec83ea22ce3b7b04de6d8c47a88da422848b61154f4c774ba02d68`, 256 lines, 13,385 bytes) returned **REQUEST_CHANGES** on **attribution only**: it found **B3 repaired** (the §12 manifest narrative now says *five* recomputations, agreeing with its own enumeration and summary) and **every substantive twelve-skill criterion and all 106 manifest rows PASS**, leaving two attribution defects — **(B1)** broad v8-only provenance sentences at v12's §9.1 and close footer that contradict v12's own exact origin maps and omit the v6 and v7 origins, and **(B2)** one non-identifying validator-persona sentence in the `goals` §5.1 refutation. **v13 repairs exactly those four sites and nothing else.** It changes no finding, severity, count, classification, per-skill analysis, witness result, improvement, limitation, or manifest row, and it **re-ran no repository evidence** — no command, validator, gate, test, or witness. The complete v6/v7/v8/v9 origin enumeration is in §13; the per-probe review attributions are pinned at each point of use in §5.1, §8, §9.1–§9.4, and the close.

**The four attribution sites repaired in v13, exactly:**

| # | Site in v12 | Defect the v12 Sol review recorded | Repair in v13 |
|---|---|---|---|
| 1 | §5.1, the `goals` `--json` refutation (v12 line 307) | *“Sol re-executed `goals measure --json --help`”* — a validator persona, naming no originating artifact | Names the **v8 Sol review** (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`) as the originating execution, **carried, not re-run**, with the v9 review's independent re-confirmation cited |
| 2 | §9.1, the not-re-executed paragraph (v12 line 1039) | Assigned the 106-row re-derivation, the ratchet, the corpus/`effects`/layout sweeps, the validator reads, and *“every skill validator, gate, and test”* to the v8 pass and the v8 Sol review — contradicting §9.2–§9.4, v12's own §10 `not_checked` (the v8 pass ran no `go`, no `ao`, and no skill validator), and the exact origin map at v12 §9.4 | Replaced with the **heterogeneous origin table**: each structural re-derivation to its own v6/v7/v8/v9 author pass, and each carried executable to the exact review that ran it — v7 (`bb650549…`), v8 (`c57b71d4…`), v9 (`c22ce975…`) |
| 3 | §11 item 8 (v12 line 1158) | Claimed **v10** *“named the originating pass at every such point”* — refuted by site 1, which survived v10, v11, **and** v12 | States what is true: the discipline was reached by increments, one site escaped it through v12, and **v13 completes the mapping** |
| 4 | Close footer, inherited-probe hygiene (v12 line 1315) | *“the probes that produced this report's evidence were run in the **v8 pass** and independently reproduced by the **v8 Sol review**”* — omits the v6 and v7 author passes and the v6, v7, and v9 reviews | Replaced with the complete exact **v6–v9** author/review origin map, cross-referenced to §9.1–§9.4 and the §13 origin set |

*(v12's own supersession note, retained as lineage:)* **Supersedes v11**, whose bytes at `/tmp/agentops-opus5-verified-skill-audit-outcomes-12-v11.md` are **SHA-256 `6b5c12e5e9025be31e55f8392f100a195cb30fc1004b8bb52d10e27e2a13b396`, 1,315 lines, 184,894 bytes, mode `0444`** — the identity pinned by the fresh v11 Sol review. v12 repairs the three **report-integrity attribution defects** that review returned **REQUEST_CHANGES** on, all of which it found leave every substantive repository finding intact: **(B1)** the close footer gave an incomplete origin set that excluded the v6 and v7 executions §9.3/§9.4/§10 attribute to those passes; **(B2)** point-of-use checked-evidence blocks used the non-identifying phrase *Sol's fresh run(s)* / *Sol's fresh reviews* instead of naming the originating review pass; and **(B3)** the §12 manifest narrative said the rows were recomputed *three times* immediately before enumerating and summarizing *five*. **v12 changes no repository finding, severity, count, classification, witness result, per-skill judgment, improvement, limitation, or manifest row** — it is a report-integrity **attribution mapping correction only**, and it re-ran no repository command, validator, gate, test, or witness. The complete v6/v7/v8/v9 origin enumeration is in §13; the per-probe review attributions are pinned at each point of use in §8, §9.4, and the close.

*(v11's own supersession note, retained as lineage:)* **Supersedes v10**, whose bytes at `/tmp/agentops-opus5-verified-skill-audit-outcomes-12-v10.md` are **SHA-256 `b5d1f2aba267a2632f6a131b48ff126ab2f40e723ec04d120e9886bdd6306f5d`, 1,305 lines, 181,414 bytes, mode `0o600`** — measured directly from that path when v11 was assembled. v11 repairs three **footer and close-state integrity defects** that survived the v10 reseal: a stale atomic-write sentence naming the v9 path, a close that simultaneously said v10 reran nothing and that "all probes" ran under a post-run check, and a `sealed read-only` mode claim that did not match the file's actual mode. **v11 changes no repository finding, severity, count, witness result, per-skill judgment, improvement, or limitation, and preserves every v10 correction**, including `context.intent.mode`, `context.sections.exclude`, and the **9 owner-only / 9 template-only / 7 shared** classification.

**Identity discrepancy, disclosed rather than absorbed.** The task assigning this reseal supplied v10's identity as SHA-256 `6a83a81ecc3082f7cb3005677725389e7103e8b3defe5e174ec5b4ead5d43dc9`, 1305 lines / 181,397 bytes. **That digest does not match the bytes now at the v10 path** (`b5d1f2aba267a263…`, 181,414 bytes); the line count agrees at 1,305. The v10 author lane wrote that path twice — a first write, then a corrected second write that added four further point-of-use provenance labels — and only the second survives. v11 records the identity it **measured**, not the identity it was handed, because recording an unverified digest against a path is the exact defect class this lineage has been correcting since the v7 identity race (§0.1). **A fresh reviewer should treat `6a83a81e…` as an unrecoverable intermediate, not as the superseded artifact.**

*(v10's own supersession note, retained as lineage:)* v10 superseded v9 (`e31928f68edacafdaab3d84725a542276d661d967c4bbaa71fc2414229d5b30d`, 1299 lines, 178,895 bytes) on the **two report-integrity defects** in the fresh v9 Sol review (`c22ce97509b9642610e2449dc7ef1914ed05c124e015c8ca2b2bb4db3efad046`, 277 lines, 17,829 bytes): point-of-use execution provenance that contradicted v9's own execution ledger, and two flattened nested paths in the reverse standards set. **v10 changes no repository finding, severity, count, witness result, per-skill judgment, improvement, or limitation.** It is a **report-integrity reseal**: no repository command and no witness was rerun.

*(v9's own supersession note, retained as lineage:)* v9 superseded v8 (`e85dbbf9929d5639717daae967a4ce6e22802cf52b60ef6fafad84f8944ed15e`, 1214 lines, 163,315 bytes) on the **two report-integrity blockers** in the v8 Sol review (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`, 264 lines): a false claim that §§6–7 were untouched, and an off-by-one in the standards witness's assertion-1 path count. **That review found every substantive repository criterion PASS and all three v8 corrections substantively repaired.** No repository finding, severity, ledger unit, or count changes in v9.

*(v8's own supersession note, retained as lineage:)* v8 superseded v7 (`93563e25ce410789201f4e171d0dc36b6c74d746e83de90034ab3dae4d9b5645`, 1080 lines) on the three substantive findings in the v7 Sol review (`bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76`, 290 lines): a false generality claim in the toil discrimination table, a security witness incompatible with its own prescribed repair, and a behavioral-witness denominator that contradicted two of the report's own sections. **Every repository finding, count, and verified fact in v7 survives unchanged**, including all 106 embedded path/digest rows.

**Why v8 is a new subject, not a reseal of v7.** The v7 review was assigned an audit identity (`e99b15b2…c792`, 1076 lines) that did not match the bytes at the reviewed path (`93563e25…b5645`, 1080 lines). Its binding disposition is therefore **REQUEST_CHANGES** on identity grounds alone, independent of content. Its *substantive* checks nevertheless ran against the real sealed v7 bytes and independently reproduced the full manifest and counts, and passed both repaired witnesses — the toil floor/product/missing-factor cases and the standards three-surface semantic check. Editing v7 in place would leave two artifacts sharing one path with different digests and would not repair the identity failure. **v8 is written to a new path so the digest published below is the digest of the exact bytes offered for review.** §0.2 records the race in full.

**Self-containment contract.** v8 is readable and recheckable from its own bytes. It embeds the complete 106-row path/digest inventory (§12), a witness for each of the twelve skills recording its current state and supplying a discriminating accept/reject test (§8), a mechanical validator taxonomy whose counts can be re-derived with one command (§8.0), and every counting rule needed to recompute its own totals (§6, §7). It references v2–v7 for lineage only; no claim in v8 depends on holding them.

## Subject identity

| Field | Opening | Closing |
|---|---|---|
| Worktree | `/Users/bo/dev/agentops-worktrees/skill-overhaul` | same |
| Branch | `codex/skill-overhaul-20260724` | same |
| HEAD | `0088c6e3824da201eabb1e751ac8e976599e0b5c` | **verified identical** |
| HEAD tree | `c0c43eefb8042af5a6a7877c0f7f0de80149ffc6` | **verified identical** |
| `HEAD:skills` tree | `a264078d2795f3f7238e0c9ff5a8c15149a77c8d` | **verified identical** |
| `git status --porcelain=v1` | 0 lines | **0 lines** |

## Inputs and lineage

**Primary inputs to v13** (measured in the v13 pass with `shasum -a 256`, `wc -l`, and `wc -c` **before either file was read**, both matching the identities supplied with the task): the **v12 audit** (`8fe38af42a681ae9eafea0af8f776831d7cd03dbf4a0158e90675b3ecbd02bcd`, **1,319 lines / 188,877 bytes**) and the **fresh v12 Sol review** (`4522542fe9ec83ea22ce3b7b04de6d8c47a88da422848b61154f4c774ba02d68`, **256 lines / 13,385 bytes**, disposition **REQUEST_CHANGES** on attribution only). No other file was read as an input to v13, and no repository evidence was re-derived by it.

*(v11's own statement of its inputs, retained as lineage:)* **Primary inputs to that reseal** (measured in the v11 pass, read in full): the **v10 audit** (`b5d1f2aba267a263…`, 1,305 lines / 181,414 bytes) and the **v9 Sol review** (`c22ce975…ad046`, 277 lines / 17,829 bytes) — at that time the only binding review in this lineage. The v8-era rows below are retained as lineage:

| Input | SHA-256 | Extent |
|---|---|---|
| v8 audit `…-outcomes-12-v8.md` | `e85dbbf9929d5639717daae967a4ce6e22802cf52b60ef6fafad84f8944ed15e` | **1214 lines / 163,315 bytes** |
| Sol review on v8 `…-outcomes-12-v8-review-sol.md` | `c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66` | **264 lines / 15,953 bytes** |

Both were SHA-verified against the values supplied with the task **before** either was read. v9 is seeded from the exact v8 bytes and edited into a new path, so every unedited line — including all 106 inventory rows — is byte-identical to the reviewed artifact by construction.

*(v8's primary inputs, retained as lineage:)*

| Input | SHA-256 | Extent |
|---|---|---|
| v7 audit `…-outcomes-12-v7.md` | `93563e25ce410789201f4e171d0dc36b6c74d746e83de90034ab3dae4d9b5645` | **1080 lines** |
| Sol review on v7 `…-outcomes-12-v7-review-sol.md` | `bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76` | **290 lines** |

Both were SHA-verified against the values supplied with the task before either was read. v8 is seeded from the exact v7 bytes and edited into a **new path**, so every unedited line — including all 106 inventory rows — is byte-identical to the reviewed artifact by construction, not by re-transcription; and the 106 rows were additionally re-derived and re-hashed against the live tree in the v8 pass (§9.1 records that neither v9 nor v10 re-derived them).

**Historical lineage only — not inputs to this correction:**

| Artifact | SHA-256 | Extent | Status |
|---|---|---|---|
| v6 audit | `238d5c020fab29139029e27b923a5f76e579c2be0eb7713f3a779f177f1af97e` | 941 lines | v6 lineage |
| Sol advisory on v6 | `dfc1cc41d1e22ba8f6d5496e6c8af0b00d1b4f2723f70fb086593066992c4bc4` | 305 lines | v6 lineage |
| v5 audit | `93cf769b1b03c5e27808dd9f30f80b05f4cdc70a5484cfa695fa6f22066f7a17` | 550 lines | v5 lineage |
| Sol advisory on v5 | `d20611ae0067a1d894f9db33522031b66e0f585230547e4c9641471352a161b0` | 271 lines | v5 lineage |
| v4 audit | `dcfa65341482ac249cda45795f3a364573c90e17f5bd729d630be5aa19b44448` | 1185 lines | v4 lineage |
| Sol advisory on v4 | `be247c9c7a4e053f510bae3702bed582a673689a0e3489a0752fef454447cadf` | 277 lines | v4 lineage |
| v3 audit | `c0a583ffce8147353e7afdf27c31577e532c8d3dec236d4963e2ecf1dacf0ff0` | 1003 lines | v3 lineage |
| Sol advisory on v3 | `3434b8c4fe7d5ff0acc078d479cbc566f5e2fa2d74f555430ff283245047c971` | 242 lines | v3 lineage |
| v2 audit | `d8261742d373e0f9986cb2368a7e567f7e3afc079d5623893014040aaf10f394` | 685 lines | v2 lineage |
| Sol advisory on v2 | `77f269af4c53b158017810fb43acb981ba6899139092f004880510af6740040b` | 434 lines | v2 lineage |

## 0. v8 → v9 correction ledger

The v8 review returned **REQUEST_CHANGES** on exactly two report-integrity claims. It found **every substantive repository criterion PASS** — the exact 106-path subject, the 8 / 21 / 6 ledger with multiplicity, the five effects buckets, the 49 / 43 / 6 fallback with four complete and two incomplete profiles, goals, security current behavior and the corrected future gate, the Python ratchet at both scopes, the closed-layout destinations, all three toil witness cases, the 6 / 3 / 3 validator taxonomy, the corrected residual claim, five improvements in every section, and all twelve one-by-one judgments — and it confirmed all three v8 corrections substantively repaired. **Neither blocker changes a repository finding, a severity, a ledger unit, or a count.**

| Sol blocker | Correction in v9 | Section |
|---|---|---|
| **B1 — the v7 → v8 provenance statement contradicts the diff.** v8 line 63 said *"§6 and §7 are untouched."* The first half of that sentence is true; the second is false. The direct v7 → v8 diff changes **§6.2.1**: a v8 row is added to the §8-extent table, the narrow-extent sentence gains `514` and the 13-heading explanation, and the growth paragraph gains the v7 → v8 clause. | **Replaced with a subsection-exact statement, measured in the v9 pass** (§0.1.1). §7 is **byte-identical** (17 lines, zero diff lines). Within §6, the four **finding-ledger** subsections are **byte-identical** — §6.1 P0 (16 lines), §6.2 P1 (39 lines), §6.3 P2 (14 lines), §6.4 fallback (5 lines) — while **§6.2.1 alone changed**, by 5 diff lines across exactly 3 edits, all of them **provenance/extent rows**. The claim now distinguishes **ledger membership and totals (untouched)** from **provenance measurements (updated)**, which is the distinction the false sentence collapsed. | §0.1.1, §6.2.1 |
| **B2 — standards assertion 1 undercounts the path-set difference.** v8's three-surface table said the owner *"declares 8 paths absent from `skill-template.md`"*. Assertion 1 requires equality of the **complete nested-path sets**, and `metadata.effects` is an owner-declared path absent from the template — so the complete set is **nine**, not eight. | **Corrected to nine throughout, and the arithmetic made explicit** so table, prose, totals, and witness explanation agree. Re-derived mechanically this pass from the owner block (`skill-structure.md:35-50`) against the template's canonical block (`skill-template.md:20-46`): **nine** owner paths absent from the template — `practices`, `hexagonal_role`, `consumes`, `produces`, `context_rel`, `metadata.capabilities`, **`metadata.effects`**, `metadata.canonical_status`, `metadata.disposition`. The prose that reads "independently of `effects`" keeps that framing but now states `8 + metadata.effects = 9` explicitly, so no reader has to reconcile two numbers. | §8.11, §9.1 |

**A precision note on B2's own wording.** The review states that *"the surrounding prose itself enumerates all nine at v8 line 791."* It does not — line 791 enumerates **eight** and explicitly scopes itself *"independently of `effects`"*, which is internally consistent. The defect was never a contradiction between the prose and the table; it was that **the table row sits under assertion 1, which requires the complete set**, and therefore had to include `metadata.effects`. v9 fixes the real defect and does not adopt the review's account of where the inconsistency lay.

**A fact v8 did not record, measured in the v9 pass** *(path names corrected in v10 — see below)*. The path-set difference is **nine in both directions**: nine owner paths absent from the template (above), and nine template paths absent from the owner — `user-invocable`, `context`, `context.window`, `context.intent`, `context.intent.mode`, `context.sections`, `context.sections.exclude`, `context.intel_scope`, `metadata.stability`. Seven paths are shared: `name`, `description`, `skill_api_version`, `metadata`, `metadata.tier`, `metadata.dependencies`, `output_contract`. This strengthens assertion 1's failure description without changing any finding.

**Preserved from v8, unchanged:** all 106 embedded path/digest rows; 8 P0 classes / 21 unique P1 units / 6 P2 classes with multiplicity (`P1-LAYOUT` ×4, `P1-INIT-OC` ×2, 25 occurrences over 21 units); the five effects buckets; 49 / 43 / 6 with four complete and two incomplete fallbacks; the goals, security, Python-ratchet, and closed-layout facts; all twelve one-by-one intent / RPI-fit / live-behavior analyses with five improvements each; the toil metamorphic, product-order, and missing-factor witnesses; the standards three-surface semantic witness (design unchanged, count corrected); the canonical-root security future gate; the §8.0 behavioral-witness classification and its 6 / 3 / 3 counts; and the full provenance chain including the identity race.

**Nothing in v9 is a new repository finding, a new ledger unit, or a changed severity.** Both corrections are to the report's claims about itself.

### 0.1.1 Exactly what changed between v7 and v8, measured

Direct diff of the sealed artifacts: **165 lines inserted, 31 deleted.** Section-level identity, computed in the v9 pass:

| Section | v7 | v8 | Result |
|---|---:|---:|---|
| §6.1 — P0 ledger | 16 | 16 | **byte-identical** |
| §6.2 — P1 ledger | 39 | 39 | **byte-identical** |
| **§6.2.1 — set equality and multiplicity** | 36 | 37 | **changed — 5 diff lines, 3 edits, all provenance/extent** |
| §6.3 — P2 ledger | 14 | 14 | **byte-identical** |
| §6.4 — output fallback | 5 | 5 | **byte-identical** |
| §7 — recomputed totals | 17 | 17 | **byte-identical** |

The three §6.2.1 edits, exactly:

1. **inserted** — `| v8 | **561** | **12** | 21 IDs — unchanged |`, a new row in the §8-extent table;
2. **replaced** — the narrow-extent parenthetical, gaining `/ 514` and `/ v8` and the 13-heading explanation;
3. **replaced** — the growth paragraph, gaining the v7 → v8 clause.

**What that means, stated exactly:** every **normalized finding ID, severity, ledger membership, and total** is untouched from v7 through v9 — §6.1, §6.2, §6.3, §6.4, and §7 are byte-identical across all three. What changed in §6.2.1 is **only the provenance and extent measurements the section publishes about the report's own lineage**. v8's sentence "§6 and §7 are untouched" was true of the ledgers and false of §6.2.1's provenance rows, and collapsing that distinction is what made it a false claim.

### 0.2 Carried forward — the v7 → v8 correction ledger

The v7 review returned **REQUEST_CHANGES** on two grounds: an identity mismatch (§0.2.1) and three substantive precision findings. It reproduced every repository fact — the 106-row manifest, the 8 / 21 / 6 normalized sets, shared-ID multiplicity, the five effects buckets, the output fallback, and the goals, security, Python-ratchet, closed-layout, and migration-authority facts — and it **passed both witnesses repaired in v7**: the toil floor/product/missing-factor cases and the standards three-surface semantic check. All three substantive findings are correct and are accepted without argument.

| Sol finding | Correction in v8 | Section |
|---|---|---|
| **B1 — one fixture cannot defeat every fixed order.** v7's discrimination table carried the row `any fixed constant order \| fixed \| No — a specific order is asserted`, and §10 generalized to "every degenerate order … constant". **False:** a scorer hard-coded to emit `C, D, B` passes the fixture. The fixture does defeat frequency-only, cost-only, error-only, source-order, and reverse-source-order — it does not defeat a constant. | **Both remedies applied, not one.** (a) The false row is **deleted** and the base fixture's claim narrowed to exactly what one fixture proves — five named degenerate rules, constant explicitly *not* among them. (b) A **deterministic metamorphic companion (case 2)** is added that does defeat every constant: the three factor-triples are held as a fixed multiset and reassigned across the three cluster identities under all **3! = 6** permutations, with source order held constant. Verified mechanically this pass: the six permutations yield **six distinct expected orders — all 6 orderings of three labels — so any constant emitter passes exactly 1/6 and fails 5/6**, as do source-order and label-sort emitters. The §10 guard sentence drops the word `constant` and now points at the metamorphic requirement instead. | §8.12, §10 |
| **B2 — the security gate witness contradicts its own repair.** Improvement 1 deletes `skills/security/scripts/security-gate.sh`; v7's witness then proposed running that package-local path and accepting only when it reaches the toolchain. After the prescribed deletion the command does not exist, so the witness can never go green. | **The future check now executes the canonical root gate and asserts the duplicate is gone.** Verified this pass: `scripts/security-gate.sh` exists at the repository root, `scripts/toolchain-validate.sh` **exists** at the root (which is exactly why the root copy resolves and the package-local copy does not), and `SKILL.md:52`, `:67`, and `:77` already document the **root** path. The rewritten witness runs the root gate, asserts `gate_status == "PASS"` via the validator predicate the skill itself publishes at `SKILL.md:129`, and **separately asserts `skills/security/scripts/security-gate.sh` does not exist**. Both halves are satisfiable only *after* improvement 1 lands, so witness and repair are now compatible. The package-local exit 1 is retained as **current** evidence for the duplicate-path defect, where it belongs. | §8.9 |
| **B3 — the behavioral-witness denominator contradicts the report's own sections.** v7 twice called toil-mining "one of only two behavioral validators" and §11 said "ten of the twelve have no witness that can fail on behavior" — while the same report described automation-shape-routing's `awk` boundary as a real machine boundary, pattern-mining's live `jq` gate with two passing fixtures, and skill-builder's "two real ones" (a mutation-boundary proof and self-witnessing invariants). The narrower class was never defined. | **Every such total is removed and replaced by a defined, mechanically reproducible taxonomy** in **new §8.0**, built by reading all six package validators in full this pass rather than by recollection. Classifying the twelve by *what the package-local validator executes*: **6 have no package validator** · **3 have one that executes no skill logic** (craft-goal structural, doc prose, security prose+syntax) · **3 have one that executes real skill logic** (pattern-mining, skill-builder, toil-mining). 6 + 3 + 3 = 12. "Only two" and "ten of twelve" are **withdrawn**, along with every count derived from them. The narrower residual-risk claim the review found supported — five named high-value guarantees remain unwitnessed — is retained and now stated *without* a denominator. | §8.0, §8.12, §11 |

**Preserved from v7, unchanged:** all 106 embedded path/digest rows, re-verified against the live tree in the v8 pass; 8 P0 classes / 21 unique P1 units / 6 P2 classes; shared-ID multiplicity (`P1-LAYOUT` ×4, `P1-INIT-OC` ×2, 25 occurrences over 21 units); the five effects buckets (4 / 5 / 1 / 1 / 1); 49 live / 43 with `output_contract` / 6 without, with 4 complete and 2 incomplete fallbacks; the goals, security, Python-ratchet, and closed-layout facts; five prioritized improvements in every section; all twelve one-by-one intent / greater-RPI-fit / live-control-flow analyses; and **the ten witness blocks the review reproduced as discriminating, plus the two v7 repairs it passed** — the toil floor/product/missing-factor coverage and the standards three-surface check both survive intact, extended rather than replaced.

**Nothing in v8 is a new repository finding, a new ledger unit, or a changed severity.** The three corrections are to the report's own claims about its witnesses, not to what the repository does. **§7 is byte-identical, and §6's four finding-ledger subsections — §6.1, §6.2, §6.3, §6.4 — are byte-identical; §6.2.1's provenance/extent rows were updated by three edits** (§0.1.1). *(v8 stated this as "§6 and §7 are untouched", which was true of the ledgers and false of §6.2.1's provenance rows; corrected in v9 — F1.)*

### 0.2.1 The identity race, recorded

| Field | Assigned to the v7 review | Actually present at the reviewed path |
|---|---|---|
| SHA-256 | `e99b15b2e640e6f3bcde3f4630244744212c27637a853d034326813faf40c792` | `93563e25ce410789201f4e171d0dc36b6c74d746e83de90034ab3dae4d9b5645` |
| Extent | 1076 lines | 1080 lines |

**What is known.** v7 was authored by seeding from v6 and applying edits in sequence; it was sealed `0444` only after the final edit. The assigned digest corresponds to **an intermediate state during that authoring**, sampled before the last edits landed — the final two changes filled in v7's own measured §8 extent and extended two §9.1 probe rows. The sealed artifact is `93563e25…b5645` / 1080 lines, which is the digest v7 itself published on delivery and the bytes this correction consumed.

**What is not known, and is not asserted.** The exact intermediate that hashes to `e99b15b2…c792` was never sealed and no longer exists; it cannot be reproduced or verified from here. **This report does not claim to know which intermediate it was** — only that it was not the sealed v7, and that no artifact with that digest was present at the path. A reviewer holding only the assigned digest is correct to refuse to bind.

**Why it happened, and the guard.** The failure is a race between *sampling an artifact for review* and *sealing it*: a digest was taken from a file still being written. Guard adopted here: **an audit digest is quotable only after the artifact is sealed read-only**, and a correction that changes an artifact's bytes goes to a **new path** rather than mutating a path someone may already be reviewing. v8 follows both rules — it is a new path, and its digest is published only after the final byte is written and the file is sealed.

### 0.3 Carried forward — the v6 → v7 correction ledger

The v6 advisory returned **REQUEST_CHANGES** on proof design, not on findings. It confirmed the embedded 106-row inventory, the normalized 8 / 21 / 6 sets, the shared-ID multiplicity wording, the five effects buckets, the 49 / 43 / 6 fallback inventory, and the goals, security, Python-ratchet, closed-layout, migration-readiness, and provenance facts — and it independently reproduced ten of the twelve witness blocks as discriminating. Two were not.

| Sol blocker | Correction in v7 | Section |
|---|---|---|
| **B1 — the restored toil fixture cannot test ranking.** v6's fixture defined only cluster A (2 occurrences, excluded by the three-occurrence floor) and cluster B (9 occurrences). Once A is correctly excluded, **B is the only eligible cluster**, so "ranks B above any cluster with a lower product" is vacuous and the reject condition "if any single factor determines the order" is unobservable. **The advisory is correct and the defect is mine:** v6 tested the floor and claimed to test ordering. | **The fixture is rebuilt with four clusters — one below-floor and three eligible — and every factor and expected product is published as a number.** Cluster A (2 occ, cost 900, error 0.50) is narrated "we do this constantly" and carries the fixture's **largest** would-be product, so the floor must exclude it *despite* scoring ranking it first — proving the floor precedes ranking rather than acting as an incidental tiebreak. The three eligible clusters are chosen so that **product order (C > D > B) differs from frequency order, cost order, error order, source order, and reverse-source order simultaneously** (§8.12 discrimination table). A **separate** two-cluster case (E/F) tests the floor-versus-midpoint rule without entangling it with ordering. | §8.12 |
| **B2 — the standards witness cannot detect `P0-TEMPLATE`.** v6 compared `skill-structure.md` against `heal.sh` and `init.sh`, **omitting `skill-template.md` entirely**, and compared **key sets** — but `metadata.effects` is present in both `skill-structure.md` (`:45`) and `init.sh` (`:88`), so a presence comparison **cannot fail on `effects`**. v6's claim that it "fails today on `output_contract` and on `effects`" was half true: it fails on `output_contract` only. | **Replaced with a three-surface semantic comparison** over `skills/standards/references/skill-structure.md`, `skills/skill-builder/references/skill-template.md`, and `skills/skill-builder/scripts/init.sh`, normalized to **nested key paths plus default-value semantics** and compared against one declared owner. It asserts `metadata.effects` is **deliberate rather than silently empty** and that `output_contract` is **emitted and validated**. It **fails on all three surfaces today, in three different ways** — owner seeds `[]`, template omits the key entirely, emitter defaults `${SKILL_EFFECTS:-[]}`. All three surfaces were read verbatim this pass; the defect table is published with line anchors. | §8.11 |
| **Precision — the lineage sentence overclaims.** §6.2.1 said occurrences were measured over "v5's §8, which v6 preserves unchanged." False: only the **normalized P1 membership** is unchanged; §8 itself grew substantially when the witnesses were restored. | Corrected to the narrower true statement, with **both extents measured this pass and the measurement convention published** so the numbers are reproducible: measuring `## 8.` through the `## 9.` terminator inclusive, **v5 §8 = 181 lines with 0 `Witness` blocks; v6/v7 §8 = 413 lines with 12**. (A narrower range starting at `### 8.1` yields 175 / 405 — the same fact under a different boundary; the convention is stated so the two never conflict.) | §6.2.1 |
| **Precision — the witness-shape summary overclaims.** §8's preamble said every section states both a current *and* a future accept/reject shape. Several sections honestly record that **no validator exists** as the current state, and `pattern-mining` deliberately declines a future behavioral witness because its current one already discriminates. | Reworded: each section **records its current witness state — including the honest absence of any validator — and supplies a present or future discriminating accept/reject witness as appropriate.** No claim is made that every current state has a validator, nor that `pattern-mining` proposes an unnecessary future one. | §8 preamble |

**Preserved from v6, unchanged:** all 106 embedded path/digest rows (byte-identical by construction — v7 is seeded from the v6 bytes); 8 P0 classes / 21 unique P1 units / 6 P2 classes; shared-ID multiplicity (`P1-LAYOUT` ×4, `P1-INIT-OC` ×2, 25 occurrences over 21 units); the five effects buckets (4 / 5 / 1 / 1 / 1); 49 live / 43 with `output_contract` / 6 without, with 4 complete and 2 incomplete fallbacks; the goals, security, Python-ratchet, closed-layout, and provenance facts; five prioritized improvements in every section; and all twelve intent / greater-RPI-fit / live-behavior analyses. **The ten witness blocks the advisory reproduced as discriminating are unchanged.**

**Nothing in v7 is a new repository finding, a new ledger unit, or a changed severity.** Both replaced witnesses are *specifications for future tests*; neither was implemented or run, and neither alters §6 or §7. The one substantive addition is the three-surface template defect table in §8.11, which supplies line anchors for a defect (`P0-TEMPLATE`) v6 already reported at the same severity.

### 0.4 Carried forward — the v5 → v6 correction ledger

| Sol blocker | Correction in v6 | Section |
|---|---|---|
| **B1 — the path/digest inventory is not present in v5.** v5 §12 carried the heading "Per-file SHA-256 inventory (106 files)" with zero rows, calling an absent appendix "preserved by reference to v4 §12." | **All 106 path/digest rows are embedded in §12.** They were **recomputed against this exact landing in this pass** — not copied forward on trust — by re-deriving membership from `git ls-tree -r HEAD:skills/<slug>` for the twelve and hashing each blob. The recomputed set was then diffed against v4's 106 rows: **zero missing, zero extra, zero digest mismatches** (`diff` reported the files identical). Package counts `2, 3, 17, 1, 1, 1, 3, 1, 11, 46, 16, 4 = 106` are retained and were re-derived the same pass. The "preserved by reference" formulation is **withdrawn**. | §1, §12 |
| **B2 — the one-by-one witnesses were shortened out.** v4 had twelve explicit `Witness` blocks; v5 had none. For `toil-mining` the loss was material: v5 still reported the ranking contract as prose-only while its five Improvements contained no behavioral repair. | **An explicit `Witness` block is restored to every one of the twelve §8 sections**, each **recording its current witness state** and **supplying a present or future discriminating accept/reject witness as appropriate**, mapped to a numbered improvement in the same section. *(v6 worded this as "current and future" for all twelve; corrected in v7 — see §0 and the §8 preamble.)* Still-valid v4 witnesses are recovered; where v5 changed a finding the witness is corrected to match (goals' three discriminating witnesses replace the withdrawn `ao …`-parse witness; skill-builder gains a build-report-effect witness for the new P0 member; security gains the `validate.sh:9` deadlock clause). **The missing toil-mining ranking repair is authored in full** (§8.12) — a two-cluster fixture in which measured occurrence, cost, and error-proneness values must defeat a narrative "we do this constantly," the three-occurrence floor must exclude the narrated cluster at its real measured count, and the ordering must follow the declared `frequency × cost × error-proneness` product. It is carried as **improvement 4** in that section, so the repair is in Improvements and not only in the witness. | §8.1–8.12 |
| **B3 — the P1 "exactly one section" sentence is false.** v5 §6.2 claimed every P1 ID "appears in exactly one §8 section." | **Rewritten as set equality over normalized canonical IDs, not occurrence bijection.** The aggregate normalized canonical-ID set and the per-skill normalized canonical-ID set are **equal and both contain 21 members**. Occurrence counts exceed 21 because shared class and companion repairs are referenced by every skill they touch: **`P1-LAYOUT` occurs in four sections** (`doc`, `pattern-mining`, `skill-builder`, `toil-mining`) and **`P1-INIT-OC` occurs in two** (`skill-builder` as owner, `standards` as companion). 19 IDs occur once; total occurrences **25** over **21** unique repair units. Ownership-versus-reference multiplicity is defined explicitly so each canonical unit is still counted once. **The "exactly one section" claim is withdrawn.** | §6.2 |

**Preserved from v5, unchanged and re-verified where cheap to re-run:** 8 unique P0 classes / 21 unique P1 units / 6 unique P2 classes; the five-bucket effect accounting (**4 accurate empty, 5 false empty, 1 coherent nonempty, 1 ambiguous nonempty, 1 incomplete nonempty**); the `skill-builder` build-report effect gap and its P0 class membership; the four newly mapped P1 units `P1-SEC-VAL`, `P1-OP-RETURN`, `P1-CG-BOUNDARY`, `P1-HEAL-MESH`; the honest three-tier executed / source-inspected / carried provenance; 49 live skills / 43 with `output_contract` / 6 without, with 4 complete and 2 incomplete fallbacks; and the goals, security, toil-mining, closed-layout, Python-ratchet, inventory, and mutation-harness facts.

*(v6 statement, retained as lineage:)* **Nothing in v6 was a new repository finding.** Every defect was already established; v6 changed the report's completeness, not its verdicts. Where v6 stated something v5 did not, it was either a probe re-executed in that pass (§9) or a witness/repair made explicit — never a new severity or a new ledger unit. The same holds for v7 relative to v6.

## 1. Mechanical counts — re-derived in the v8 pass

Executed with `git ls-tree -r --name-only "HEAD:skills/<slug>"`:

| Skill | Tracked files |
|---|---:|
| `automation-shape-routing` | 2 |
| `craft-goal` | 3 |
| **`doc`** | **17** |
| `domain` | 1 |
| `goals` | 1 |
| `operationalize` | 1 |
| `pattern-mining` | 3 |
| `product` | 1 |
| `security` | 11 |
| `skill-builder` | 46 |
| `standards` | 16 |
| `toil-mining` | 4 |
| **Total** | **106** |

Sequence reproduced exactly in the v8 pass: `2 3 17 1 1 1 3 1 11 46 16 4 = 106`. **The 106-entry digest appendix is embedded at §12**, recomputed against this landing.

## 2. Authority — T2 artifacts are shadow, not authoritative *(preserved)*

- `skills/skill-builder/SKILL.md:273-275` — the **shadow** compiler validates one named `metadata.contract_v3` declaration **without changing live API1 or catalog-v3 authority**.
- `SKILL.md:320-321` — generated inventories, the readiness ledger, and shadow receipts are **not live routing authority**.
- The active plan's **D4** keeps legacy API1 and generated `skill-catalog.v3` authoritative until one all-catalog cutover; its status line reads **`in_progress/T1-complete`**.

The ledger result (`49 rows; 1 ready; 48 explicit blockers`) is mechanically correct and stays cited as such. **That exactness does not make it authority.** `EARLY_CONTRACT_V3` remains valid only in its governed migration context.

### 2.1 Intended placement — the active plan's target matrix

| Skill | Target primary layer | Target seam(s) | Tranche |
|---|---|---|---|
| `automation-shape-routing` | support | cross_cutting, standalone | T3 |
| `craft-goal` | campaign | goal_design | T3 |
| `doc` | implementation | implement_method | T5 |
| `domain` | evidence | plan_input, cross_cutting | T4 |
| `goals` | product | product_input, goal_observe | T3 |
| `operationalize` | evolution | post_verdict, plan_input | T6 |
| `pattern-mining` | evolution | post_verdict, plan_input | T6 |
| `product` | product | product_input | T3 |
| `security` | evidence | validate_evidence, standalone | T4 |
| `skill-builder` | implementation | implement_method | T2 |
| `standards` | evidence | plan_input, implement_method, validate_evidence | T4 |
| `toil-mining` | evolution | goal_observe, post_verdict | T6 |

**None of the twelve holds any of the four restricted authority verbs, and all twelve declare `dependencies: []`.**

## 3. ADR-0016 — two independent parts

### 3.1 The Python ratchet is RED *(re-executed in the v8 pass)*

```text
bash scripts/check-skill-python-ratchet.sh --scope head      → exit 0
bash scripts/check-skill-python-ratchet.sh --scope upstream  → exit 1
  skill-python-ratchet: 12 new shipped-Python file(s); 0 stale grandfather entry/entries; snapshot-grew=0.
  skill-python-ratchet: 24 grandfathered file(s) remain on the execution path.
```

Head scope compares only the last commit; **upstream evaluates the branch landing, which is what the ADR gates.** Exactly **five** of the twelve branch-wide failures are in audited scope, all in `skill-builder` — enumerated directly from the v8 pass's upstream run:

```text
skills/skill-builder/scripts/check_migration_readiness.py
skills/skill-builder/scripts/compile_contracts.py
skills/skill-builder/scripts/contract_v3.py
skills/skill-builder/scripts/probe_runtime.py
skills/skill-builder/scripts/run_contract_probe.py
```

None is grandfathered; the growth guard rejects adding them.

**The 5 / 8 / 9 split over all 22 tracked `.py` in the twelve** *(preserved; grandfather membership source-inspected)*:

| Class | Count | Files |
|---|---:|---|
| New, ungrandfathered, blocking | **5** | the five above |
| Grandfathered execution path | **8** | `security/scripts/{prompt_redteam,security_suite}.py`; `skill-builder/scripts/{authoring_scan,conformance_profile,craft_score,scan_descriptions,score_agentops_skill}.py`; `toil-mining/scripts/recent_human.py` |
| Outside the governed user-execution class | **9** | 4 under `skills/*/tests/**`, 5 under `skills/skill-builder/fixtures/contract-v3/probe-harnesses/**` |

### 3.2 Four forbidden closed-layout destinations *(re-executed in the v8 pass)*

A `.agents/` destination sweep across the twelve returned exactly five distinct top-level names: `ao` (canonical) plus four forbidden.

| Skill | Forbidden destination | Where |
|---|---|---|
| `doc` | `.agents/doc/` | `SKILL.md`, repeated in `references/default-mode.md` |
| `pattern-mining` | `.agents/patterns/` | `SKILL.md` output contract |
| `skill-builder` | `.agents/audits/` | `SKILL.md:316`, **implemented** in `scripts/init.sh:149-150` and `scripts/build.sh:32` |
| `toil-mining` | `.agents/toil-mining/` | `SKILL.md` output contract |

**Not violations:** `.agents/ao/` uses by `skill-builder`, `operationalize`, and `goals` are canonical. `goals`' defect is an undeclared effect, **not** a layout fault. **Enforcement is absent** — `fm-ws-noncanonical-topdir` appears only inside the ADR text. **Repair is relocation, not a declaration change.**

## 4. Effects accounting — five buckets *(preserved from v5)*

*(Frontmatter `effects` read for all twelve in the v8 pass; write behavior source-inspected in v8 or carried as noted.)*

| Bucket | Skills | Count |
|---|---|---:|
| **Accurate empty** | `automation-shape-routing`, `craft-goal`, `domain`, `standards` | **4** |
| **False empty** — declares `[]`, writes | `doc`, `goals`, `pattern-mining`, `security`, `toil-mining` | **5** |
| **Coherent nonempty** — declaration matches behavior | `product` (`write_product_document` + `produces: [PRODUCT.md]`) | **1** |
| **Ambiguous nonempty** — declaration cannot be reconciled with the body | `operationalize` (`write_advisory_proposal` vs "Return the proposal to the caller") | **1** |
| **Incomplete nonempty** — declaration is true but does not cover all real filesystem effects | **`skill-builder`** | **1** |

4 + 5 + 1 + 1 + 1 = 12. Raw frontmatter in the v8 pass: **9 empty, 3 nonempty** — the three nonempty are `operationalize`, `product`, `skill-builder`.

### 4.1 The `skill-builder` omission, exactly

v4 §4 placed `skill-builder` in an undifferentiated "nonempty" bucket and §8.10 strengthened that to "**Effects typed and honest**." **That conclusion is false.**

**The live writer** *(re-read verbatim in the v8 pass)*:

- `skills/skill-builder/scripts/init.sh:149` — `mkdir -p "$REPO_ROOT/.agents/audits"`;
- `skills/skill-builder/scripts/init.sh:150` — `report="$REPO_ROOT/.agents/audits/${slug}-build.json"`, written by the following heredoc;
- `skills/skill-builder/scripts/build.sh:32` — `report="$REPO_ROOT/.agents/audits/${slug}-build.json"`, rewriting the same path;
- `skills/skill-builder/SKILL.md:316` — "The build report is `.agents/audits/<slug>-build.json` and conforms to …".

**The declaration** *(read in the v8 pass)*. API1 names exactly three effects:

```text
writes_skill_source · regenerates_skill_projections · optional_skill_projection_repair
```

The shadow rail's scopes are narrower still *(read verbatim at `SKILL.md:42-50`)*:

- `source-write` — `kind: filesystem.write`, scope **"explicitly named `skills/<slug>` source package"**;
- `projection-write` — `kind: filesystem.write`, scope **"explicitly invoked owned skill projections"**.

`.agents/audits/<slug>-build.json` is **neither** a skill-source write nor a projection write. No declared effect covers it.

### 4.2 `artifacts.produces` is not effect authority — preserved distinction

`build-report` **is** declared under `metadata.contract_v3.artifacts.produces` (`kind: report`, `semantics: factual`, `schema_ref: skills/skill-builder/schemas/build-report.json`). That declaration describes **what artifact the skill emits**; it does not describe, authorize, or scope the **filesystem effect** of writing it. The two rails answer different questions, and an artifact declaration cannot discharge an effect declaration. This distinction is the reason `skill-builder` is *incomplete nonempty* rather than *false empty* — its declaration is true as far as it goes.

### 4.3 Severity, applied consistently

Under §7.0's class rule, the governing class is **"the `effects` declaration does not describe real filesystem behavior."** Its members are the five false-empty skills **and** `skill-builder`'s uncovered build-report write — six members, one class, one severity: **P0**. Ranking `skill-builder`'s omission lower because its declaration is partly true would reintroduce exactly the per-skill-blast-radius reasoning §7.0 rejects.

**`operationalize` is deliberately *not* in that class.** Its declaration is not false and not incomplete — it is *ambiguous*, and the repair is to resolve the ambiguity rather than to add a missing effect. It is ranked **P1** as its own unit (§6.2 member 20).

## 5. `goals` — refutation and replacement *(preserved)*

### 5.1 REFUTED: the `--json` P0

`cli/cmd/ao/root.go:153` registers a **global persistent** `--json` flag; v2 cited line 152 and stopped one line short. `cli/internal/commands/goals/module_test.go:97-99` deliberately asserts goals must **not** register a local `--json`. The **v8 Sol review** (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`) executed `goals measure --json --help` → exit 0, `--json` under Global Flags; **that result is carried here, not re-run by this pass or by any pass after v8**, and its global-`--json` treatment was independently re-confirmed by the **v9 Sol review** (`c22ce975…`). **P0-6 is REFUTED** and is not carried.

### 5.2 The replacement finding *(source-inspected)*

- **Eight subcommands, not six** (`cli/internal/commands/goals/module.go:112-119`): `measure`, `validate`, `drift`, `history`, `export`, `meta`, **`scenarios`**, **`render`**. The skill documents six.
- **`RunMeasure` reaches `SaveSnapshot` only on the successful ordinary, non-directives path.** `cli/internal/goals/commands.go:171` sets `SnapDir = .agents/ao/goals/baselines`, `:207` calls `SaveSnapshot` — but four gates return first: a `LoadGoals` failure; the `--directives` branch (`:182-191`); `ValidateGoals` errors; an `applyMeasureFilters` error. The write is also best-effort — a save failure warns and the command continues.
- `RunExport` saves when no baseline exists (`:389`, `:399`); `RunDrift` creates or updates (`:484`, `:495`, `:504`); `render --out` writes a caller-selected file (`module.go:335`).

`goals` is therefore **not effect-free**, and the destination is canonical `ao/`, so this is a **declaration defect only**.

## 6. Finding ledger — canonical IDs

**Counting rule, stated once.** *Two defects are separate repair units if either can be fixed without deciding the other.* Every total below is recomputable from the explicit IDs; nothing is asserted as a bare number.

### 6.1 P0 — 8 classes

| # | ID | Class |
|---:|---|---|
| 1 | `P0-SEC-GATE` | `security-gate.sh` unrunnable as shipped |
| 2 | `P0-SEC-REDTEAM` | security redteam pack fails 4/6 against its own tree |
| 3 | `P0-SEC-PRODUCES` | `produces: security-report.json` never produced |
| 4 | `P0-SEC-RELEASE` | security features/OWASP assert release-gating authority |
| 5 | `P0-EFFECTS` | **effect declarations that do not describe real filesystem behavior — 6 members:** `doc`, `goals`, `pattern-mining`, `security`, `toil-mining` (false empty) **+ `skill-builder`** (incomplete nonempty, §4.1) |
| 6 | `P0-TEMPLATE` | three-way frontmatter template conflict seeding `effects: []` |
| 7 | `P0-HEAL-N-A` | five artifacts specify an unbuilt auto-fixing `heal.sh` |
| 8 | `P0-RATCHET` | ADR-0016 `skill.python-ratchet` red: five ungrandfathered execution-path files in `skill-builder` |

*Removed from P0:* the goals `--json` claim (refuted). **Class count 8; `P0-EFFECTS` membership 6.**

### 6.2 P1 — 21 unique units

**Base 16** *(v1's 14, minus P1-4 superseded into N-A, plus N-B, N-D, N-E)*:

| # | ID | Unit | Owning skill |
|---:|---|---|---|
| 1 | `P1-N-B` | containment test red and unwired; repair owner is the plan's **D7** transactional projection owner, *not* "mirror codex-sync" | skill-builder |
| 2 | `P1-N-D` | doc references carry work-creation and next-action authority; rationale corrected — `bd` is **not** retired, the defect is that `doc` has no authority to create work | doc |
| 3 | `P1-N-E` | seven distinct stale surface names in the doc package | doc |
| 4 | `P1-OWASP` | OWASP cites nonexistent flags; evidence owner corrected to the `validate`/`postmortem` **skill contracts** | security |
| 5 | `P1-HEAL-SUBSET` | heal checks only a subset of the fields downstream artifacts claim | skill-builder |
| 6 | `P1-HEAL-MESH` | heal `--fix` regenerates the mesh unscoped | skill-builder |
| 7 | `P1-HEAL-CRASH` | heal crash ≡ ordinary findings; non-strict mode swallows both | skill-builder |
| 8 | `P1-GUARD-RG` | lifecycle guards fail open when `rg` is unavailable | skill-builder |
| 9 | `P1-PROFILE` | `skill-conformance-profiles.yaml` load-bearing but unlinked | skill-builder |
| 10 | `P1-INIT-OC` | `init.sh` omits `output_contract` | skill-builder |
| 11 | `P1-DOC-VAL` | doc's validator is prose-grep, not a behavioral witness | doc |
| 12 | `P1-CG-VAL` | craft-goal lacks an output validator | craft-goal |
| 13 | `P1-OP-V1` | operationalize names an unschematised `.v1` artifact | operationalize |
| 14 | `P1-PROD-BYTE` | product's byte-preservation rule is unchecked | product |
| 15 | `P1-TOIL-OUT` | toil-mining's three output declarations disagree | toil-mining |
| 16 | `P1-ASR-SPEC` | automation-shape-routing's `skill.spec.json` is stale | automation-shape-routing |

**Added by the v4 correction line:**

| # | ID | Unit | Owning skill(s) |
|---:|---|---|---|
| 17 | `P1-LAYOUT` | four forbidden `.agents/` top-level destinations (§3.2) | doc, pattern-mining, skill-builder, toil-mining |
| 18 | `P1-GOALS-DRIFT` | eight live subcommands documented as six; `produces: [result.json]` unbacked (§5.2) | goals |

**Added by the v5 correction — the per-skill P1 labels v4 left unmapped:**

| # | ID | Unit | Owning skill | Why a separate unit |
|---:|---|---|---|---|
| 19 | `P1-SEC-VAL` | `validate.sh` is prose-grep + `py_compile` + JSON-parse and exits 0 while the gate and redteam are broken | security | Repairable independently of `P0-SEC-GATE` and `P0-SEC-REDTEAM`: making the validator behavioral neither fixes the gate nor refreshes the pack |
| 20 | `P1-OP-RETURN` | `write_advisory_proposal` versus "Return the proposal to the caller" — the mutation boundary is unknowable from the contract | operationalize | The **ambiguous-nonempty** bucket's repair (§4.3). Distinct from `P1-OP-V1`, which is about the artifact's missing schema, and outside `P0-EFFECTS`, which covers false and incomplete declarations |
| 21 | `P1-CG-BOUNDARY` | state the continuation-authority boundary explicitly for the emitted goal artifact | craft-goal | Distinct from `P1-CG-VAL`: adding a validator does not state the boundary, and stating the boundary does not validate output |

#### 6.2.1 Set equality, and why occurrences exceed it *(B3 correction)*

**The claim, stated correctly.** The **normalized canonical-ID set** derived from this aggregate ledger and the **normalized canonical-ID set** derived from the twelve §8 sections are **equal**, and both contain **21** members. Equality holds over *normalized canonical IDs*, **not** over occurrences.

**v5's sentence — "every ID above appears in exactly one §8 section" — is withdrawn.** It is mechanically false, and occurrence-level bijection was never what the ledger counted.

**What is and is not preserved across v5 → v6 → v7.** The **normalized P1 membership is unchanged** — the same 21 canonical IDs occur in the same sections, with the same multiplicities. **§8 itself is not unchanged**: restoring the twelve witness blocks expanded it substantially, and v7 rewrote two of them. v6 stated the broader claim ("v5's §8, which v6 preserves unchanged"); that was false and is corrected here. Both extents were measured in the v9 pass, with the convention published so the numbers are reproducible — counting from the `## 8.` heading through the `## 9.` terminator inclusive:

| Report | §8 lines | `Witness` blocks | Normalized P1 membership |
|---|---:|---:|---|
| v5 | **181** | **0** | 21 IDs |
| v6 | **413** | **12** | 21 IDs — unchanged |
| v7 | **490** | **12** | 21 IDs — unchanged |
| v8 | **561** | **12** | 21 IDs — unchanged |
| v9 | **571** | **12** | 21 IDs — unchanged |

*(A narrower range starting at `### 8.1` yields **175 / 405 / 473 / 514 / 524** for v5 / v6 / v7 / v8 / v9. Same facts, different boundary; the convention is stated so the two sets of figures never read as a contradiction. v8's `### 8.` heading count is **13**, not 12 — §8.0 is the validator taxonomy, and the twelve per-skill sections remain §8.1 through §8.12.)*

The v6 → v7 growth (413 → 490) was the two rewritten witness blocks and the `standards` three-surface defect table. The v7 → v8 growth (490 → 561) was the new §8.0 taxonomy, the toil metamorphic case 2, and the rewritten security gate witness. The v9 growth (561 → 571) is the standards path-set arithmetic table and the two line-range corrections in §8.11. **Across all three steps: no per-skill finding, severity, improvement count, or ledger ID changed.**

**This subsection is the one place in §6 whose rows move between generations.** §6.1, §6.2, §6.3, §6.4, and §7 have been byte-identical since v7 (§0.1.1); the table above and the two sentences following it publish the report's own lineage measurements, so they gain a row each generation by design. Saying "§6 is untouched" is therefore false for §6.2.1 and true for every other part of §6 — the distinction v8 collapsed and v9 restores.

Occurrence counts, measured over the §8 sections whose normalized P1 membership all three reports share:

| ID | Occurrences | Sections |
|---|---:|---|
| `P1-LAYOUT` | **4** | §8.3 `doc`, §8.7 `pattern-mining`, §8.10 `skill-builder`, §8.12 `toil-mining` |
| `P1-INIT-OC` | **2** | §8.10 `skill-builder`, §8.11 `standards` |
| the other 19 IDs | 1 each | one section each |

**19 + 4 + 2 = 25 occurrences over 21 unique units.**

**Ownership versus reference — how a shared unit is still counted once.** Each canonical unit has exactly one **owning repair**: the single place a fix is made, after which every referencing section is satisfied without further work. Sections that inherit the fix carry a **reference**, not a second unit.

- `P1-LAYOUT` — one owning repair: reclassify the four noncanonical `.agents/` top-level destinations under a permitted ADR-0016 tier and add the absent `fm-ws-noncanonical-topdir` detector. Four skills reference it because four skills name a forbidden destination; **relocating all four is one decision**, and no skill's site can be resolved without settling the tier question for the others. It is therefore **one unit with four references**, not four units.
- `P1-INIT-OC` — one owning repair in `skill-builder`'s `init.sh` (emit `output_contract`). `standards` references it as the companion half of template reconciliation: `standards` owns the template that omits the key, `init.sh` owns the emitter that omits it, and **the emitter change is the unit**. `standards`' own template correction is already counted as `P0-TEMPLATE`, so no unit is minted twice and none is lost.

This is the expected shape for a class repair and a companion repair. **The totals do not change: 21 unique P1 units.** A reader who prefers to count occurrences gets 25 and should say so explicitly; the two numbers measure different things.

### 6.3 P2 — 6 classes

| # | ID | Class |
|---:|---|---|
| 1 | `P2-N-F` | auditor-internal inconsistencies — dual kernel-length standards, unexercised `--probe` mode, `craft_score` false-positive classes, live-tree test writes |
| 2 | `P2-N-G` | standards internal defects — dead `#dedup-manifest` anchor, `javascript.md` missing from the owners table, duplicated sections in `python.md` |
| 3 | `P2-N-H` | markdown-standard violations inside doc |
| 4 | `P2-N-I` | `security/validate.sh` bytecode write; corrected remedy is `ast.parse` or an explicit temporary `cfile` outside the package — `cfile=None` still writes the default `.pyc` |
| 5 | `P2-INVOCABLE` | `user-invocable` spelling and advertised-trigger mismatches (`toil-mining`, `automation-shape-routing`) |
| 6 | `P2-CEREMONY` | per-skill ceremony and hygiene items named in §8 |

**Removed and not replaced:** the output-fallback calibration class — it rested on a false corpus claim (§6.4).

### 6.4 The output fallback — preserved

Re-executed in the v8 pass: **49 live skills; 43 with a nonempty `output_contract`; 6 without** — `cass`, `cc-hooks`, `dcg`, `implement`, `ms`, `plan`. The fallback component matrix (`cass`/`cc-hooks`/`dcg`/`ms` **complete**, all five components true; `implement`/`plan` **incomplete**, all five false) is **carried from the v9 Sol review's (`c22ce975…`) fresh `conformance_profile.py` evaluation**, not re-executed here. The fallback therefore has **four canonical positive witnesses and two canonical failures**.

## 7. Recomputed totals

### 7.0 The severity rule *(stated once, applied everywhere)*

> **Severity attaches to the finding class; every member of a class inherits the class severity.**

Consequences applied: `toil-mining`'s "declare the conditional write" is **P0** as a `P0-EFFECTS` member; **`skill-builder`'s uncovered build-report write is also a `P0-EFFECTS` member** (§4.3).

| Rank | Count | Basis |
|---|---:|---|
| **P0** | **8 classes** | §6.1, IDs `P0-SEC-GATE` … `P0-RATCHET`; `P0-EFFECTS` has 6 members |
| **P1** | **21 unique units** | §6.2, IDs 1–21; 25 occurrences across §8 (§6.2.1) |
| **P2** | **6 classes** | §6.3 |
| Refuted | 2 | `output-spec-explicit` not vacuous; `ao goals … --json` exists |
| Withdrawn | 1 | "the fallback has no canonical positive witness" |

## 8. Per-skill findings — one by one

Each section carries: **intent** · **greater-RPI-loop fit** · **live behavior / I/O / effects / authority** · **strengths** · **exact defects with source links and severity** · **five prioritized improvements** · **witness** · **ledger IDs** · **three-tier checked** · **not_checked** · **residual risk**.

**What the `Witness` block claims, stated precisely.** Each section **records its current witness state** and **supplies a present or future discriminating accept/reject witness as appropriate**, mapped to a numbered improvement in the same section.

This is deliberately narrower than "every section has a current and a future witness," which would be false in two directions:

- **Several current states are the honest absence of any validator.** `domain`, `operationalize`, `product`, and `standards` have no validator at all; `craft-goal`'s delegates entirely to a structural check and reads none of its own output. Recording "none" is the accurate disclosure, not a gap in the report.
- **`pattern-mining` supplies no future witness, deliberately.** Its current `validate-output.sh` plus two resolving Bats fixtures already discriminate its behavior; its remaining defects are declaration- and location-level, which **no output validator can detect by construction**. Proposing a future behavioral test there would be ceremony, and the section says so explicitly.

A witness is only counted as discriminating if a wrong implementation would **fail** it. Two v6 witnesses did not meet that bar — `toil-mining`'s ranking fixture and `standards`' template parity check — and both are replaced in v7 (§0). The other ten are unchanged and were independently reproduced as discriminating by the v6 advisory.

**Provenance tiers used in every `checked` block:** **(a) executed in the v8 pass** · **(b) source inspected in v8** · **(c) carried from a prior review, named at that point of use with its pinned digest** — the v7 (`bb650549…`), v8 (`c57b71d4…`), or v9 (`c22ce975…`) Sol review, enumerated completely in the §9.1 origin table and in §13. **No tier ever means execution by v10, v11, v12, or v13 — none of those passes reran anything** (§9, §10).

### 8.0 Validator taxonomy — the denominator, defined and counted

*(B3 correction. v7 asserted "one of only two behavioral validators" and "ten of the twelve have no witness that can fail on behavior." Both are **withdrawn**: they contradicted this report's own descriptions of `automation-shape-routing`, `pattern-mining`, and `skill-builder`, and the narrower class they implied was never defined. What follows is defined, reproducible, and re-derived by reading all six package validators in full in the v8 pass.)*

**The classifying question — one axis, chosen because it is mechanical:** *what does the skill's own package-local validator execute?* Enumerate `scripts/` and `tests/` for each of the twelve, then read each validator:

```bash
for s in automation-shape-routing craft-goal doc domain goals operationalize \
         pattern-mining product security skill-builder standards toil-mining; do
  printf '%-26s %s\n' "$s" \
    "$(git ls-tree -r --name-only "HEAD:skills/$s" | grep -E '^(scripts|tests)/' | tr '\n' ' ')"
done
```

| Class | Count | Skills | Basis, read in the v8 pass |
|---|---:|---|---|
| **No package-local validator** | **6** | `automation-shape-routing`, `domain`, `goals`, `operationalize`, `product`, `standards` | No `scripts/` or `tests/` entries tracked at all |
| **Validator exists, executes no skill logic** | **3** | `craft-goal`, `doc`, `security` | `craft-goal/scripts/validate.sh` is 6 lines ending `exec … heal.sh --check --strict` — structural only, reads no output. `doc/scripts/validate.sh` is 24 lines, 13 of them `grep` against doc's own prose. `security/scripts/validate.sh` is 22 lines: prose greps, `py_compile`, JSON parse — and `:9` is `grep -q '^  effects: \[\]$'`, which *pins* the defect |
| **Validator executes real skill logic** | **3** | `pattern-mining`, `skill-builder`, `toil-mining` | `pattern-mining/scripts/validate-output.sh` is a 43-line `jq` predicate over a supplied output artifact. `skill-builder/scripts/validate.sh` is 87 lines and runs `heal.sh --check --strict`, `compile_contracts.py check`, `test-contract-v3.sh`, `check_migration_readiness.py check`, plus a hash→`heal.sh --check`→re-hash **mutation-boundary proof** at `:65-71`. `toil-mining/scripts/validate.sh` is 14 lines: 4 prose greps **then** `python3 -m unittest discover` over its real test suite |

**6 + 3 + 3 = 12.**

**Three qualifications the single axis does not capture** — stated rather than folded into a number:

1. **A real boundary can exist outside a package validator.** `automation-shape-routing` has no `scripts/`, but its `SKILL.md` specifies an `awk` check that rejects multi-line output and off-map owners. That is a genuine accept/reject boundary — it is **caller-executed**, not a package script, which is why it falls in the "no package validator" row. `goals` is similar in a different way: its behavioral coverage lives in Go tests under `cli/internal/goals/`, outside the skill package entirely.
2. **Executing real logic is not the same as covering the contract.** `toil-mining`'s validator runs a real unit suite — over the **retrieval helper only**. The ranking contract it advertises has no scorer to test (§8.12). `pattern-mining`'s validator checks a **supplied output artifact** and exits 2 without one; it cannot detect that skill's undeclared effect or forbidden destination, which is precisely why §8.7 declines to propose a further behavioral witness.
3. **A skill's executable *products* are not its validator.** `security` ships a gate, a suite, and a redteam pack — all executable, and the gate and pack currently fail. None of that is reachable from `scripts/validate.sh`, which exits 0 regardless. That gap is the finding `P1-SEC-VAL`, not evidence of a behavioral validator.

**What is *not* claimed.** No percentage, ratio, or "N of twelve have real proof" figure is asserted anywhere in this report. The one surviving aggregate claim about proof coverage is the named list in §11 — five specific high-value guarantees with no machine boundary — which is a list, not a denominator, and each member is traceable to its own section.

**On improvement counts.** Each section carries exactly five prioritized improvements. Where a skill's real repair surface is smaller than five, the remaining entries are **explicit declines with stated rationale** rather than invented work — the calibration rule that protects concise kernels is itself an audit finding, and padding it would be a defect. Where the surface is larger than five, lower-ranked items are **batched into a single numbered entry** with every member named; nothing is dropped. An entry marked *"acceptance criterion of `<ID>`, not a new unit"* refines an existing ledger unit and mints no new one, which is why §6's totals are unchanged.

### 8.1 `automation-shape-routing` — 2 files

**Intent.** A pure decision function: map one task-intent to exactly one of six execution shapes, then hand off.

**RPI-loop fit.** Target **support**, seams **cross_cutting + standalone**, T3. Pre-loop routing. Holds no authority verb, declares `dependencies: []`, and explicitly requires operator selection for `using-gc`. It routes and stops without starting a runtime — it never enters the bounded experiment it routes toward.

**Live behavior / I/O / effects / authority.** In: task-intent. Out: one stdout line `shape=…; axis=…; owner=…`. **`effects: []` accurate** — accurate-empty bucket.

**Strengths.** The embedded `awk` validator is a real machine boundary: it rejects multi-line output and enforces the shape→owner mapping. "Smallest shape that preserves evidence" is the corpus's best routing argument.

**Exact defects.** *(P1)* `skill.spec.json` is a stale second metadata source contradicting `SKILL.md` on sections (8 declared, 0 present), `context_rel` (2 vs 5), `produces` (`[]` vs `[automation-shape-verdict]`), invocability, and triggers; its `evidence.sources` cites `~/dev/agentops-3cat-spike/`, outside the repository. *(P2)* `user_invocable` vs `user-invocable` spelling. Only 3 of 49 skills carry a `skill.spec.json`, so the second-source hazard is narrow but unguarded.

**Improvements.**
1. **(P1, `P1-ASR-SPEC`)** Delete `skill.spec.json`, or regenerate it from `SKILL.md` under a parity check.
2. **(P2, `P2-INVOCABLE`)** Resolve the `user_invocable` / `user-invocable` spelling corpus-wide.
3. **(P2, `P2-CEREMONY`)** Drop the out-of-repo `~/dev/agentops-3cat-spike/` citation; a provenance source outside the repository cannot be re-verified by any consumer.
4. **(P2, `P2-CEREMONY`, mechanizing improvement 1)** Add the parity check to the corpus gate so all three spec-carrying skills are covered, not just this one — otherwise improvement 1 fixes an instance and leaves the class.
5. **Declined, deliberately: no additional validator.** The `awk` boundary already rejects the only output shape this skill can get wrong. Adding rubric ceremony to a two-file decision function would lower its signal.

**Witness.** *Current:* the embedded `awk` validator **accepts** exactly one stdout line matching `shape=…; axis=…; owner=…` with an owner drawn from the shape→owner map, and **rejects** multi-line output or an off-map owner. It never reads `skill.spec.json`, so the stale second source is invisible to it. *Future:* a parity check over every `skills/*/skill.spec.json` — **accept** when the spec's `produces`, `context_rel`, section list, and invocability agree with the sibling `SKILL.md`, or when no spec exists; **reject** on any disagreement. **Fails today on this skill** on all four fields. *Maps to improvement 1, mechanized by improvement 4.*

**Ledger.** `P1-ASR-SPEC`; `P2-INVOCABLE`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects` read (`effects: []`); tracked-file count (2); both files present in the recomputed §12 inventory. **(b)** both files in full. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`), the most recent review to run the twelve-skill `audit.sh` sweep; the v8 and v9 reviews did not.

**not_checked.** Whether any runtime actually reads `skill.spec.json`; the parity check does not exist to be run.

**Residual risk.** Low — a reader trusting the spec file gets a wrong section map and wrong relations, but no execution path depends on it.

### 8.2 `craft-goal` — 3 files

**Intent.** Compile or lint a persistent goal prompt that sequences many bounded RPIs toward one terminal outcome.

**RPI-loop fit.** Target **campaign**, seam **goal_design**, T3 — **above** bounded RPIs, not inside one. The skill itself is clean: one compilation or lint pass, "Never create a goal or mutate beads." The *artifact* it emits is a continuation contract (wave envelopes, dispatch budgets, andon thresholds); it **authors** autonomy authority for a caller-owned runtime rather than exercising it. That asymmetry — clean skill, powerful artifact — is the whole of its risk.

**Live behavior / I/O / effects / authority.** In: caller outcome and acceptance. Out: `SAFE_TO_CREATE | USE_RPI | UNSAFE_GOAL` plus a copy-paste prompt. **`effects: []` accurate.**

**Strengths.** Highest craft score of the twelve; provenance resolves 2/2. The paired failure modes (*completion treadmill* / *first-red abandonment*) and the explicit ratchet definition are the sharpest prose in the corpus; it discloses a real product gap rather than papering over it.

**Exact defects.** *(P1)* `scripts/validate.sh` is a three-line shim to `heal.sh --check --strict` — **structural only**. *(P1)* The declared `output_contract` (three-way decision + budgets + fourteen lint dimensions) has **no validator**. *(P1)* The continuation-authority boundary of the emitted artifact is never stated. *(P2)* No `context:` block (verified absent in the v8 pass); `stability: experimental`.

**Improvements.**
1. **(P1, `P1-CG-VAL`)** Add a real output validator for the three terminal tokens and the required lint dimensions.
2. **(P1, `P1-CG-BOUNDARY`)** State the continuation-authority boundary explicitly: what the emitted artifact may authorize a caller-owned runtime to do, and what it may not.
3. **(P2, `P2-CEREMONY`)** Add a `context:` block.
4. **(P2, `P2-CEREMONY`)** Declare the `bd` / `br` / `bv` coupling as `context_rel` or as explicitly external; today the coupling is real and undeclared in either direction.
5. **(P2, `P2-CEREMONY`)** Keep `stability: experimental` until 1 and 2 land, and say so in the contract. The honest label is load-bearing while the highest-authority output in the set is unvalidated; promoting stability first would be the actual regression.

**Witness.** *Current:* `scripts/validate.sh` **accepts** any structurally well-formed package and **rejects** only structural defects, because it delegates entirely to `heal.sh --check --strict`. It never reads a compiled goal, so no property of the output is tested (exit 0 this landing — carried from the **v9 Sol review** (`c22ce975…`)). *Future:* a fixture pair over compiled goal output — **reject** a goal missing a hard-envelope section (wave envelope, dispatch budget, or andon threshold) or terminating in a token outside `SAFE_TO_CREATE | USE_RPI | UNSAFE_GOAL`; **accept** a well-formed goal carrying all fourteen lint dimensions. Nothing checks this today. *Maps to improvement 1; improvement 2 is the prose half of the same gap and must land with it, or the validator will certify an artifact whose authority is still unstated.*

**Ledger.** `P1-CG-VAL`; `P1-CG-BOUNDARY`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (3); `context:` block **absent** (grep count 0); all 3 files present in the recomputed §12 inventory. **(b)** all 3 files in full. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`); `validate.sh` exit 0 delegating only to structural `heal.sh --check --strict` — the **v9 Sol review** (`c22ce975…`).

**not_checked.** Whether emitted prompts converge in a live goal runtime; no goal was compiled.

**Residual risk.** Medium — an unvalidated artifact conferring continuation authority is the highest-leverage unchecked output in the set.

### 8.3 `doc` — 17 files

**Intent.** Three-mode documentation generator/validator: default (code/API), `readme`, `oss`.

**RPI-loop fit.** Target **implementation**, seam **implement_method**, T5 — one bounded, caller-authorized documentation operation, outside the core loop's evidence path. Policy is well specified: OSS scaffold is create-missing-only, `refresh` requires explicit confirmation, existing docs are never silently overwritten, "Doc does not commit, push, release, or decide completion." **Its references violate that boundary.**

**Live behavior / I/O / effects / authority.** In: repo context. Out: `README.md`, root OSS docs, `.agents/doc/YYYY-MM-DD-<target>.md`. **`effects: []` false** — the most write-heavy of the twelve; false-empty bucket, `P0-EFFECTS` member.

**Strengths.** The bounded-chunk coverage ledger and conceptual-surprise floor are genuinely good, non-duplicated doctrine. `readme-craft.md`'s patterns and anti-pattern table are concrete and testable. `de-slopify.md` correctly refuses to be a regex pass.

**Exact defects.**
(a) **(P0)** `effects: []` false.
(b) **(P1)** `scripts/validate.sh` runs 14 checks, **all 14 greps of doc's own prose** — it cannot fail while the wording is intact.
(c) **(P1)** `references/validation-rules.md:134-145` ships `--create-issues` emitting `bd create` and `gh issue create`; `references/default-mode.md:172` ships a `## Next Steps` action-box template. **Corrected rationale: `bd` is not retired; the defect is that `doc` has no authority to create work at all.**
(d) **(P1)** Seven distinct stale surface names — five skill names, `subagents/explorer.md`, and the script's `/oss-docs scaffold` hint.
(e) **(P2)** `scripts/audit-oss-docs.sh` is unlinked from `SKILL.md` and uses bare `set -e` against the required `set -euo pipefail`.
(f) **(P2)** Markdown-standard violations: 7 `#` H1s in `references/architecture-report.md`; an unclosed fence in `references/oss-pack.md`.
(g) **(P2)** Three `.feature` files with no `@covered-by`.
(h) **(P1)** **`.agents/doc/` violates the ADR-0016 closed layout** (§3.2), repeated in `references/default-mode.md`.

**Improvements.**
1. **(P0, `P0-EFFECTS`)** Declare real effects covering every destination the three modes write.
2. **(P1, `P1-DOC-VAL`)** Replace the 14 prose greps with one behavioral test: scaffold into a temp project, assert missing files created and an existing file byte-identical.
3. **(P1, `P1-N-D`)** Delete `--create-issues` and the `## Next Steps` action-box template — both confer work-creation authority the skill disclaims.
4. **(P1, `P1-N-E`)** Fix or remove the seven stale surface names.
5. **(P1, `P1-LAYOUT`)** Reclassify `.agents/doc/` as proof, scratch, or projection and relocate it — **and in the same pass clear the P2 batch** (`P2-N-H`, `P2-CEREMONY`): link `audit-oss-docs.sh` from `SKILL.md` and fix its `set -e` preamble; split `architecture-report.md` to one H1; close the fence in `oss-pack.md`; add `@covered-by` to the three `.feature` files.

**Witness.** *Current:* `scripts/validate.sh` **accepts** the package whenever doc's own wording is intact and **rejects** nothing else — all 14 checks grep doc's prose, so it reports 14/14 PASS on this landing (carried from the **v9 Sol review** (`c22ce975…`)) while defects (a), (c), (d), and (h) are all live. It is structurally incapable of failing on behavior. *Future:* a fixture repository containing a **populated** `CONTRIBUTING.md` and missing the remaining OSS files. Run `--mode=oss scaffold`. **Accept** iff every previously-missing OSS file now exists **and** `CONTRIBUTING.md`'s SHA-256 is byte-identical to its pre-run digest; **reject** on any byte change to a pre-existing file, or on a missing file that was not created. The no-overwrite guarantee is doc's central safety claim and is currently unproven in either direction. *Maps to improvement 2.*

**Ledger.** `P0-EFFECTS`; `P1-DOC-VAL`, `P1-N-D`, `P1-N-E`, `P1-LAYOUT`; `P2-N-H`, `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (**17**, correcting v2's 22); `.agents/doc` in the destination sweep; all 17 files present in the recomputed §12 inventory. **(b)** all 17 files in full; both owning files' `.agents/doc` occurrences. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`); `validate.sh` 14/14 prose checks PASS — the **v9 Sol review** (`c22ce975…`).

**not_checked.** No scaffold or README generation was executed — that mutates the live tree. The no-overwrite guarantee is therefore unverified by anyone in this lineage.

**Residual risk.** **High.** The most mutating skill has the least behavioral validation, its references instruct out-of-boundary actions, and its output destination is forbidden by an accepted decision with no live detector.

### 8.4 `domain` — 1 file

**Intent.** Read-only lookup returning the exact definition of an AgentOps term plus its source path.

**RPI-loop fit.** Target **evidence**, seams **plan_input + cross_cutting**, T4. Exact read-only lookup and stop — the cleanest boundary in the set: four steps ending "Stop," disclaiming promotion, index mutation, and continuation, and actively guarding the vocabulary boundary by naming `queue, claim, lease, close, land, release, delivery` as **caller-system** terms that AgentOps does not own. That single list does more product-boundary work than most of the corpus.

**Live behavior / I/O / effects / authority.** In: a term. Out: definition + path to stdout. **`effects: []` accurate.** Both cited contracts verified present.

**Strengths.** Correct declarations, resolvable references, a real stop condition, provenance 2/2, and a `context:` block (present — verified in the v8 pass). *synonym smuggling* is precise and actionable.

**Exact defects.** *(P2)* No validator of any kind — the package is a single `SKILL.md` with no `scripts/` directory (verified in the v8 pass). Craft score is low only because a 58-line kernel legitimately lacks several rubric elements; that is a rubric artifact, not a defect.

**Improvements.**
1. **(P2, `P2-CEREMONY`)** A three-line check that both cited contract paths exist.
2. **(P2, `P2-CEREMONY`)** Extend the same check to assert the looked-up term actually resolves **inside** the cited file — a path that exists but no longer defines the term is the failure this skill is for.
3. **(P2, `P2-CEREMONY`)** Package 1–2 as `skills/domain/scripts/validate.sh` so `audit.sh` has something to run; today there is no validator entry point at all.
4. **Declined, deliberately: no `context:` addition and no rubric expansion.** The `context:` block is already present, and adding rubric ceremony to a 58-line kernel would lower its real quality. The calibration rule exists to protect exactly this file.
5. **Declined, deliberately: no validator beyond 1–3.** The "Stop" boundary is enforced by prose with no machine failure mode; inventing a gate for it would manufacture a false signal, which is worse than the honest absence.

**Witness.** *Current:* none. There is no validator of any kind, so the package has no accept/reject shape whatsoever — correctness rests entirely on reading. *Future:* the three-line check of improvements 1–3 — **accept** iff both cited contract paths exist **and** the queried term resolves within the cited file; **reject** when either the path or the term is missing. Concretely: rename `ubiquitous-language.md` and the check must fail. **Today nothing notices** — the rename would silently produce a lookup pointing at a nonexistent path. *Maps to improvements 1–3.*

**Ledger.** `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (1); `context:` block **present** (grep count 1); package contents (`SKILL.md` only — no `scripts/`); the file present in the recomputed §12 inventory. **(b)** the file in full; both referenced contracts' existence. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`).

**not_checked.** The contract files' contents (outside the twelve).

**Residual risk.** Low.

### 8.5 `goals` — 1 file

**Intent.** Run one caller-selected measurement against the active goals document and report numbers plus evidence gaps.

**RPI-loop fit.** Target **product**, seams **product_input + goal_observe**, T3. Measurement is deliberately separated from campaign control: it observes goal state without selecting work or mutating the GOALS source. The authority boundary is excellent — *advice creep* names the exact failure ("a measurement report that ends with 'you should…' has silently become work selection"), and the boundary forbids add/remove/prioritize/recommend/apply/prune/migrate.

**Live behavior / I/O / effects / authority.** In: `GOALS.md`. Out: measurement report. **Effects: writes** — `.agents/ao/goals/baselines` snapshots from `RunMeasure` / `RunExport` / `RunDrift`, plus `render --out`. **`effects: []` false**; false-empty bucket, `P0-EFFECTS` member. **Precisely:** the snapshot write is reached on a *successful ordinary, non-directives* `measure`; `--directives`, load failures, validation errors, and filter errors all return before it, and a failed save warns rather than fails. The destination is under the canonical `ao/` tier, so this is a **declaration defect, not a layout violation**.

**Strengths.** The measurement/advice boundary is one of the sharpest in the corpus and is entirely unaffected by the correction. The skill can legitimately remain read-only with respect to the goals *source* while declaring snapshot and output writes — the two claims are compatible and the contract should say both.

**Exact defects.**
(a) **(P0)** `effects: []` false — snapshot and `--out` writes are undeclared.
(b) **(P1)** **Eight live subcommands documented as six** (`cli/internal/commands/goals/module.go:112-119`) — `scenarios` and `render` omitted.
(c) **(P1)** `produces: [result.json]` contradicts both `output_contract` and the live artifact set.
(d) v2's P0 that `--json` does not exist is **REFUTED** (§5.1) and is not carried.

**Improvements.**
1. **(P0, `P0-EFFECTS`)** Declare the snapshot and `render --out` writes.
2. **(P1, `P1-GOALS-DRIFT`)** Document `scenarios` and `render`; correct `produces` to the real artifact contract.
3. **(P2, `P2-CEREMONY`)** State explicitly that the goals *source* is never mutated, so the read-only intent survives the effect declaration rather than appearing to contradict it.
4. **(P2, `P2-CEREMONY`, mechanizing improvement 2)** Add a command-tree parity check so six-versus-eight drift cannot recur silently after the one-time correction.
5. **(P2, `P2-CEREMONY`, acceptance criterion of `P0-EFFECTS`, not a new unit)** Qualify the declaration with the real reachability: the snapshot is written on the successful ordinary non-directives path only, and the save is best-effort. An unqualified "writes baselines" would be as inaccurate in the other direction.

**Witness.** *Current, withdrawn:* v3's witness — "extract every fenced `ao …` line and assert it parses" — targeted the **refuted** `--json` defect, passes today, and discriminates none of the three live defects. It is withdrawn again here and must not be reinstated. *Future — three discriminating witnesses, each failing on a real defect:*

1. **Snapshot-write.** The focused tests `TestGoalsDrift_NoBaseline_CreatesInitialSnapshot` (`cli/internal/goals/goals_drift_test.go:14`) and `TestSaveAndLoadSnapshot` (`cli/internal/goals/snapshot_test.go:13`) — both **PASS** (carried from the **v9 Sol review** (`c22ce975…`)) — plus a scratch-root ordinary `measure` run: **accept** iff a file appears under `.agents/ao/goals/baselines`; **reject** if none does. This is what makes `effects: []` demonstrably false. *Maps to improvement 1.*
2. **Command-tree parity.** **Accept** iff the skill's documented subcommand set equals the live registered set; **reject** otherwise. **Fails today** — eight registered, six documented. *Maps to improvements 2 and 4.*
3. **Declared-output/effect.** **Accept** iff every durable destination the adapter writes (`.agents/ao/goals/baselines`, `render --out`) appears in both `effects` and the output contract; **reject** otherwise. **Fails today on both**, and additionally exposes `produces: [result.json]` as unbacked. *Maps to improvements 1, 2, and 5.*

**Ledger.** `P0-EFFECTS`; `P1-GOALS-DRIFT`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (1); the file present in the recomputed §12 inventory. **(b)** the file in full; `cli/cmd/ao/root.go:153` global persistent flag registration; `cli/internal/commands/goals/module.go:112-119` (eight subcommands); `cli/internal/goals/commands.go` SnapDir `:171`/`:389`/`:484` and `SaveSnapshot` `:207`/`:399`/`:495`/`:504` **plus the four early-return gates ahead of them**; `module_test.go:97-99`. **(c)** the two focused snapshot tests PASS, the live eight-command tree, and `--json` under Global Flags — the **v9 Sol review** (`c22ce975…`); the `goals measure --json --help` exit-0 execution — the **v8 Sol review** (`c57b71d4…`); `audit.sh` PASS — the **v7 Sol review** (`bb650549…`).

**not_checked.** **The v8 pass ran no `go` command and no `ao` command, and v10 ran none either.** No scratch ordinary-`measure` run; no snapshot artifact produced; no live `GOALS.md` measurement. Witness 1 is therefore established by its two focused tests plus source reading, not by an end-to-end write in the v8 pass.

**Residual risk.** Medium — an adapter documented as effect-free that writes durable baselines on its most common path.

### 8.6 `operationalize` — 1 file

**Intent.** Convert repeated, cited expertise into a proposal for the smallest reusable artifact, refusing to abstract on thin evidence.

**RPI-loop fit.** Target **evolution**, seams **post_verdict + plan_input**, T6 — a post-verdict consumer feeding a later plan, advisory only. Explicit: does not create tracker work, promote policy, start a factory, validate its own output, or control another invocation. It is downstream of a verdict and upstream of a future intent, never inside a running experiment.

**Live behavior / I/O / effects / authority.** In: cited evidence. Out: `operationalization-proposal.v1`. Declares `effects: [write_advisory_proposal]` while the body says "Return the proposal to the caller" — **ambiguous-nonempty bucket: the mutation boundary is unknowable from the contract.** Names `.agents/ao/` only, which is canonical.

**Strengths.** The best evidence discipline in the corpus, in three independent falsifiable layers: a **three-instance floor**, a **reapply proof** ("No reapply proof, no rule"), and a **quote bank** with locally resolving citations.

**Exact defects.**
(a) **(P1)** `produces: [operationalization-proposal.v1]` names a versioned artifact with **no schema, no path, no filename convention, and no validator** — the package is one file. The `.v1` suffix promises a contract that does not exist.
(b) **(P1)** `write_advisory_proposal` versus "return to caller" is unresolved — a **distinct** defect from (a): defining the artifact does not decide whether the skill writes it, and deciding that does not define it.
(c) **(P2)** No `context:` block (verified absent in the v8 pass); `user-invocable` sits at top level while the rest is under `metadata`.

**Improvements.**
1. **(P1, `P1-OP-V1`)** Define the artifact — schema + path + validator — or drop the version suffix.
2. **(P1, `P1-OP-RETURN`)** Resolve `write_advisory_proposal` vs "return the proposal to the caller." See §4.3 for why this is P1 rather than a `P0-EFFECTS` member: the declaration is ambiguous, not false.
3. **(P2, `P2-CEREMONY`)** Normalize the frontmatter shape — move `user-invocable` under `metadata` with the rest.
4. **(P2, `P2-CEREMONY`)** Add a `context:` block.
5. **(P1, acceptance criterion of `P1-OP-V1`, not a new unit)** The validator built in 1 must assert the **three-instance floor** and the **reapply proof**, not merely schema shape. A structure-only validator would reproduce craft-goal's exact failure mode (§8.2) — a green check over an artifact whose load-bearing property is untested.

**Note.** The shadow rail's `BINDING_ARTIFACT_UNVALIDATED` is exactly improvement 1, but it is a **shadow** rule and `operationalize` is T6 — **do not wait for it, and do not treat it as current authority** (§2).

**Witness.** *Current:* none. The package is one file with no validator, so the three-instance floor, the reapply proof, and the quote bank have no accept/reject shape — all three are prose the skill asks the model to honor. *Future:* fixture proposals run through the validator of improvement 1 — **reject** a proposal carrying only two exemplars; **reject** one with three exemplars but no reapply proof; **reject** one whose quote-bank citation does not resolve locally; **accept** one with three resolvable exemplars plus a reapply proof. Nothing checks the floor mechanically today. *Maps to improvement 1 and its acceptance criterion, improvement 5.*

**Ledger.** `P1-OP-V1`; `P1-OP-RETURN`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects` (`[write_advisory_proposal]`); tracked-file count (1); `context:` block **absent** (grep count 0); `.agents/` destinations (canonical `ao/` only); the file present in the recomputed §12 inventory. **(b)** the file in full. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`).

**not_checked.** Whether any proposal has ever been produced, and therefore whether the skill writes or returns in practice.

**Residual risk.** Medium — the strongest evidence rules in the corpus produce an unvalidatable artifact.

### 8.7 `pattern-mining` — 3 files

**Intent.** Decide whether repeated code is an earned abstraction or a hypothesis, gating promotion behind an exemplar floor, a holdout, and back-application.

**RPI-loop fit.** Target **evolution**, seams **post_verdict + plan_input**, T6. Only a validated promotion routes onward; it never creates the reusable artifact, never promotes a failed holdout, and routes hypotheses to `no-action`. Like `operationalize`, it sits after a verdict and before a future intent.

**Live behavior / I/O / effects / authority.** In: repo context. Out: `pattern-mining.v1` JSON at `.agents/patterns/<run-id>/`. **Writes; `effects: []` false** (`P0-EFFECTS` member), and **`.agents/patterns/` violates the closed layout** (§3.2).

**Strengths.** **The reference implementation for output contracts in this corpus.** `scripts/validate-output.sh` is a real `jq` gate enforcing, for `promote`: ≥3 unique exemplars, non-empty invariants, `holdout.result == "pass"`, `back_application == "pass"`, `route == "operationalize"`, **and** that the holdout source is not itself an exemplar — closing the exact loophole the prose is about. Its two `@covered-by` tags are the **only** ones among the twelve and both resolve, each with accept and reject fixtures.

**Exact defects.** *(P0)* `effects: []` false. *(P1)* `.agents/patterns/` noncanonical. *(P2)* `jq` undeclared as a dependency — it fails closed under `set -euo pipefail`, which is acceptable but silent.

**Improvements.**
1. **(P0, `P0-EFFECTS`)** Declare the write.
2. **(P1, `P1-LAYOUT`)** Reclassify and relocate `.agents/patterns/` under a permitted tier.
3. **(P2, `P2-CEREMONY`)** Emit an explicit error when `jq` is absent rather than failing closed silently.
4. **(P2, `P2-CEREMONY`)** Name `skills/pattern-mining/scripts/validate-output.sh` in `standards`' canonical-owners table as the reference output-contract validator. **Verified in the v8 pass: `standards` contains zero references to it** — the corpus's one good example is currently undiscoverable from the skill that owns doctrine, which is why craft-goal, doc, product, and operationalize each reinvented nothing.
5. **Declined, deliberately: no further behavioral work.** This is the best-validated skill of the twelve and its defects are declaration- and location-level only. Adding ceremony here would lower the signal of the corpus's one working template.

**Witness.** *Current — and it already exists and passes.* `scripts/validate-output.sh` is a live accept/reject boundary: for `route: promote` it **rejects** fewer than 3 unique exemplars, empty invariants, `holdout.result != "pass"`, `back_application != "pass"`, `route != "operationalize"`, and a holdout source that is itself an exemplar; it **accepts** a promotion meeting all six. Its two `@covered-by` Bats fixtures — one accept, one reject — are the only resolving coverage tags among the twelve and ran **2/2 PASS** (carried from the **v9 Sol review** (`c22ce975…`)). *Future:* **none needed for behavior.** The remaining gap is declaration and location, which no output validator can detect. *The existing witness is the template propagated by improvement 4; improvements 1–2 are outside any output validator's reach by construction.*

**Ledger.** `P0-EFFECTS`; `P1-LAYOUT`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (3); `.agents/patterns` in the destination sweep; `standards` → `validate-output` cross-reference count = **0**; all 3 files present in the recomputed §12 inventory. **(b)** all 3 files in full; validator logic line-by-line. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`); both Bats witnesses **2/2 PASS** — the **v9 Sol review** (`c22ce975…`).

**not_checked.** No `pattern-mining.json` artifact was produced in the v8 pass, and none in v10.

**Residual risk.** Low — defects are declaration- and location-level, not behavioral.

### 8.8 `product` — 1 file

**Intent.** Create or refine `PRODUCT.md`, forcing every claim into evidence or labeled aspiration.

**RPI-loop fit.** Target **product**, seam **product_input**, T3 — pre-intent boundary shaping. It creates or refines the caller-authorized product contract that later intents are scoped against. Clean: "does not select work, invoke a loop, repair itself, validate itself, or choose delivery."

**Live behavior / I/O / effects / authority.** In: repo / README / goals / evidence. Out: `PRODUCT.md`. **Effects honestly declared** — `effects: [write_product_document]`, `produces: [PRODUCT.md]`. **Coherent-nonempty bucket — the only member.**

**Strengths.** The evidence/aspiration split; *aspiration laundering* is well named; the byte-for-byte preservation corrective is a checkable standard rather than a wish.

**Exact defects.** *(P1)* No validator, no test — the byte-for-byte preservation rule is machine-checkable and unchecked. *(P2)* *aspiration laundering* has no detector. *(P2)* No `context:` block (verified absent in the v8 pass).

**Improvements.**
1. **(P1, `P1-PROD-BYTE`)** A preservation test: fixture `PRODUCT.md`, scoped refine, untouched sections must hash identically.
2. **(P2, `P2-CEREMONY`)** Require explicit `## Proven` / `## Assumptions` headings so the evidence/aspiration split is greppable rather than inferred.
3. **(P2, `P2-CEREMONY`)** Add a `context:` block.
4. **(P2, `P2-CEREMONY`)** Add an *aspiration laundering* detector on top of improvement 2: assert every claim under `## Proven` carries a resolvable citation. The named failure mode currently has no machine boundary at all.
5. **(P1, acceptance criterion of `P1-PROD-BYTE`, not a new unit)** The test in 1 must exercise the **real** refine path against a fixture, not a mocked writer. A preservation test that never invokes the writer proves nothing about the guarantee it claims to protect.

**Witness.** *Current:* none. There is no validator and no test; the byte-for-byte preservation rule — the skill's one strong guarantee — has no accept/reject shape. *Future:* a fixture `PRODUCT.md` carrying `## Proven` and `## Assumptions` sections. Run a **scoped refine of one section through the real writer**. **Accept** iff every untouched section hashes identically to its pre-run SHA-256 **and** the targeted section changed; **reject** on any drift in an untouched section, and **reject** a run in which nothing changed at all (which would pass a naive hash check vacuously). *Maps to improvement 1 and its acceptance criterion, improvement 5.*

**Ledger.** `P1-PROD-BYTE`; `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects` (`[write_product_document]`); tracked-file count (1); `context:` block **absent** (grep count 0); the file present in the recomputed §12 inventory. **(b)** the file in full. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`).

**not_checked.** No `PRODUCT.md` was generated or refined.

**Residual risk.** Low-medium — smallest skill, real declared write, zero verification.

### 8.9 `security` — 11 files

**Intent.** Run authorized bounded scans (repository gate, composable binary suite, offline prompt redteam) and report findings plus coverage gaps.

**RPI-loop fit.** Target **evidence**, seams **validate_evidence + standalone**, T4. It supplies evidence to a judgment it does not make: remediation, risk acceptance, and promotion are caller decisions, and it holds **no remediation or release authority**. `SKILL.md` is correct and careful — coverage gaps are never clean findings; it "distinguishes a passing scan from permission to promote or release." **The `.feature` files and OWASP checklist contradict it.**

**Live behavior / I/O / effects / authority.** Writes extensively (gate summary, suite outputs, redteam results). **`effects: []` false, and *actively enforced* false** by `scripts/validate.sh:9`, which greps for the empty literal. `P0-EFFECTS` member.

**Strengths.** The hunt discipline (full-taxonomy walk, empirical proof per finding, fail-open probes, identity-chain traces, quiet-round convergence) is the best security doctrine in the repo. `prompt_redteam.py` fails closed on an empty glob. Highest provenance of the twelve.

**Exact defects.**
1. **(P0, `P0-SEC-GATE`)** `scripts/security-gate.sh` is byte-identical to the root copy but one directory deeper, so `SCRIPT_DIR/..` resolves to `skills/security`; quick mode exits 1 on missing `scripts/toolchain-validate.sh`. **Repair is deletion of the skill-local duplicate** — the redteam pack's `security-gate-bypass` case requires `SKILL.md` to document the root path.
2. **(P0, `P0-SEC-REDTEAM`)** The shipped attack pack **fails 4/6** against this landing (exit 3); `docs/strategic-direction.md` no longer exists and `_evaluate_case` correctly fails closed on the empty glob. Only the two cases targeting the skill's own prose pass.
3. **(P0, `P0-SEC-PRODUCES`)** `produces: [security-report.json]` — no code path writes that filename.
4. **(P0, `P0-SEC-RELEASE`)** Features and the OWASP checklist assert release-gating and "Block merge" authority the skill and `AGENTS.md` disclaim.
5. **(P0, `P0-EFFECTS`)** `effects: []` false, and **pinned false by its own validator** at `validate.sh:9`.
6. **(P1, `P1-SEC-VAL`)** `validate.sh` is prose-grep + `py_compile` + JSON-parse; it exits 0 while (1) and (2) are broken.
7. **(P1, `P1-OWASP`)** OWASP cites `/validate --preset=security-audit` and `/postmortem --scope security`. **Corrected evidence owner:** these are *skill invocations*, not `ao` subcommands, so searching `cli/` proves nothing — the staleness holds because the canonical `validate` and `postmortem` **skill contracts** expose neither flag.
8. **(P2, `P2-N-I` / `P2-CEREMONY`)** Features say `supplier-to vibe` (now `validate`); `prompt_redteam._evaluate_file` can never return `WARN` yet two aggregators test for it; `security_suite.shutil_which` shells out to `bash -lc`, sourcing the login profile inside a security tool; `glob.glob(root_dir=…)` needs Python ≥3.10, undeclared. **`P2-N-I`:** `validate.sh:19` runs `py_compile`, writing `__pycache__/*.pyc` into the package; **corrected remedy** — `cfile=None` still writes the default `.pyc`, so use `ast.parse` or an explicit temporary `cfile` outside the package with cleanup.
9. Its two execution-path Python files are **grandfathered** (§3.1) — acknowledged debt, not a new ratchet failure.

**Improvements.**
1. **(P0, `P0-SEC-GATE`)** Delete the duplicated skill-local gate script; keep `SKILL.md` documenting the root path, which the redteam pack's `security-gate-bypass` case depends on.
2. **(P0, `P0-SEC-REDTEAM`)** Refresh the attack pack against current control surfaces, or retire the four dead cases explicitly rather than shipping them red.
3. **(P0, `P0-SEC-PRODUCES` + `P0-SEC-RELEASE` + `P0-EFFECTS`)** The declaration triad: correct `produces`; strip release-gating and "Block merge" verbs from the features and the OWASP checklist; declare real effects **and remove the `validate.sh:9` empty-literal grep in the same change**.
4. **(P1, `P1-SEC-VAL` + `P1-OWASP`)** Make `validate.sh` behavioral (see the witness) and fix the two stale skill-contract citations.
5. **(P2, `P2-N-I` + `P2-CEREMONY`)** The P2 batch with the corrected bytecode remedy: replace `py_compile` with `ast.parse` or an explicit temp `cfile` outside the package; `supplier-to vibe` → `validate`; remove the unreachable `WARN` branch and its two aggregator tests, or make it reachable; replace `bash -lc` in `shutil_which`; declare the Python ≥3.10 floor that `glob(root_dir=…)` requires.

**Witness.** *Current:* `scripts/validate.sh` **accepts** the package whenever doc-style prose greps match, `py_compile` succeeds, and the JSON parses — and it **rejects nothing that is actually broken**: it exits 0 on this landing while the gate is unrunnable (defect 1) and the pack fails 4/6 (defect 2). Worse, it **pins the defect**: line 9 greps for the empty `effects: []` literal, so declaring real effects would break the skill's own validator. > **v7's proposed gate check is withdrawn — it was incompatible with its own repair.** Improvement 1 **deletes** `skills/security/scripts/security-gate.sh`. v7 then proposed a validator command that runs that package-local path and accepts only when it reaches the toolchain. After the prescribed deletion the command does not exist, so the witness could never go green: the repair and the test contradicted each other. The package-local exit 1 is real evidence, but it is evidence of the **current** defect, not the shape of the **future** check.

*Future — three commands added to `validate.sh`, all satisfiable only after the repairs land:*

1. **Root gate executes and passes.** Run the **canonical root** gate — `scripts/security-gate.sh --mode quick`, the path `SKILL.md:52`, `:67`, and `:77` already document. **Accept** iff it exits 0 **and** its summary satisfies the predicate the skill itself publishes at `SKILL.md:129` — `jq -e '… .gate_status=="PASS" …' "$OUT/security-gate-summary.json"`. **Reject** on nonzero exit, a missing or unparsable `security-gate-summary.json`, or `gate_status != "PASS"`. *(Verified in the v8 pass: the root gate exists and `scripts/toolchain-validate.sh` **is present at the root** — which is exactly why the root copy resolves its toolchain and the package-local copy, one directory deeper, does not.)*
2. **The duplicate is absent.** **Accept** iff `skills/security/scripts/security-gate.sh` **does not exist**; **reject** if the package-local copy is present. *(Fails today — the file exists and is byte-identical to the root copy at `deeea2c…6ee3f7`.)* This is the assertion that makes improvement 1 checkable, and it replaces the withdrawn attempt to execute the deleted path.
3. **Redteam passes against its own tree.** Run `prompt_redteam.py scan` against the repository. **Accept** iff exit 0; **reject** on the current exit 3 with four FAIL.

Checks 2 and 3 fail today; check 1 is not runnable as a *validator* assertion today because `validate.sh` never invokes any gate. Together they are compatible with improvements 1–3: after the duplicate is deleted, check 2 goes green, check 1 exercises the surviving canonical path, and neither references a path the repair removes. *Maps to improvement 4 — and improvement 3 must remove the `:9` empty-literal pin in the same change, or improvements 3 and 4 deadlock: declaring effects turns the validator red for the right reason while it is still green for the wrong ones.*

**Ledger.** `P0-SEC-GATE`, `P0-SEC-REDTEAM`, `P0-SEC-PRODUCES`, `P0-SEC-RELEASE`, `P0-EFFECTS`; `P1-SEC-VAL`, `P1-OWASP`; `P2-N-I`, `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (11); grandfather membership of both `.py` in the v8 pass's ratchet output; all 11 files present in the re-verified §12 inventory; **root/package gate byte-identity recomputed in the v8 pass — both hash to `deeea2c6848fd07ee29064b0b0ade49cee35edd5ba69f06bc0dd5717636ee3f7`**; **`scripts/toolchain-validate.sh` confirmed present at the repository root and absent under `skills/security/scripts/`**, which is the mechanism of the defect; `SKILL.md:52`, `:67`, `:77` confirmed to document the **root** gate path, and `:129` to publish the summary validator predicate. **(b)** all 11 files in full; `scripts/validate.sh` read in full (22 lines) — `:9` `grep -q '^  effects: \[\]$'` pins the empty literal, `:19` `python3 -m py_compile …` writes bytecode into the package. **(c)** the package-local gate (exit 1 on missing `scripts/toolchain-validate.sh`), the redteam (exit 3, four FAIL, two PASS), and the security validator exiting 0 in a scratch copy with `PYTHONPYCACHEPREFIX` outside the repository — the **v9 Sol review** (`c22ce975…`), the most recent review to run them; `audit.sh` PASS — the **v7 Sol review** (`bb650549…`). **None was executed by this lineage.**

**not_checked.** **Neither the v8 pass nor this v10 reseal executed the skill-local gate, the redteam scan, or `scripts/validate.sh`.** `validate.sh` invokes `python3 -m py_compile`, which writes bytecode into the package; it was deliberately not run. v4's claim that its lineage executed it remains **withdrawn**. No binary suite run (`collect-dynamic` executes a target binary); `tests/scripts/test-security-suite-redteam.sh` not run.

**Residual risk.** **Highest of the twelve.** A security skill whose gate cannot run, whose declared output does not exist, and whose own adversarial pack fails — all while its validator reports PASS and structurally prevents one of the fixes.

### 8.10 `skill-builder` — 46 files

**Intent.** Own the full structural lifecycle of one `skills/<slug>/` package: create, check, heal, audit, and — on the **shadow** rail — compile and prove a typed contract.

**RPI-loop fit.** Target **implementation**, seam **implement_method**, T2. Correctly disclaims scheduling, writer allocation, Git, candidate validation, learning promotion, and post-failure decisions. **Its compiler and readiness ledger are shadow and non-authoritative by its own contract** (§2) — the shadow rail informs a future cutover; it does not route anything today.

**Live behavior / I/O / effects / authority.** Out: source package, build/audit/compile/probe reports, and **`.agents/audits/<slug>-build.json`**. **Effects incomplete, not "typed and honest"** — §4.1. Three API1 entries plus shadow records with `kind` / `scope` / `authorization` / `cleanup` / `receipt`, **none of which covers the build-report filesystem write**; `source-write` is scoped to "explicitly named `skills/<slug>` source package" and `projection-write` to "explicitly invoked owned skill projections." **`artifacts.produces: build-report` declares the artifact, not the effect** (§4.2). Incomplete-nonempty bucket.

**Strengths — real and preserved.** The only typed effects model in the corpus. The only artifact implementing the `PASS | FAIL | NOT_PROVEN` trichotomy, with `NOT_PROVEN` when confinement was unavailable, timed out, was interrupted, cleanup was incomplete, or live-root bytes changed. **Fail-closed confinement:** `probe_runtime._confined_command` returns `proven: False, command_executed: False` off macOS or without `sandbox-exec`, and the runner then exits 125 without running the command. A real mutation-boundary proof in `validate.sh` hashes the package, runs `heal.sh --check`, re-hashes, and fails on any change. Self-witnessing invariants AST-walk its own source to require that `ContractError` is constructed only by `_reject`, with unique literal IDs, and the test asserts the invariant inventory maps exactly onto the hostile fixture cases.

**Exact defects.**
1. **(P0, `P0-RATCHET`)** Five execution-path Python files fail the accepted ADR-0016 `skill.python-ratchet` at upstream scope: `check_migration_readiness.py`, `compile_contracts.py`, `contract_v3.py`, `probe_runtime.py`, `run_contract_probe.py`. None is grandfathered; the growth guard rejects adding them. **The rail's engineering quality does not exempt it from the gate.** Its other five execution-path `.py` *are* grandfathered.
2. **(P0, `P0-EFFECTS`)** **The build-report write is undeclared.** `scripts/init.sh:149` creates `.agents/audits/`, `:150` binds `<slug>-build.json`; `scripts/build.sh:32` rewrites it; `SKILL.md:316` declares the path. No API1 or shadow effect covers it (§4.1). v4's "effects typed and honest" is **withdrawn**.
3. **(P0, `P0-HEAL-N-A`)** Five artifacts specify an auto-fixing `heal.sh` that does not exist.
4. **(P1, `P1-N-B`)** `scripts/test-mutation-boundaries.sh` is red at its first assertion (`fix_rc=2`, expected 1), never reaches its sibling-isolation or traversal assertions, and is not wired into `validate.sh`. **Corrected repair owner:** the plan's **D7** transactional projection owner, not "mirror codex-sync."
5. **(P1, `P1-HEAL-MESH`)** `heal --fix` regenerates the mesh unscoped.
6. **(P1, `P1-LAYOUT`)** `.agents/audits/` violates the closed layout and is **implemented**, not merely documented.
7. **(P1, carried)** `P1-HEAL-SUBSET` heal checks a subset of claimed fields; `P1-HEAL-CRASH` heal crash ≡ ordinary findings; `P1-GUARD-RG` guards fail open without `rg`; `P1-PROFILE` `skill-conformance-profiles.yaml` load-bearing but unlinked; `P1-INIT-OC` `init.sh` omits `output_contract`.
8. **(P2, `P2-N-F`)** Dual kernel-length standards (250-line cap vs 220/500/800 bands) with its own `SKILL.md` at 353 lines tripping the first (`audit.sh` **WARN**); `scan_descriptions.py --probe` entirely unexercised (0 probe pairs across 49 skills); `craft_score.py` provenance false-positive classes; `test_contract_v3.py` writes into the live `skills/` tree; `audit.sh` requires bash ≥4; `validate.sh` does not set `PYTHONDONTWRITEBYTECODE`.

**Improvements.**
1. **(P0, `P0-RATCHET`)** Route the five failing files into the `ao` binary with `sh` glue, per ADR-0016 §3 — allowlisting is mechanically rejected by the growth guard.
2. **(P0, `P0-EFFECTS`)** Declare the `.agents/audits/<slug>-build.json` write as a real effect with an explicit scope — and note that its presence in `artifacts.produces` **does not discharge this** (§4.2).
3. **(P0, `P0-HEAL-N-A`)** Resolve N-A: build the auto-fixer or retract it from all five artifacts.
4. **(P1, `P1-N-B` + `P1-HEAL-MESH` + `P1-LAYOUT`)** Implement D7's transactional projection owner; turn `test-mutation-boundaries.sh` green and wire it into `validate.sh`; scope the heal mesh regeneration; relocate `.agents/audits/`.
5. **(P1 carried + P2, `P1-HEAL-SUBSET` / `P1-HEAL-CRASH` / `P1-GUARD-RG` / `P1-PROFILE` / `P1-INIT-OC` / `P2-N-F`)** Copy `contract_v3.load_frontmatter`'s guard into `heal.sh`; separate crash from findings so a non-strict run cannot swallow both; fail closed without `rg`; link `skill-conformance-profiles.yaml`; emit `output_contract` from `init.sh`; then the P2 batch — trim `SKILL.md` under the cap, exercise or delete `--probe`, fix the `craft_score.py` false-positive classes, move `test_contract_v3.py`'s writes out of the live tree, guard the bash ≥4 requirement, and set `PYTHONDONTWRITEBYTECODE` in `validate.sh`.

**Witness.** *Current — two real ones exist, and one is red.* `scripts/validate.sh`'s mutation-boundary proof **accepts** a package whose bytes are unchanged across `heal.sh --check` and **rejects** any change — a genuine reject shape. The contract-v3 self-witnessing invariants AST-walk the source and **reject** any `ContractError` not constructed by `_reject` or carrying a duplicate literal ID. Against those, `scripts/test-mutation-boundaries.sh` is **red at its first assertion** (`fix_rc=2`, expected 1; carried from the **v7 Sol review** (`bb650549…`), the last review to run `test-mutation-boundaries.sh`), never reaches its sibling-isolation or traversal assertions, and is not wired into `validate.sh` — so the containment property it exists to prove is currently unproven. *Future — three:*

1. **Ratchet.** **Accept** iff `scripts/check-skill-python-ratchet.sh --scope upstream` exits 0; **reject** on the present exit 1 with five in-scope files. *Maps to improvement 1.*
2. **Build-report effect.** Run `init.sh` against a scratch repository root and enumerate every path written outside `skills/<slug>/`. **Accept** iff each is covered by a declared effect scope; **reject** on the present uncovered `.agents/audits/<slug>-build.json`. *Maps to improvement 2.*
3. **Heal auto-fix.** A fixture missing `skill_api_version` with an unlinked reference: run `--fix`, then `--check --strict`. **Accept** iff the second call is clean; **reject** if `--fix` does not exist (today), or exists but is not idempotent. *Maps to improvement 3.*

**Ledger.** `P0-RATCHET`, `P0-EFFECTS`, `P0-HEAL-N-A`; `P1-N-B`, `P1-HEAL-MESH`, `P1-LAYOUT`, `P1-HEAL-SUBSET`, `P1-HEAL-CRASH`, `P1-GUARD-RG`, `P1-PROFILE`, `P1-INIT-OC`; `P2-N-F`.

**Checked.** **(a)** the ratchet at both scopes with the five in-scope failures enumerated from the v8 pass's upstream run; frontmatter `effects` read; tracked-file count (46); `.agents/audits` in the destination sweep; **`init.sh:149-150`, `build.sh:32`, and `SKILL.md:316` re-read verbatim in the v8 pass**; the shadow `source-write` and `projection-write` scopes re-read verbatim at `SKILL.md:42-50`; all 46 files present in the recomputed §12 inventory. **(b)** all 46 files in full; the `artifacts.produces` `build-report` entry; the shadow-authority statements at `SKILL.md:273-275` and `:320-321`. **(c)** `audit.sh` **WARN** and `test-mutation-boundaries.sh` exit 1 at first assertion with `fix_rc=2` — the **v7 Sol review** (`bb650549…`), the last review to run either; the v8 and v9 reviews did not.

**not_checked.** `heal.sh --fix`, `build.sh`, `init.sh`, `codex-sync.sh`, and `generate-skill-mesh.py` were **not** executed — all mutate the live tree. N-A, the mesh scope, and the scaffold findings rest on source reading plus the red harness. The full contract-v3 proof was not run; its test corpus writes into the live `skills/` tree. Linux confinement was not exercised; the host is macOS.

**Residual risk.** **High.** The rail is well engineered *and* currently ships in a form the accepted ADR forbids; its legacy heal/audit layer has diverged from its specification; and its effect model — the corpus's only typed one — does not describe a write it performs on every build.

### 8.11 `standards` — 16 files

**Intent.** Load the smallest set of standards justified by the caller's change and return cited findings. Explicitly not a linter.

**RPI-loop fit.** Target **evidence**, seams **plan_input + implement_method + validate_evidence**, T4 — three seams, the widest reach of the twelve. It advises and stops: "does not edit, validate, retry, approve, commit, release, deliver, or decide continuation." Its reach is why its template defect is P0.

**Live behavior / I/O / effects / authority.** In: paths, language, risk cues. Out: cited findings to stdout. **`effects: []` accurate.**

**Strengths.** All 15 references are linked and all 15 links resolve. The mutation-safety standards (single audited chokepoint / hash-witnessed backups / self-administered ambition gate) carry an explicit stop condition. `test-pyramid.md`'s "Green tests are factual evidence, not a semantic verdict" is the correct framing of the Implement→Validate seam. `go.md`'s declared Go 1.26 target matches `cli/go.mod` — verified, no drift.

**Exact defects.** *(P0, `P0-TEMPLATE`)* It owns the frontmatter template that seeds `effects: []` corpus-wide, while a second canonical template in `skill-builder` disagrees with it and `init.sh` disagrees with both. **All three surfaces were read verbatim in the v8 pass; the conflict is semantic, not merely a key-set difference:**

| Surface | Role | `metadata.effects` | `output_contract` |
|---|---|---|---|
| `skills/standards/references/skill-structure.md:34-51` | declared owner / doctrine template | **present, seeded `[]`** (`:45`) — a bare empty literal with **no deliberateness marker**, so "no effects" and "nobody filled this in" are indistinguishable | **present** (`:50`) |
| `skills/skill-builder/references/skill-template.md:19-47` | second canonical template | **absent entirely** — the string `effects` occurs **0 times in the whole file**, and `metadata.effects` is likewise missing from its own required-key list at `:189-195` | **present** (`:46`) |
| `skills/skill-builder/scripts/init.sh` | live emitter | **emitted, silently defaulting `[]`** — `:56` `effects="${SKILL_EFFECTS:-[]}"`, interpolated at `:88` `  effects: $effects`; a caller who says nothing gets `[]` with no prompt and no record | **not emitted** — `output_contract` occurs **0 times in the whole file** (`P1-INIT-OC`) |

The three surfaces also disagree on structure independently of `effects`: the owner declares **eight** structural paths — `practices`, `hexagonal_role`, `consumes`, `produces`, `context_rel`, `metadata.capabilities`, `metadata.canonical_status`, `metadata.disposition` — none of which appear in `skill-template.md`; `skill-template.md` declares a `context:` block and `metadata.stability`, neither of which appears in the owner. **A new skill's frontmatter therefore depends on which of the three surfaces its author followed.**

**The arithmetic assertion 1 uses, stated once so no reader reconciles two numbers.** Those eight are the structural disagreements *excluding* `effects`. **Assertion 1 compares the complete nested-path sets, so `metadata.effects` — owner-declared at `:45`, absent from the template — is the ninth member: `8 + 1 = 9`.** Measured in the v9 pass over the owner block (`skill-structure.md:35-50`) against the template's canonical block (`skill-template.md:20-46`), the difference is **nine in both directions**:

| Direction | Count | Paths |
|---|---:|---|
| Owner declares, template lacks | **9** | `practices` · `hexagonal_role` · `consumes` · `produces` · `context_rel` · `metadata.capabilities` · **`metadata.effects`** · `metadata.canonical_status` · `metadata.disposition` |
| Template declares, owner lacks | **9** | `user-invocable` · `context` · `context.window` · `context.intent` · `context.intent.mode` · `context.sections` · `context.sections.exclude` · `context.intel_scope` · `metadata.stability` |
| Shared by both | 7 | `name` · `description` · `skill_api_version` · `metadata` · `metadata.tier` · `metadata.dependencies` · `output_contract` |

*(v8's table row said eight, counting only the structural set and omitting `metadata.effects`. Assertion 1 requires the complete set, so nine is the figure that binds — F2.)*

*(P2, `P2-N-G`)* Dead `#dedup-manifest` TOC anchor; `javascript.md` missing from the canonical-owners table though it exists and is linked; duplicated Security/General sections in `python.md`. *(P2)* No validator.

**Improvements.**
1. **(P0, `P0-TEMPLATE`)** Correct or annotate the `effects` line in the frontmatter template — it is the mechanical origin of five of the six `P0-EFFECTS` members.
2. **(P1, `P1-INIT-OC` companion — no new unit)** Reconcile **all three** surfaces to one declared owner under a **semantic** parity check — nested key paths *and* default-value meaning, not key presence — that additionally asserts `effects` is deliberate rather than silently empty and that `output_contract` is emitted by `init.sh` and validated by `heal.sh`. See the witness below for the exact accept/reject shape.
3. **(P2, `P2-N-G`)** Fix the dead `#dedup-manifest` anchor, add `javascript.md` to the owners table, and de-duplicate the Security/General sections in `python.md`.
4. **(P2, `P2-CEREMONY`)** A reference-link resolution check, converting today's read-verified 15/15 into a standing gate.
5. **(P2, `P2-CEREMONY`)** Name `skills/pattern-mining/scripts/validate-output.sh` in the canonical-owners table as the reference output-contract validator. **Verified in the v8 pass: zero references to it exist anywhere in `skills/standards/`** — the doctrine owner does not point at the corpus's one working example, which is a direct cause of the unvalidated-output cluster in §8.2, §8.6, and §8.8.

**Witness.** *Current:* none. `standards` has no validator, and the 15/15 link resolution is a fact established by reading — nothing would fail if a link broke tomorrow.

> **v6's proposed witness is withdrawn.** It compared `skill-structure.md` against `heal.sh` and `init.sh`, **omitting `skill-template.md` entirely**, and it compared **key sets** — but `metadata.effects` is present in *both* `skill-structure.md` (`:45`) and `init.sh` (`:88`), so a presence comparison **cannot fail on `effects`**. It would have failed only on `output_contract`. v6's claim that it "fails today on `output_contract` and on `effects`" was half false, and a witness that cannot fail on the defect it names is not a witness for `P0-TEMPLATE`.

*Future — a three-surface semantic conformance check.* Declare **one owner** (`skills/standards/references/skill-structure.md`, per this skill's doctrine role). Normalize each of the three surfaces to a map of **nested key path → default-value classification**, where a path is fully qualified (`metadata.effects`, never bare `effects`, so a top-level key can never satisfy a nested one) and each value is classified as one of `absent | empty-literal | placeholder | caller-supplied-with-empty-fallback | deliberate`. The surfaces are:

- `skills/standards/references/skill-structure.md` — declared owner;
- `skills/skill-builder/references/skill-template.md` — second canonical template;
- `skills/skill-builder/scripts/init.sh` — live emitter (classify the value it *emits*, including its shell default).

**Accept** iff all three assertions hold:

1. **Path coverage.** The three surfaces declare the same set of nested key paths, and every path the owner declares is emitted by `init.sh` and checked by `heal.sh`.
2. **`effects` deliberateness.** `metadata.effects` is present on all three surfaces **and** classifies as `deliberate` on each — either a nonempty effect list, or an empty list carrying an explicit justification marker (e.g. `effects: []  # verified: no filesystem, process, network, or environment effect`). For the emitter, `deliberate` additionally requires that it **cannot silently produce `[]`**: it must require the caller to state effects, or emit the justification marker.
3. **`output_contract` emitted and validated.** `output_contract` is present in every template, **emitted by `init.sh`**, and checked by `heal.sh`.

**Reject** on any of: a nested key path present on one surface and absent on another; `metadata.effects` classified `absent` on any surface; `metadata.effects` classified `empty-literal` (a bare `[]` with no marker); an emitter whose `effects` classifies `caller-supplied-with-empty-fallback`; `output_contract` missing from a template, not emitted by `init.sh`, or unchecked by `heal.sh`.

**This fails today on all three surfaces, in three distinct ways** — which is the property v6's version lacked:

| Surface | Assertion 1 (paths) | Assertion 2 (`effects`) | Assertion 3 (`output_contract`) |
|---|---|---|---|
| `skill-structure.md` | **reject** — declares **9** paths absent from `skill-template.md` (the eight structural paths plus `metadata.effects`; assertion 1 requires the *complete* nested-path set, so `metadata.effects` counts here even though assertion 2 also judges its default) | **reject** — `empty-literal` at `:45` | pass |
| `skill-template.md` | **reject** — declares `context.*` and `metadata.stability` absent from the owner | **reject** — `absent` (0 occurrences in the file) | pass |
| `init.sh` | **reject** — emits no `context.*` | **reject** — `caller-supplied-with-empty-fallback` (`:56`) | **reject** — not emitted (0 occurrences) |

Note that assertion 2 is what distinguishes this from v6's check: it compares **default-value meaning**, so it fails on the owner and the emitter *even though both contain the key*. *Maps to improvement 2; improvement 1 is the owner-side half of assertion 2, and improvement 4 converts the link-resolution fact into a second standing gate.*

**Ledger.** `P0-TEMPLATE`; `P2-N-G`, `P2-CEREMONY`. *(The template-reconciliation repair is owned by `P0-TEMPLATE` and its `init.sh` half by `P1-INIT-OC` — see §6.2.1; no separate unit is minted, which is why `P1-INIT-OC` occurs in two sections.)*

**Checked.** **(a)** frontmatter `effects`; tracked-file count (16); `standards` → `validate-output` cross-reference count = **0**; **all three template surfaces read verbatim in the v8 pass** — `skill-structure.md:34-51` (owner frontmatter block; fences `:34`/`:51`, declarations `:35-50`), **`skill-template.md:19-47`** (canonical frontmatter block; fences `:19`/`:47`, declarations `:20-46`, including `name` at `:20` and `description` at `:21`) plus its required-key list at `:189-195`, and `init.sh:56` and `:74-92` (the emitted heredoc); **whole-file occurrence counts: `effects` = 0 in `skill-template.md`, `output_contract` = 0 in `init.sh`**; **the nested-path sets of both frontmatter blocks extracted and differenced in both directions in the v9 pass (9 / 9 / 7 shared; the two template-side names were corrected to their fully qualified forms in v10 — §B2)**; all 16 files present in the recomputed §12 inventory. *(v8's Checked block cited the template block as `:33-47`; that range begins mid-block and omits `name`/`description`, and is corrected here — F2.)* **(b)** all 16 files in full; `cli/go.mod` Go version. **(c)** `audit.sh` **PASS** — the **v7 Sol review** (`bb650549…`).

**not_checked.** Whether the guidance is actually followed in the wider codebase.

**Residual risk.** Medium — as the doctrine owner, its template defect propagates to every future skill.

### 8.12 `toil-mining` — 4 files

**Intent.** Mine explicitly supplied history for repeated toil and emit ranked evidence, without filing work or scheduling automation.

**RPI-loop fit.** Target **evolution**, seams **goal_observe + post_verdict**, T6. **Explicit-history evidence only** — it takes caller-supplied JSONL paths and a timezone-qualified window, never discovering its own inputs. "Does not file work, schedule automation, or mutate a tracker"; the helper is "retrieval/report-only."

**Live behavior / I/O / effects / authority.** In: explicit JSONL paths + a timezone-qualified window. Out: inline report by default; optional `.agents/toil-mining/YYYY-MM-DD-candidates.md`. **Conditionally writes; `effects: []` false** (`P0-EFFECTS` member), and **`.agents/toil-mining/` violates the closed layout** (§3.2).

**Strengths.** `scripts/recent_human.py` is the best-engineered script in the twelve: explicit paths only (no discovery), rejects naive and reversed windows, treats non-empty `client_id` as the UI-origin boundary and reports `missing_client_id` rather than guessing, counts malformed records instead of dropping them, sorts before dedup so output is path-order independent (with a dedicated test), and emits structured `checked` / `not_checked` whose entries are genuinely self-limiting. `tests/test_recent_human.py` asserts the full exclusion histogram by exact count. `scripts/validate.sh` (14 lines) runs four prose greps and then `python3 -m unittest discover` over that real suite — placing it in the **executes-real-skill-logic** class alongside `pattern-mining` and `skill-builder` (§8.0). Its one execution-path `.py` is **grandfathered** (§3.1).

**Exact defects.** *(P1)* A three-way output mismatch: `produces: [result.json]` (never written) vs `output_contract: ranked evidence report under .agents/toil-mining/` vs the body's inline-by-default `candidates.md`. *(P0)* `effects: []` false. *(P1)* `.agents/toil-mining/` noncanonical. *(P2)* `user-invocable: false` while the description advertises caller trigger phrases. *(P2)* `audit.sh` **WARN** (`constraints-frontloaded`, `quality-rubric`) — verified in the v8 pass: `SKILL.md` has no `## Constraints` heading. *(P1)* **The ranking contract is prose-only.** `SKILL.md:55-66` declares a three-occurrence floor ("Two occurrences are a coincidence… never round an impression up to a frequency") and names *salience mining* as the failure mode; `SKILL.md:67-83` declares ranking "by the product of three measured factors" — frequency, cost, error-proneness — with each shown beside the composite and an unmeasured factor "scored at the floor, never guessed at the midpoint," naming *frequency-only ranking* as the failure mode. **The helper does no clustering and no scoring**, so neither rule has any machine boundary. The skill discloses this in its own `not_checked`, but the headline contract is unenforced.

**Improvements.**
1. **(P0, `P0-EFFECTS`)** **Declare the conditional write** — a member of the effects class, inheriting P0 under §7.0.
2. **(P1, `P1-TOIL-OUT`)** Reconcile the three output declarations to the body's actual behavior.
3. **(P1, `P1-LAYOUT`)** Reclassify and relocate `.agents/toil-mining/` under a permitted tier.
4. **(P1, acceptance criterion of `P1-TOIL-OUT`, not a new unit)** **Give the ranking contract a machine boundary.** Add a fixture validator with **three cases**: (i) the three-occurrence floor excludes a below-floor cluster at its **actual measured count** even when that cluster would otherwise score highest, and ordering among **at least two eligible clusters** follows the declared `frequency × cost × error-proneness` product — chosen so the product order differs from frequency, cost, error, source, and reverse-source order — with all three inputs shown beside each composite; (ii) a **metamorphic permutation** case that reassigns a fixed factor multiset across the cluster identities under all six permutations, so that **no hard-coded ordering can pass**; and (iii) **separately**, an unmeasured factor reported as unmeasured and scored at the floor rather than the midpoint. This is the repair the v5 shortening dropped; the witness below is its acceptance test, with every factor and expected product published as a number. Case (i) alone is insufficient — it cannot rule out a constant, which is why (ii) exists.
5. **(P2, `P2-INVOCABLE` + `P2-CEREMONY`)** Reconcile `user-invocable: false` with the advertised trigger phrases; add a `## Constraints` heading and a third quality bullet to clear the two `audit.sh` WARNs.

**Witness.** *Current:* `scripts/validate.sh` runs four prose greps and then the real unittest suite — **3 unit tests PASS, contract PASS** (carried from the **v9 Sol review** (`c22ce975…`)) — and `tests/test_recent_human.py` **rejects** any change to the exclusion histogram by exact count. That is a genuine accept/reject boundary; it places this skill in §8.0's executes-real-skill-logic class, one of three. **It proves the retrieval helper and nothing about ranking**: `recent_human.py` performs no clustering and no scoring, so no ordering is produced for any test to check. Coverage, not existence, is the gap here.

> **v6's proposed fixture is withdrawn.** It defined only cluster A (2 occurrences, below the floor) and cluster B (9 occurrences). Once A is correctly excluded, **B is the only eligible cluster**, so "ranks B above any cluster with a lower product" is vacuous and the reject condition "if any single factor determines the order" is unobservable — with one ranked item, every ordering rule agrees. v6 tested the floor and *claimed* to test ordering. The defect is proof design, and the claim is withdrawn.

*Future — case 1: the ranking fixture (four clusters, three eligible).* Build a supplied-history fixture whose measured factors are fixed and published:

| Cluster | Occurrences (frequency) | Cost (median s/occ) | Error-proneness (fraction) | Product | Eligible? |
|---|---:|---:|---:|---:|---|
| **A** *(narrated "we do this constantly")* | **2** | 900 | 0.50 | *(900.0)* | **No — below the three-occurrence floor** |
| **B** | **40** | 5 | 0.05 | **10.0** | Yes |
| **C** | **6** | 120 | 0.50 | **360.0** | Yes |
| **D** | **25** | 200 | 0.01 | **50.0** | Yes |

Products: B = 40 × 5 × 0.05 = **10.0** · C = 6 × 120 × 0.50 = **360.0** · D = 25 × 200 × 0.01 = **50.0**. A's would-be product is 2 × 900 × 0.50 = **900.0** — deliberately the **largest in the fixture**, so the floor must exclude it *despite* scoring ranking it first. That proves the floor **precedes** ranking rather than acting as an incidental tiebreak.

**Expected order: C (360.0) > D (50.0) > B (10.0).** The three eligible clusters are chosen so that this order is distinct from every degenerate rule a wrong implementation might use:

| Ordering rule | Order it would produce | Same as product order (C, D, B)? |
|---|---|---|
| **`frequency × cost × error` (the contract)** | **C, D, B** | — |
| frequency only | B (40), D (25), C (6) | **No — exactly reversed** |
| cost only | D (200), C (120), B (5) | **No — wrong head** |
| error-proneness only | C (0.50), B (0.05), D (0.01) | **No — 2nd and 3rd swapped** |
| source order (fixture emits A, B, C, D) | B, C, D | **No** |
| reverse source order | D, C, B | **No** |

**Exactly what case 1 proves, and what it does not.** It defeats those **five** named degenerate rules. **It does not defeat a constant.** A scorer hard-coded to emit `C, D, B` — computing nothing — passes case 1 with full marks. v7 claimed a row here for "any fixed constant order" and generalized in §10 to "every degenerate order … constant"; **that claim was false and is withdrawn.** One fixture with one expected order cannot distinguish "computed the product" from "printed the answer." Case 2 exists precisely to close that gap.

**Accept** iff the emitted report:

1. **excludes A as below the three-occurrence floor**, reporting its **actual measured count of 2** rather than rounding the impression up (`SKILL.md:55-66`);
2. ranks the three eligible clusters **exactly C, then D, then B** — in particular **C (frequency 6) above B (frequency 40)**, which is the assertion that proves product ordering rather than frequency ordering (`SKILL.md:67-83`); and
3. **shows all three input factors beside every composite score**, so the caller can re-weigh them.

**Reject** on any of: A admitted at all; A reported at a rounded-up or narrative frequency rather than its measured 2 (the named *salience mining* failure); B ranked above C (frequency-only ordering — the named *frequency-only ranking* failure); D ranked above C (cost-only ordering); B ranked above D (error-only ordering); the eligible clusters emitted in source or reverse-source order; or any composite score printed without its three inputs.

*Future — case 2: the metamorphic permutation companion, which does defeat every constant.* Hold the three eligible factor-triples fixed as a **multiset** and reassign them across the three cluster identities under all **3! = 6** permutations. Cluster A and the input emission order are held constant across every run, so nothing but the label→factor assignment varies. The triples:

```text
t1 = (frequency 40, cost   5, error 0.05)  →  product  10.0
t2 = (frequency  6, cost 120, error 0.50)  →  product 360.0
t3 = (frequency 25, cost 200, error 0.01)  →  product  50.0
```

Run the scorer once per permutation and require the emitted order to follow the **recomputed** products:

| # | Assignment | Products, ranked | Required order |
|---|---|---|---|
| π1 | B=t1, C=t2, D=t3 | C 360.0, D 50.0, B 10.0 | **C, D, B** |
| π2 | B=t1, C=t3, D=t2 | D 360.0, C 50.0, B 10.0 | **D, C, B** |
| π3 | B=t2, C=t1, D=t3 | B 360.0, D 50.0, C 10.0 | **B, D, C** |
| π4 | B=t2, C=t3, D=t1 | B 360.0, C 50.0, D 10.0 | **B, C, D** |
| π5 | B=t3, C=t1, D=t2 | D 360.0, B 50.0, C 10.0 | **D, B, C** |
| π6 | B=t3, C=t2, D=t1 | C 360.0, B 50.0, D 10.0 | **C, B, D** |

**The six permutations produce six distinct required orders — all 6 orderings of three labels.** *(Enumerated mechanically in the v9 pass; see §9.1.)* That is the property case 1 lacked, and it yields the discrimination directly:

| Wrong implementation | Permutations passed |
|---|---|
| **any** fixed constant order (e.g. always `C, D, B`) | **1 of 6** — fails 5 |
| source order / label-sort (always `B, C, D`) | **1 of 6** — fails 5 |
| any single-factor rule | 0 of 6 — each is already refuted by case 1 and stays refuted here |
| genuine `frequency × cost × error` | **6 of 6** |

**Accept** iff all six permutations emit their required order. **Reject** if any permutation deviates — in particular, **reject an implementation that emits the same label order for two different permutations**, which is the signature of a constant, a source-order emitter, or a label sort. Because a constant emitter passes at most one permutation by construction, no hard-coded ordering can survive case 2.

*Future — case 3: the missing-factor case (floor versus midpoint), kept separate.* Ordering and unmeasured-factor handling are different rules and are tested apart, so a failure localizes. Build a third fixture with two clusters identical on both measurable factors:

| Cluster | Occurrences | Cost | Error-proneness | Product |
|---|---:|---:|---|---:|
| **E** | 10 | 30 | **unmeasurable** — no failure, retry, or correction marker is derivable from E's supplied records | floor-scored |
| **F** | 10 | 30 | **measured 0.50** | **150.0** |

The rule under test (`SKILL.md:67-83`): "A factor the history cannot support is reported as unmeasured — scored at the floor, never guessed at the midpoint." Because E and F are identical on frequency and cost, the two behaviors are cleanly separable: **midpoint substitution** (0.50 for a 0–1 fraction) makes E's product 150.0 and E **ties** F; **floor scoring** makes E's product strictly less than 150.0 and E ranks **strictly below** F.

**Accept** iff E's error-proneness is reported as `unmeasured` — a label, not a number — **and** E ranks **strictly below** F. **Reject** on a tie between E and F (the exact prohibited midpoint substitution), on E ranked above F, or on any numeric estimate emitted for E's error-proneness. This assertion never needs to know the floor's value; it only requires floor < midpoint, which the rule itself guarantees.

**All three cases fail today in the strongest possible way: there is no scorer and no clustering, so no ordering is produced at all** — none of the three fixtures can even be run against the current helper. *Maps to improvement 4.*

**Coverage summary, stated exactly.** Case 1 proves the floor precedes ranking and defeats five named degenerate rules but **not** a constant. Case 2 defeats every constant, source-order, and label-sort implementation via six distinct required orders over a fixed factor multiset. Case 3 separates floor-versus-midpoint handling of an unmeasured factor from ordering entirely. No single case carries the whole contract, and none is claimed to.

**Ledger.** `P0-EFFECTS`; `P1-TOIL-OUT`, `P1-LAYOUT`; `P2-INVOCABLE`, `P2-CEREMONY`.

**Checked.** **(a)** frontmatter `effects`; tracked-file count (4); `.agents/toil-mining` in the destination sweep; grandfather membership in the v8 pass's ratchet output; **`scripts/validate.sh` read in full in the v8 pass (14 lines: four `grep` assertions then `python3 -m unittest discover -s tests -p 'test_*.py'`)**, which is the basis for its §8.0 classification; the ranking-contract prose at `SKILL.md:55-83` re-read verbatim; `SKILL.md` heading map (no `## Constraints`); **the six-permutation enumeration of case 2 computed mechanically in the v9 pass — six distinct required orders, any constant emitter passing 1 of 6**; all 4 files present in the re-verified §12 inventory. **(b)** all 4 files in full. **(c)** `audit.sh` **WARN** (`constraints-frontloaded`, `quality-rubric`) — the **v7 Sol review** (`bb650549…`); `validate.sh` 3 unit tests PASS and contract PASS — the **v9 Sol review** (`c22ce975…`). Not executed by this lineage.

**not_checked.** No live session JSONL was mined; the ranking fixture does not exist to be run.

**Residual risk.** Low-medium — the executable core is sound and the defects are declaration- and location-level, **except** the ranking contract, which is the skill's headline claim and has no machine boundary at all.

## 9. Probes executed

All read-only, with `PYTHONDONTWRITEBYTECODE=1` set for every step and `PYTHONPYCACHEPREFIX` pointed outside the repository for the one Python invocation. **No bytecode was created** — post-run searches for recently modified `__pycache__` directories returned **0** in every pass. The subject was verified clean after every step. Scratch output was confined to `mktemp -d` and session-scratch directories outside the repository.

### 9.1 Executed in the v9 pass *(no later pass — v10, v11, v12, or v13 — reran any of it)*

| Probe | Result |
|---|---|
| `shasum -a 256` + `wc -l/-c` on both primary inputs, **before reading either** | v8 `e85dbbf9…44ed15e` / **1214 lines / 163,315 bytes**; v8 review `c57b71d4…47aa2a66` / **264 lines / 15,953 bytes** — both match the supplied digests |
| Subject identity at open and close | HEAD `0088c6e3…`; tree `c0c43eef…`; `HEAD:skills` `a264078d…`; status **0 lines** — unchanged |
| **Direct v7 → v8 diff volume** | **165 inserted / 31 deleted** |
| **§6 subsection identity, v7 vs v8** | §6.1 **16/16 identical** · §6.2 **39/39 identical** · **§6.2.1 36→37, CHANGED, 5 diff lines** · §6.3 **14/14 identical** · §6.4 **5/5 identical** |
| **§7 identity, v7 vs v8** | **17/17 lines, byte-identical** |
| The three §6.2.1 edits enumerated | 1 inserted table row (`v8 | 561 | 12`); 2 replaced lines (narrow-extent parenthetical; growth paragraph) |
| **Standards owner block** — `skill-structure.md:34-51` | fences at `:34` and `:51`; declarations `:35-50`; **16 nested paths** |
| **Standards template block** — `skill-template.md:19-47` | fences at `:19` and `:47`; declarations `:20-46`; **16 nested paths**, including `name` at `:20` and `description` at `:21` |
| **Path-set difference, both directions** | owner-only **9** (incl. `metadata.effects`) · template-only **9** · shared **7** |
| Truncated-range control | slicing the template at v8's cited `:33-47` omits `name`/`description` and yields **11** owner-only paths — the arithmetic that produced v8's undercount context |
| `effects` occurrences in `skill-template.md` (whole file) | **0** |
| `metadata.effects` in the owner | `:45` — `  effects: []` |
| §8 extents for v9 | broad **571**, narrow **524**, **12** `Witness` blocks |
| Repository bytecode after probes | **0** recently modified `__pycache__` directories |

**Not re-executed in the v9 pass, nor by the v10, v11, v12, or v13 report-integrity passes, and not claimed as such — stated with each item's exact origin, because this evidence is heterogeneous and must not be collapsed to a single pass:**

| Carried item | Originating pass or review |
|---|---|
| The **106-row manifest re-derivation** | **five independent recomputations**, each enumerated with its result in §12: the **v6 pass**, the **v6 Sol advisory** (`dfc1cc41d1e22ba8f6d5496e6c8af0b00d1b4f2723f70fb086593066992c4bc4`), the **v7 pass**, the **v7 Sol review** (`bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76`), and the **v8 pass** |
| The **ADR-0016 Python ratchet** at both scopes, with the five in-scope failures enumerated | the **v6 pass** (§9.4) and again the **v8 pass** (§9.2) |
| The **corpus / `effects` / closed-layout sweeps**, live-skill enumeration, and the 49 / 43 / 6 `output_contract` split | the **v6 pass** (§9.4) and again the **v8 pass** (§9.2) |
| The **validator reads** — the `scripts/`+`tests/` census and all six package validators read in full, the input to the §8.0 taxonomy | the **v8 pass** (§9.2). **This is source inspection, not execution:** the v8 pass ran no validator |
| The twelve-skill **`audit.sh` sweep** (10 PASS, 2 WARN — `skill-builder` and `toil-mining`) and **`test-mutation-boundaries.sh`** (exit 1 at first assertion, `fix_rc=2`) | the **v7 Sol review** (`bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76`) — the last review to run either; the v8 and v9 reviews explicitly did not |
| The **migration-readiness ledger** (PASS; 49 rows, 1 ready, 48 explicit blockers) and the last execution of **`goals measure --json --help`** (exit 0) | the **v8 Sol review** (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`) |
| The **remaining carried skill validators, gates, tests, and fallback/readiness checks** — the skill-local security gate (exit 1), the redteam scan (exit 3, four FAIL, two PASS), the root/skill gate SHA comparison, `conformance_profile.py` and its fallback component matrix, the goals focused snapshot tests and the live eight-command tree, pattern-mining's two Bats witnesses (2/2 PASS), toil-mining's validator (3 unit tests PASS, contract PASS), doc's 14 prose checks (14/14 PASS), and craft-goal's validator (exit 0, structural only) | the **v9 Sol review** (`c22ce97509b9642610e2449dc7ef1914ed05c124e015c8ca2b2bb4db3efad046`) — the most recent review to run them |
| **`security/scripts/validate.sh`** | the **v5 Sol advisory** (`d20611ae0067a1d894f9db33522031b66e0f585230547e4c9641471352a161b0`) only, and only in a scratch copy with `PYTHONPYCACHEPREFIX` outside the repository (§9.4). **No author pass in this lineage ever ran it** |

**This table replaces the v12 sentence at this site, which was wrong in both directions.** v12 said all of the above “were executed in the v8 pass and independently reproduced by the v8 Sol review.” The v8 pass **executed no `go` command, no `ao` command, and no skill validator** — v12's own §10 `not_checked` says exactly that — and the executable evidence originates in **three different reviews**, not one, while the structural re-derivations span **four author passes**. Each item remains attributed at its point of use in §8. v9's two corrections are report-integrity repairs whose evidence is the diff and the two frontmatter blocks above.

### 9.2 Executed in the v8 pass

| Probe | Result |
|---|---|
| `shasum -a 256` + `wc -l` on both primary inputs, **before reading either** | v7 `93563e25…b5645` / **1080**; v7 review `bb650549…95c2bc76` / **290** — both match the values supplied with the task |
| Subject identity at open | HEAD `0088c6e3…`; tree `c0c43eef…`; `HEAD:skills` `a264078d…`; status **0 lines** |
| **106-row manifest re-derived and re-hashed**, diffed against the rows embedded below | **106 / 106 — 0 missing, 0 extra, 0 digest mismatches** |
| Per-package `git ls-tree` counts | `2 3 17 1 1 1 3 1 11 46 16 4` = **106** |
| Live skills / `output_contract` | **49** live; absent in `cass`, `cc-hooks`, `dcg`, `implement`, `ms`, `plan` → **43 with, 6 without** |
| Frontmatter `effects` × 12 | nonempty only in `operationalize`, `product`, `skill-builder` → **9 empty / 3 nonempty** |
| `.agents/` destination sweep × 12 | `ao` (canonical) + `audits`, `doc`, `patterns`, `toil-mining` (**4 forbidden**) |
| `fm-ws-noncanonical-topdir` detector search | **only** `docs/adr/ADR-0016-state-tiers.md` — still no live enforcement |
| `check-skill-python-ratchet.sh --scope head` | **exit 0** |
| `check-skill-python-ratchet.sh --scope upstream` | **exit 1**; 12 new shipped-Python files; 0 stale grandfather entries; snapshot-grew=0; 24 grandfathered remain |
| In-scope ratchet failures | exactly **5**, all `skill-builder`: `check_migration_readiness.py`, `compile_contracts.py`, `contract_v3.py`, `probe_runtime.py`, `run_contract_probe.py` |
| **`scripts/` + `tests/` census across the twelve** (§8.0 taxonomy input) | 6 packages with neither; validators in `craft-goal`, `doc`, `pattern-mining`, `security`, `skill-builder`, `toil-mining` |
| **All six package validators read in full** | `craft-goal` 6 lines (`exec heal.sh --check --strict`) · `doc` 24 lines, 13 `grep` · `pattern-mining/validate-output.sh` 43 lines (`jq` over a supplied artifact, exit 2 without one) · `security` 22 lines (`:9` pins `effects: []`, `:19` `py_compile`) · `skill-builder` 87 lines (runs `compile_contracts.py`, `test-contract-v3.sh`, `check_migration_readiness.py`; mutation-boundary hash proof at `:65-71`; `rg` guards at `:73-85`) · `toil-mining` 14 lines (4 greps + `unittest discover`) |
| **Root vs package-local security gate** | both hash `deeea2c…6ee3f7`; `scripts/toolchain-validate.sh` **present at root**, **absent** under `skills/security/scripts/`; `SKILL.md:52`, `:67`, `:77` document the **root** path; `:129` publishes the summary validator predicate |
| **Toil case-2 permutation enumeration** | 6 permutations → **6 distinct required orders** (all orderings of three labels); any constant emitter passes **1/6**; source-order/label-sort emitter passes **1/6** |
| Toil case-1 and case-3 arithmetic recomputed | B=10.0, C=360.0, D=50.0; excluded A would-be 900.0 (largest); F=150.0 |
| Repository bytecode after all probes | **0** recently modified `__pycache__` directories |
| Subject identity at close | HEAD / tree / `HEAD:skills` / status **all unchanged** |

**Not re-executed in the v9 pass, nor by the v10, v11, v12, or v13 report-integrity passes, and not claimed as such:** `audit.sh` across the twelve; the package-local security gate; the redteam scan; `security/scripts/validate.sh`; `test-mutation-boundaries.sh`; `conformance_profile.py` and the fallback component matrix; the goals focused tests and command tree; pattern-mining's Bats fixtures; toil-mining's validator; doc's prose checks; craft-goal's validator; the migration-readiness ledger. Each is attributed to its exact originating review — the **v5 advisory** (`d20611ae…`), **v7** (`bb650549…`), **v8** (`c57b71d4…`), or **v9** (`c22ce975…`) — in the §9.1 origin table and again at its point of use in §8.

### 9.3 Executed in the v7 pass

| Probe | Result |
|---|---|
| `shasum -a 256` + `wc -l` on both primary inputs | v6 `238d5c02…f1af97e` / **941**; v6 review `dfc1cc41…92c4bc4` / **305** — both match the expected digests |
| Subject identity at open | HEAD `0088c6e3…`; tree `c0c43eef…`; `HEAD:skills` `a264078d…`; status **0 lines** |
| **106-row inventory re-derived and re-hashed**, then diffed against **v7's own embedded rows** | **106 / 106 — 0 missing, 0 extra, 0 digest mismatches** |
| Per-package `git ls-tree` counts | `2 3 17 1 1 1 3 1 11 46 16 4` = **106** |
| **Template surface A** — `skills/standards/references/skill-structure.md:34-51` read verbatim (fences at `:34` and `:51`; declarations `:35-50`) | `metadata.effects: []` at `:45`; `output_contract` at `:50`; also declares `practices`, `hexagonal_role`, `consumes`, `produces`, `context_rel`, `metadata.capabilities`, `metadata.canonical_status`, `metadata.disposition`; **no `context:` block and no `metadata.stability`**. **Nine** of its declared paths are absent from surface B — the eight structural ones plus `metadata.effects` |
| **Template surface B** — `skills/skill-builder/references/skill-template.md:19-47` read verbatim (fences at `:19` and `:47`; declarations `:20-46`) | declares `name` at `:20`, `description` at `:21`, `skill_api_version`, `user-invocable`, `context.window`, `context.intent.mode`, `context.sections.exclude`, `context.intel_scope`, `metadata.tier`, `metadata.dependencies`, `metadata.stability`, and `output_contract` at `:46`; **the string `effects` occurs 0 times in the whole file**, and `metadata.effects` is absent from its own required-key list at `:189-195`. **Range correction (F2):** v8 cited `:33-47`, which begins mid-block and omits `name`/`description`; recomputing the path-set difference from that truncated range yields **eleven** instead of nine. The canonical block is `:19-47` |
| **Template surface C** — `skills/skill-builder/scripts/init.sh:56` and `:74-92` read verbatim | `:56` `effects="${SKILL_EFFECTS:-[]}"`, interpolated at `:88` `  effects: $effects`; **the string `output_contract` occurs 0 times in the whole file** |
| §8 extents, convention `## 8.` through `## 9.` inclusive | v5 **181** lines / **0** `Witness` blocks; v6 **413** / **12**; v7 **490** / **12** |
| §8 extents, narrower convention starting at `### 8.1` | v5 **175**; v6 **405**; v7 **473** — the same facts under a different boundary |
| P1 unique canonical IDs in §8 | **21** — unchanged from v5 and v6 |
| Five-entry improvement lists in §8 | **12 / 12** |
| Repository bytecode after all probes | **0** recently modified `__pycache__` directories |
| Subject identity at close | HEAD / tree / `HEAD:skills` / status **all unchanged** |

### 9.4 Executed in the v6 pass — carried, and the origin of the embedded appendix

| Probe | Result |
|---|---|
| `shasum -a 256` + `wc -l` on all three v6 primary inputs | v5 `93cf769b…f7a17` / **550**; v5 review `d20611ae…52a161b0` / **271**; v4 `dcfa6534…b44448` / **1185** — all three match the expected digests |
| Subject identity at open | HEAD `0088c6e3…`; tree `c0c43eef…`; `HEAD:skills` `a264078d…`; status **0 lines** |
| **106-row inventory recomputed** from `git ls-tree -r HEAD:skills/<slug>` + `shasum -a 256` | **106 rows** |
| **Recomputed rows diffed against v4 §12's 106 rows** | **identical** — 0 missing, 0 extra, 0 digest mismatches |
| Per-package `git ls-tree` counts | `2 3 17 1 1 1 3 1 11 46 16 4` = **106** |
| `check-skill-python-ratchet.sh --scope head` | **exit 0** |
| `check-skill-python-ratchet.sh --scope upstream` | **exit 1**; 12 new shipped-Python files; 0 stale grandfather entries; snapshot-grew=0; 24 grandfathered remain |
| In-scope ratchet failures enumerated from the upstream run | exactly **5**, all `skill-builder`: `check_migration_readiness.py`, `compile_contracts.py`, `contract_v3.py`, `probe_runtime.py`, `run_contract_probe.py` |
| Live-skill enumeration (`skills/*/SKILL.md`) | **49** |
| `output_contract` presence across all 49 | **43 with · 6 without** — `cass`, `cc-hooks`, `dcg`, `implement`, `ms`, `plan` |
| Frontmatter `effects` read × 12 | **9 empty · 3 nonempty** (`operationalize`, `product`, `skill-builder`); bucket assignment in §4 |
| `.agents/` destination sweep × 12 | 5 distinct names: `ao` (canonical) + `audits`, `doc`, `patterns`, `toil-mining` (**4 forbidden**) |
| `skill-builder` build-report write sites re-read verbatim | `init.sh:149` `mkdir -p …/.agents/audits`; `init.sh:150` and `build.sh:32` bind `<slug>-build.json`; `SKILL.md:316` declares the path |
| `skill-builder` shadow effect scopes re-read verbatim | `SKILL.md:42-44` `source-write` / `filesystem.write` / "explicitly named skills/<slug> source package"; `:48-50` `projection-write` / `filesystem.write` / "explicitly invoked owned skill projections" |
| **P1 canonical-ID occurrence count over §8** | **21 unique IDs**; `P1-LAYOUT` ×4 (`doc`, `pattern-mining`, `skill-builder`, `toil-mining`), `P1-INIT-OC` ×2 (`skill-builder`, `standards`), 19 IDs ×1 → **25 occurrences** |
| `context:` block presence | **present** in `domain`; **absent** in `craft-goal`, `operationalize`, `product` |
| `standards` → `validate-output` cross-references | **0** |
| `skills/domain/` package contents | `SKILL.md` only — no `scripts/`, confirming "no validator of any kind" |
| `toil-mining` `SKILL.md` heading map + ranking prose | headings `Procedure`, `Repetition threshold…`, `Weighted priority…`, `Output`, `References` — **no `## Constraints`**; floor and product rules read verbatim at `:55-83` |
| Repository bytecode after all probes | **0** `__pycache__` directories modified in the last 30 minutes |
| Subject identity at close | HEAD / tree / `HEAD:skills` / status **all unchanged** |

**Carried from the v7, v8, and v9 Sol reviews, not executed here — each attributed to the most recent review whose checked section records running it, by pinned digest:** the twelve-skill `audit.sh` sweep (10 PASS, 2 WARN — `skill-builder` and `toil-mining`) and `test-mutation-boundaries.sh` (exit 1 at first assertion, `fix_rc=2`) — the **v7 Sol review** (`bb650549…`), the last review to run either; the v8 and v9 reviews explicitly did not. The migration-readiness ledger (PASS; 49 rows, 1 ready, 48 explicit blockers) — the **v8 Sol review** (`c57b71d4…`). The skill-local security gate (exit 1), the redteam scan (exit 3, four FAIL, two PASS), the root/skill gate SHA comparison (byte-identical `deeea2c…6ee3f7`), the `conformance_profile.py` fallback matrix, the goals focused snapshot tests, the live eight-command tree, pattern-mining's two Bats witnesses (2/2 PASS), toil-mining's validator (3 unit tests PASS, contract PASS), doc's 14 prose checks (14/14 PASS), and craft-goal's validator (exit 0, structural only) — the **v9 Sol review** (`c22ce975…`), the most recent review to run them. `goals measure --json --help` (exit 0) was last run by the **v8 Sol review** (`c57b71d4…`), with its global-`--json` treatment re-confirmed by the v9 review.

**Deliberately not executed by any author pass in this lineage without bytecode confinement:** `skills/security/scripts/validate.sh`, which invokes `python3 -m py_compile` and writes `__pycache__/*.pyc` into the package. v4 claimed its pass executed it; **that claim remains withdrawn.** The **v5 Sol advisory** (`d20611ae0067a1d894f9db33522031b66e0f585230547e4c9641471352a161b0`) is the only pass that ever ran it, and only in a scratch copy with `PYTHONPYCACHEPREFIX` pointed outside the repository.

## 10. checked / not_checked

### checked

- **Executed in the v9 pass:** both primary input digests and extents verified *before* either file was read; the direct v7 → v8 diff volume; §6 subsection-by-subsection and §7 identity between v7 and v8, with the three §6.2.1 edits enumerated; both standards frontmatter blocks read at their correct fence ranges, with the nested-path sets extracted and differenced in both directions; the truncated-range control that reproduces v8's counting context; whole-file `effects` and `metadata.effects` occurrence checks; v9's own §8 extents and witness-block count; subject identity and repository bytecode at open and close.
- **Executed in the v8 pass:** both v8 primary input digests and extents verified *before* either file was read; the 106-row manifest re-derived, re-hashed, and diffed against the rows embedded in §12; per-package counts; the 49 / 43 / 6 `output_contract` split; frontmatter `effects` for all twelve; the `.agents/` destination sweep; the `fm-ws-noncanonical-topdir` detector search; the Python ratchet at both scopes with the five in-scope failures enumerated; the `scripts/`+`tests/` census and **all six package validators read in full** (the §8.0 taxonomy input); the root/package-local security gate digests and `toolchain-validate.sh` presence at root versus package; the gate paths documented at `SKILL.md:52`/`:67`/`:77` and the validator predicate at `:129`; the six-permutation enumeration and all case-1/case-3 arithmetic; the bytecode check; subject identity at open and close.
- **Executed in the v7 pass:** both v7 primary input digests and extents; the 106-row inventory re-derived, re-hashed, and diffed against v7's own embedded rows; per-package counts; all three template surfaces read verbatim with whole-file occurrence counts for `effects` and `output_contract`; the §8 extents and `Witness`-block counts for v5 and v6 under two stated conventions; the P1 unique-ID count and the five-entry improvement-list count; the bytecode check; subject identity at open and close.
- **Executed in the v6 pass (a):** all three v6 primary input digests and extents; the complete 106-row inventory recomputed against this landing and diffed against v4's rows; per-package tracked counts totalling 106; the ratchet at both scopes with the five in-scope failures enumerated; live-skill enumeration and the 49 / 43 / 6 `output_contract` split; frontmatter `effects` for all twelve; the `.agents/` destination sweep; the `skill-builder` build-report write sites and shadow effect scopes re-read verbatim; the P1 canonical-ID occurrence census; `context:` presence for four skills; the `standards` → `validate-output` cross-reference count; the `domain` package contents; the `toil-mining` heading map and ranking prose; the bytecode check; subject identity at open and close.
- **Source inspected (b):** all twelve canonical `SKILL.md` contracts in full, and every tracked file in each of the twelve packages; the goals Go owners (`root.go:153`, `module.go:112-119`, `commands.go` SnapDir / `SaveSnapshot` sites and the four early-return gates, `module_test.go:97-99`, `module.go:335`); `security/scripts/validate.sh:9` and `:19`; `skill-builder`'s `artifacts.produces` `build-report` entry and shadow-authority statements at `SKILL.md:273-275` and `:320-321`; `cli/go.mod`; root `AGENTS.md`, `docs/architecture/operating-loop.md`, ADR-0016, and the active plan's status line, D4, D7, and 49-row target matrix.
- **Carried from prior reviews (c):** every item in §9's carried list, each named with its exact originating review and pinned digest in the §9.1 origin table and again at its point of use in §8 — the **v5 Sol advisory** (`d20611ae…`) and the **v7** (`bb650549…`), **v8** (`c57b71d4…`), and **v9** (`c22ce975…`) Sol reviews. **No carried item is attributed to a reviewer generically.**
- **Executed in the v13 pass (report-integrity only — no repository evidence):** both v13 primary input digests and extents verified *before* either file was read; repository identity observed read-only at open and close (`git rev-parse HEAD`, `HEAD^{tree}`, `HEAD:skills`, `git status --porcelain=v1`); and a **mechanical whole-file sweep of the finished v13 bytes** for every occurrence of a bare validator-persona name and of *fresh run*, *fresh review*, *all were executed*, *every validator*, and *probes that produced*, confirming each surviving occurrence is either (i) inside a labelled historical quotation or a lineage/provenance table row, (ii) accompanied by its exact originating pass or review and that artifact's pinned digest, or (iii) an explicit deferral to the single origin table at §9.1 and the origin set at §13. **No repository command, validator, gate, test, or witness was run by the v13 pass.**
- **The 106-entry digest inventory is embedded at §12**, not referenced.

### not_checked

- **No semantic verdict.** This corrects an audit; it is not a `PASS | FAIL | NOT_PROVEN` judgment over a repository change.
- **No productive mutating skill run:** no documentation scaffold, README, OSS pack, `PRODUCT.md`, pattern report, toil report, skill build or init, `heal.sh --fix`, projection generation, or publication.
- **The v8 pass executed no `go` command, no `ao` command, and no skill validator — and no pass after it did either.** v9 ran only the diff and frontmatter measurements enumerated in §9.1; v10, v11, v12, and v13 reran nothing at all. Specifically: the skill-local security gate, the redteam scan, `security/scripts/validate.sh`, `test-mutation-boundaries.sh`, `audit.sh`, `conformance_profile.py`, and the goals focused tests were **not** run by any author pass in this lineage; each is attributed to its exact originating review — the v5 advisory (`d20611ae…`), v7 (`bb650549…`), v8 (`c57b71d4…`), or v9 (`c22ce975…`) — in the §9.1 origin table and at its point of use in §8.
- **No binary security suite**; `tests/scripts/test-security-suite-redteam.sh` not run.
- **No full `skill-builder` contract-v3 proof** — its test corpus writes into the live `skills/` tree.
- **Linux confinement not exercised** — the host is macOS.
- **Generated projections were not treated as authority** — `docs/SKILL-ROUTER.md`, `skills/catalog.json`, `registry.json`, and the Codex twins were not used as evidence for any finding.
- External tool behavior for `bd`, `br`, `bv`, and live goal-check commands was not exercised.
- The other **37** skills of the 49-skill corpus are out of scope; only their `output_contract` presence was queried.
- Whether any runtime consumes `skill.spec.json`.
- **None of the proposed witnesses was implemented or run** — every "future" witness in §8 is a specification, not a result. **This includes both witnesses rewritten in v7**: the four-cluster toil ranking fixture and the two-cluster missing-factor case were not built, and the three-surface template conformance check was not executed. Their published expected values (products 10.0 / 360.0 / 50.0; the three-way `effects` classification) are **derived from source read in the v8 pass**, not observed from a running scorer or checker — the current implementations of both do not exist. Nor was any proposed repair tested against repository CI.

### Disclosed process failures in this lineage

- **v3** asserted that all disputed claims were re-derived while carrying v2's unverified "all 49 skills have a nonempty `output_contract`." False — six lack it. Guard: a corpus-wide quantifier is a claim about the corpus and must be counted, never inherited.
- **v4** claimed this lineage executed the security gate, redteam, and `validate.sh` in one section while stating the opposite in two others, and cited a two-generation-stale input extent. Guard: every `checked` entry carries an explicit provenance tier, and a lineage extent names its own generation.
- **v5** shortened a sealed audit below self-containment: it kept an inventory heading with zero rows and called an absent appendix "preserved," dropped all twelve per-skill witnesses — losing the only concrete repair for a defect it still reported — and asserted an occurrence-level bijection that mechanical counting refutes. Guard adopted: **a sealed audit embeds every artifact its own headings claim**, carries one explicit accept/reject witness per audited unit, and states set-level claims over normalized identifiers with multiplicity made explicit.
- **v6** restored twelve witness blocks but **two of them could not fail on the defect they named**, and the report claimed otherwise. The toil fixture excluded one of its two clusters by the floor, leaving a single eligible cluster and making its ordering assertion vacuous; the standards check compared **key presence** where `metadata.effects` is present on both compared surfaces, so it could not fail on `effects` despite saying it did. v6 also overclaimed in two summary sentences — that v5's §8 was preserved unchanged, and that every section states both a current and a future accept/reject shape. Guard adopted here: **a witness counts only if a wrong implementation would fail it.** Concretely — an ordering assertion needs **at least two surviving items** after every exclusion rule is applied, and the fixture must be constructed so the contract's order differs from the degenerate orders it is meant to exclude (single-factor, source, reverse-source); a conformance assertion over values must compare **semantics, not key presence**, or it is blind to exactly the defects where the key exists and the meaning is wrong. Both replacements publish their numbers and their discrimination table so the claim can be checked rather than believed.
- **v8** repaired all three v7 findings in substance and passed every substantive repository criterion — then asserted **two things about itself that its own artifacts refute**. It said "§6 and §7 are untouched" while the v7 → v8 diff changed §6.2.1 in three places; the ledgers *were* untouched, and collapsing that distinction turned a true statement into a false one. And its standards table counted **eight** owner paths absent from the template under an assertion that requires the **complete** nested-path set, which is nine — an error made easier by citing the template's block as `:33-47`, a range that begins mid-block and omits `name`/`description`. Guard adopted here: **a "nothing changed" claim must name the exact unit it ranges over** — ledger membership, subsection, or file — and be measured, not asserted; and **any count derived from a cited line range must be reproducible from that range**, so the range is verified before the count is published. Both are the same failure in different clothing: a summary that is true of the part and false of the whole it names.
- **v7** repaired both v6 witnesses and both precision sentences, and the review reproduced every repository fact — but it carried **three claims its own sections refute**. It asserted that one fixture with one expected order defeats "any fixed constant order" (a hard-coded emitter passes it); it proposed a security check that executes a path its own improvement 1 deletes; and it twice called toil-mining "one of only two behavioral validators" while describing real boundaries in three other skills. Guard adopted here, extending v6's: **a single fixture can never refute a constant — only varied input can.** An ordering claim that must exclude hard-coding requires a **metamorphic** companion that permutes inputs while holding the factor multiset fixed, so that a fixed output order is right at most once (§8.12 case 2). And **a future witness must be satisfiable under the repair it accompanies** — if the improvement deletes a path, the witness may assert that path's *absence* but must exercise the surviving one. Any aggregate count over the twelve must name its classifying question and be re-derivable by command (§8.0), or not be stated.

- **v10, v11, and v12** each declared the provenance-attribution defect closed while at least one site was still open. v10 said it had named the originating pass “at every such point”; v11 said the same of the footer and close state; v12 enumerated the complete v6/v7/v8/v9 origin set in §13 — and **all three left the `goals` §5.1 validator-persona sentence untouched**, alongside two broad v8-only provenance sentences that contradicted exact origin maps published elsewhere in the same document. The defect is not that a site was missed once; it is that the completeness claim was re-asserted three times without being measured. Guard adopted here: **a “provenance is now complete” claim must be backed by a mechanical sweep of the finished bytes** — every occurrence of a bare persona name, *fresh run*, *fresh review*, *all were executed*, *every validator*, and *probes that produced*, outside labelled historical quotation — not by the author's recollection of which sites were edited. v13 ran that sweep and records it in §10.
## 11. Residual risk

1. **The T2 rail is well engineered *and* red against the repository's own accepted blocking gate.** Any consumer citing "T2 is sound" must also carry §3.1. The same skill carries a second, independent defect: its typed effect model — the corpus's only one — does not describe a write it performs on every build (§4.1).
2. **Source precedence must stay explicit.** A generated readiness ledger can be internally exact and still non-authoritative. Until the all-catalog cutover, legacy API1 and `skill-catalog.v3` remain live authority and the plan's matrix is the statement of intended placement, not of current state.
3. **Accepted-but-ungated decisions are a standing hazard.** Four closed-layout violations are real violations of an accepted ADR with **no live detector** — `fm-ws-noncanonical-topdir` exists only inside the ADR text. Nothing will catch a fifth.
4. **Behavioral proof remains the deepest gap — stated as a list, not a ratio.** **Five specific high-value guarantees have no machine boundary at all:** doc's no-overwrite promise, product's byte-preservation promise, craft-goal's continuation artifact, operationalize's evidence floor, and toil-mining's ranking contract. Each is traceable to its own section, and each is the load-bearing claim of the skill that makes it. *(v7 quantified this as "ten of the twelve have no witness that can fail on behavior." That figure is **withdrawn** — it contradicted this report's own descriptions of `automation-shape-routing`, `pattern-mining`, and `skill-builder`. §8.0 replaces it with a defined taxonomy: 6 skills have no package validator, 3 have one that executes no skill logic, 3 have one that executes real skill logic. Note that the taxonomy and this list answer different questions — `toil-mining` appears in the executes-real-logic class **and** on this list, because its validator covers the retrieval helper while the ranking contract it advertises has no scorer to test.)*
5. **Specifying a witness is itself an error-prone act, and this lineage has now failed at it in three consecutive rounds — six distinct defects.** In v6: the toil fixture excluded its own comparison case, and the standards check compared key presence where the defect was semantic — neither could fail on its target. In v7, after both were repaired: the toil table claimed a generality one fixture cannot have, and the security witness executed a path its own repair deletes. In v8, after those were repaired: the standards witness undercounted its own assertion-1 path set by one, and cited a template line range from which that count cannot be reproduced. None of the six is visible from reading the prose; all six are visible only by asking *what would a wrong implementation do, would this notice, is this still runnable after the fix lands, and can I re-derive every published count from the ranges cited?* Only **three** of the twelve witness specifications have been revised under that scrutiny — `toil-mining` (v7 and again in v8), `standards` (v7), and `security` (v8). **The other nine have not** — `automation-shape-routing`, `craft-goal`, `doc`, `domain`, `goals`, `operationalize`, `pattern-mining`, `product`, and `skill-builder` were reproduced as discriminating by an independent reviewer but were never adversarially re-derived here. Every future witness in §8 is a specification that has never been executed. A reader implementing any of them should re-derive the discrimination argument rather than trusting the accompanying claim.
6. **One repair is self-blocking.** Declaring `security`'s real effects turns its own `validate.sh:9` red, because that line pins the empty literal. Improvements 3 and 4 in §8.9 must land together or the skill cannot be fixed without appearing to break.
7. **Counting conventions are judgment, not measurement.** 8 / 21 / 6 follows from §7.0's class rule applied to the explicit IDs in §6. A reader who splits `P0-EFFECTS` into six per-skill findings gets 13 P0; one who folds `P1-OP-RETURN` into `P0-EFFECTS` gets 20 P1 and a seventh class member; one who counts P1 occurrences rather than units gets 25 (§6.2.1). The IDs and the rule are published so the number can be recomputed rather than argued about.
8. **Provenance discipline is the load-bearing methodological claim — and this lineage reached it by increments, not in one pass.** This report separates executed / inspected / carried at every point of use. **v10 replaced the main-body “this pass” execution labels** — the defect the v9 Sol review recorded — **but it did not, in fact, name every origin at every point.** Three sites survived it: the footer and close state (repaired in **v11**); the incomplete origin set and the non-identifying *Sol's fresh run(s)* / *Sol's fresh reviews* labels (repaired in **v12**, on the v11 Sol review's B1/B2); and — proving the point — **the `goals` §5.1 sentence and two broad v8-only provenance sentences, which survived v10, v11, *and* v12** until the fresh v12 Sol review found them. **v13 completes that mapping**: every operative carried execution now names its originating pass or review artifact with a pinned digest, or defers explicitly to the single origin table at §9.1 and the complete origin set at §13, with no site deferring to a different one. The separation is still only as good as its own honesty: nothing here was executed that §9 does not list, no author pass in this lineage ran the security validator, and **v13 itself reran no repository evidence of any kind**.
9. **This report is unvalidated.** It corrects a rejected audit against a fresh advisory; only an independent judge can bind it, and the context that authored it is not author-distinct from a Validate over the same subject. Several ranks remain judgment calls a reviewer may reasonably reweigh — notably placing the closed-layout set at P1 rather than P0 because no live gate enforces it, and resolving the toil-mining conflict by promoting the member to P0 rather than demoting the class to P1 (§7.0). The rule is stated so the choice is auditable, not so it is beyond question.

## 12. Per-file SHA-256 inventory (106 files) — embedded, recomputed against this landing

**Provenance of these rows.** Membership was re-derived from `git ls-tree -r --name-only "HEAD:skills/<slug>"` for the twelve packages, and **every digest recomputed with `shasum -a 256` against the working tree at HEAD `0088c6e3824da201eabb1e751ac8e976599e0b5c`**. This has now been done independently five times against the same landing, with the same result each time:

- **in the v6 pass** — the recomputed set diffed against v4 §12's rows: **identical**;
- **by the v6 advisory** — the embedded rows compared against the live tracked corpus: **106 / 106, zero duplicate, missing, extra, or mismatched**;
- **in the v7 pass** — re-derived and re-hashed, then diffed against the embedded rows: **106 / 106**;
- **by the v7 review** — embedded manifest versus live corpus: **106 / 106, zero duplicate, missing, extra, or digest mismatch**;
- **in this v8 pass** — re-derived from `git ls-tree` and re-hashed with `shasum -a 256`, then diffed against **the rows embedded below**: **106 / 106, zero missing, zero extra, zero digest mismatches**.

Five independent recomputations against the same landing, same result each time. v8 is seeded from the v7 bytes and edited into a new path, so these rows are byte-identical to the reviewed artifact by construction; the v8 re-verification confirms they also still match the live tree.

Reproduce:

```bash
cd /Users/bo/dev/agentops-worktrees/skill-overhaul
for s in automation-shape-routing craft-goal doc domain goals operationalize \
         pattern-mining product security skill-builder standards toil-mining; do
  git ls-tree -r --name-only "HEAD:skills/$s" | sed "s|^|skills/$s/|"
done | sort | xargs shasum -a 256
```

```text
1472c7364c385a410780fed030c242b03c180e36600018cd9399a054c6a7a672  skills/automation-shape-routing/SKILL.md
f76b572093efe4f1b792448cb13fd4f979949b09c940296dfbdc7597a9a9d636  skills/automation-shape-routing/skill.spec.json
30acded86a6a2ba8d5093070b416c90b4d550dc1f48e2e34f172e39fd21a76a8  skills/craft-goal/SKILL.md
14a2c4d9ecc8e9be7fe83ef7eca38f150f94552eac7d723eaf0e7f422526422b  skills/craft-goal/references/goal-prompt.md
6a42ec444e882b99c86860a1ed86ea643a3636fabf49e0d8e59a131df5c63df2  skills/craft-goal/scripts/validate.sh
f1ed80718c34938df60527272cb3ae16c85285611b1cb04eb004a20faca31cca  skills/doc/SKILL.md
b3534765cf33d51fbbfcb0284271f9f0f656de2dc22578ad0cd8318be2e9437d  skills/doc/references/architecture-report.md
10523751077d1a63a333e6c43b4d75634ddbe48cc379c79a615748329c7793ff  skills/doc/references/de-slopify.md
0d2f70fd4632b8629f87dd0ec218683b7b0bb5942fea669ee775365cb377e8a9  skills/doc/references/default-mode.md
9b04230e875818221a7707cc45c4057fe4fa17c497df98bb65b1cd7ed087712e  skills/doc/references/doc.feature
027fe1db6dfdeece7019d4ecde69bee657428a9b56eddf88b57621f3f0b49942  skills/doc/references/generation-templates.md
1d653aeb51c282c7d90c8eaf7e150d7b745fbd4660a9bbd293cc7db35c7c8e22  skills/doc/references/oss-docs.feature
d9bec13fc96af2290bf0e1ae11fdf03aca9d4fa2ba62a1e25a251882b55d83b5  skills/doc/references/oss-documentation-tiers.md
8e5bf84f751435563675f55c4678bfb0110aeba8b42bdf089430112dd200dee2  skills/doc/references/oss-pack.md
3c78f072f8c7332aadb60d88f2e81b5bae7a279403a32adf972816f0d81a73ff  skills/doc/references/oss-project-types.md
bebe4baa8e283c9458caabf5bb617d9ed72cf257cfbbfff18f214622c1085ae1  skills/doc/references/project-types.md
c10139f26c8c64cd6e5af8d2a66cc096f8a0fed36b64b4e101c85071a9e85ce4  skills/doc/references/prose-and-report-workmanship.md
9d99d19251bedb3c65c69b4c056fbfdf0d663f3ac8452f7f7407f7ffe7db0a6a  skills/doc/references/readme-craft.md
354ad2ba799fd35d23033d377c9a01d12d301ff9e53f900bad1dac5654a2b011  skills/doc/references/readme.feature
7c1c48dc6b61457ca6991b18cbd255c7c8e0e94fa2c4663518d2d3d4cf10daca  skills/doc/references/validation-rules.md
36d7ddd672611a9761aca3b2013af5a5fc8799091312a70dbe9bdd9ecae58e70  skills/doc/scripts/audit-oss-docs.sh
de151bd81a3e05aec0166f033e579590f35866eea54cf8e55937ec8f5fd6722e  skills/doc/scripts/validate.sh
bc0fabecdcccee78f8d0a252bc9e58f64dc24f5c66e8a1402031d693aeea1c0c  skills/domain/SKILL.md
f569246fce9a074259aba9ba590f92e93e85544b36d8ea11d632ab02a8279cb4  skills/goals/SKILL.md
696fd9246250f422d2d09d9d8c75a74c1be2a70e837c536416938aed4b112db1  skills/operationalize/SKILL.md
22d1385e5e434b987e92e82ab0be5522e8f6abd551381e7c1c0510337fa0f718  skills/pattern-mining/SKILL.md
a58c16777e6ed6a7185ca7ab41a942bed3da2cc741c3eabaa393ad2630e6894f  skills/pattern-mining/references/pattern-mining.feature
5d287493b420d90594f1e6878f9a482549dda0fda08526292aa46af32de177f7  skills/pattern-mining/scripts/validate-output.sh
0db4ffc5d76efcb5c8a3420de763b1061c25e218091c1b15b6609163b165f2d1  skills/product/SKILL.md
270fa1427203d820ef4d65a5b43c991dc88eb8792ae66834c93546a5930217df  skills/security/SKILL.md
9cff6d368e97dd02ff8866c94f144067af3dd45653c98bdcc86d15f23cfcfdb4  skills/security/references/agentops-redteam-pack.json
5847b57cda2cb84a09dd1d3fd9974a9a84954e509be4001dc00dfad98a2a44e0  skills/security/references/owasp-checklist.md
b08a6a25351882cb99ea9fce0889ac56823edfd62c0d34a4808de3af88da9870  skills/security/references/policy-example.json
20dede4e1e6835aa3d4edf37bfc8e0a93b513f0424fa292c3be9fa10de1a3272  skills/security/references/security-suite-runbook.md
bf4b6de03a9a4abf1bc4f3211f3352889345e494b33f7fa0c42e8409ea46ee53  skills/security/references/security-suite.feature
452a3d75b15089f1b064dd7532562a7ddc8fad7f1c29494bf17a84af72ede1ff  skills/security/references/security.feature
5b466792dad1a0c4236fc9cedf150c2595cbd1a5aa279268f970abbe914440e3  skills/security/scripts/prompt_redteam.py
deeea2c6848fd07ee29064b0b0ade49cee35edd5ba69f06bc0dd5717636ee3f7  skills/security/scripts/security-gate.sh
bb70ed1ead776c9f34eaf6ed9a6245af585e6ca7ac43b4821ad60e7ff8ef2bbc  skills/security/scripts/security_suite.py
c600b3d11da932079076fcab9bd4b2397e0928d58f78b74a2b863bf4d57beaeb  skills/security/scripts/validate.sh
3860da6af7ffac6141e9ff40ccc322d271e1888db4fd22606a551dd7bb152990  skills/skill-builder/SKILL.md
d72c48ba5e43d08fb25efd9844478686cd6da105fdb9d6105fb36eeaf4e37c93  skills/skill-builder/fixtures/contract-v3/cases.json
3721c2787db26390f83f2605e7b99de3fb6c27c13eca215b3043ded91053c922  skills/skill-builder/fixtures/contract-v3/invariants.json
daa9191daecb4dd21e0c15336d664b8e370e9e59935ddc704e20e0feeb0640e3  skills/skill-builder/fixtures/contract-v3/probe-harnesses/absolute-live-write.py
23321a01014f2e80da8fcde3d0b106165f571f28b70786cc809a8857bc9eefeb  skills/skill-builder/fixtures/contract-v3/probe-harnesses/large-output.py
9918fd621b0645a225c0a77d46a62cc864142af638f445912da3f9a2059fed67  skills/skill-builder/fixtures/contract-v3/probe-harnesses/leave-descendant.py
ac296c990bb7649d0be6ea24b0308e2a038ed012407abe5eae8fcfabc689fbd3  skills/skill-builder/fixtures/contract-v3/probe-harnesses/mutate-copy.py
cdb5e6a5220acd6ad9060f12937767dd54c48f8940700dcdaf57d98f18eec1cb  skills/skill-builder/fixtures/contract-v3/probe-harnesses/spawn-and-sleep.py
3c40c8e5bbbbefe6a04958871439f454365905467f78f63e170e58c5aa28ca6d  skills/skill-builder/ledgers/migration-readiness.json
a68ca2e603ea03ffd47e844782e6f4559153e770afde933502b75e312896d2d6  skills/skill-builder/receipts/skill-builder-contract-v3-probe.json
537463cd03c253eafc434d8810fb8508a277eb78fd24537840e6eab3f4964cfa  skills/skill-builder/references/audit-checks.md
edd869e5bbc5bc3f8225a6f2cd851a16237a993a9fe6693ddcd3a3ffdeb65ac5  skills/skill-builder/references/authoring-doctrine.md
bb8cc99e1bf9db690a5fd50d2220893474d754e46ac5064d99d74248f95fa01e  skills/skill-builder/references/codex-parity.md
d5d715644054734995391d8fa1a875064b700b250b8ee895b8abf4a7eb1cae9a  skills/skill-builder/references/context-density-checks.md
a9a378adc642b90171c34aefc3a534f1557ce672e7b20a22df47bc1874282adb  skills/skill-builder/references/heal.feature
da9725022360800e93054df0e87ddf1d3550e9f4e520d322e3c5dab6c181f8d2  skills/skill-builder/references/skill-auditor.feature
cc37adcb6e5a74cff3c80e80a5468d30ffb266888a8bc25d34ee831b9df66728  skills/skill-builder/references/skill-builder.feature
628ea74041b4d90a8b3fb46b7c0763325a512a1a0753e85a5e5ec7eabaca6328  skills/skill-builder/references/skill-conformance-profiles.yaml
c71f5a466e9a94c3f411ca66bba4b5c4e01e965fed3f968abf2ae7aeabeaf3e4  skills/skill-builder/references/skill-contract-v3.md
704de33f7bca4f9c64b466845b5439b76e9b3aa188d110a01554c584e519b797  skills/skill-builder/references/skill-template.md
086ed43023bdfafa7327eff2794c48c91b6e0c19ed1b82880f4701f4e01ce8ff  skills/skill-builder/schemas/audit-report.json
91675777825af5b75f5402ea5b65cb7e4760c536aa39fe4b0e68ca9aed20267d  skills/skill-builder/schemas/build-report.json
8860ea2641c1f4465a8d68a3139aaea146994fbd1e1fe174f3fc3b89ac5eb561  skills/skill-builder/schemas/compile-report.json
d05a29988800d0f6f184495b382f2a9608237676aa5aaf2ef36331d966fffa24  skills/skill-builder/schemas/probe-report.json
ac9ece6592e986d349d8fdeb4a7fbf4dc5dcab8d1dd0576503bad42017c9e51b  skills/skill-builder/scripts/audit.sh
1843cad51c6bc38a09cb64095c91dc926d525be7c90e2aa322b08af59559b09f  skills/skill-builder/scripts/authoring_scan.py
5dee51f0af5491b001aaa3f3fd0420ef620d2fecda431b74bbcf7a0145bb67ba  skills/skill-builder/scripts/build.sh
00b03619314b0ef171bb2ae8084fce8425715ac6532490389c7d64fd0e4ae5b3  skills/skill-builder/scripts/check_migration_readiness.py
cc258da89ed544313c4f6808f1a449561b6690efa67556766ba8b876c6bbee8e  skills/skill-builder/scripts/compile_contracts.py
e17683ab4d7755634d9f5d47277f0190221e729ae2618731d5f4c721a8ef7bea  skills/skill-builder/scripts/conformance_profile.py
a4521aaff66b4774905a72cd69ea497428fe1ab50f70d21d67c2b701710fda39  skills/skill-builder/scripts/contract_v3.py
87d2a62501cc4857b32e338716f8b47652b6366cb2f5cb22d419ebf0a7231dfc  skills/skill-builder/scripts/craft_score.py
b79bf29e5fb947ae842d227d14176ec1a1baf3432ba83c460dfe25dc14eaa2f5  skills/skill-builder/scripts/heal.sh
e764c5db57b882de88710644869258243cada7398c73bac7694917bd5c198494  skills/skill-builder/scripts/init.sh
ccc625632662ec489f23735ec4a5a4b8351488a0c9725f32f677f321bf095d53  skills/skill-builder/scripts/probe_runtime.py
c09d3228994a596ed1e7f16b06832183dc63d8dbfaf1080c5a03960b5feb1595  skills/skill-builder/scripts/run_contract_probe.py
93ca5016ae3760baa1c2453257ee303539acd82182a7b3b5bd343da8c7dad979  skills/skill-builder/scripts/scan_descriptions.py
efb26dc7d0ca0e5629454580c9acd61252f0c4636d939ec5e04bb88598f22f32  skills/skill-builder/scripts/score_agentops_skill.py
84b532adfffcea9538e6cec63160997b3e1ca7ede4031dffb0f457dd6c078214  skills/skill-builder/scripts/test-authoring-mutations.sh
19bf410629d477e86c66486429465c23d8dc12bbc00b27f61e72fd8a2c327b7f  skills/skill-builder/scripts/test-contract-v3.sh
d84bfa8f82a0f5d89d00c83d802198f53e9ebb0f59965dc2d22dd0730e7e9d47  skills/skill-builder/scripts/test-craft-mutations.sh
063dd7f7b360f62fd2c419b0cd444259bc1100021066d44efccdf926f98d6c2e  skills/skill-builder/scripts/test-mutation-boundaries.sh
e28b8f177c02315328c4ce44c2c32b7202bdc4a02e5e128bc8ce36761ec16e01  skills/skill-builder/scripts/validate.sh
35468a5f5800c956068421d26e9d795ecbf21eb40e47a1918f910f485c63c767  skills/skill-builder/tests/test_contract_v3.py
a8b7b77da95b4ed427f11d6846282506da24ffd16f1291a0d35ec9f51aa57cc7  skills/skill-builder/tests/test_migration_readiness.py
b67e56eff0aa80941cbee16d82c7d04fb749e70981df8c98580d5b657e7bae7b  skills/skill-builder/tests/test_probe_runner.py
b579715ad79dca7a284184419bdddcb62903900a8368c70b0da7b4016968525c  skills/standards/SKILL.md
7dbb7f0d36130bcf39149bcbb7fb9b1445b8baefa56eda94ec4242c435633c24  skills/standards/references/common-standards.md
ab20ef6432fcf386f06dfbe2d402b0e156c48c878a0ae2d55eb754d196ae4cfd  skills/standards/references/go.md
1151dd9b7529106d263639e4670b6003d74b49d5ecf94b939d13ee1d78c850e0  skills/standards/references/javascript.md
494b3e08d23ecc697e52e05d9bb57db836bafca9b98513456eb6ca3661157b1f  skills/standards/references/json.md
adf15bc5288476ae3a71ff015445d9c722410df6470cc74e769a248bf91900cc  skills/standards/references/llm-trust-boundary-checklist.md
9ff2e2a46d581883cd63077c865fc1e63d3554a3d042b76db941ddad14dc292a  skills/standards/references/markdown.md
010a20e6b48c0fdfc6b511a7869b81cf75682b78106dcaf8b0de03a834deb667  skills/standards/references/python.md
3b630df0269598a839843e97075435abd2abc5bda0d669bd18e783694c50a9f2  skills/standards/references/race-condition-checklist.md
837014209e488e2d63c714aa22795b4b31c2046abcfdb48b16882cbaad12cd88  skills/standards/references/rust.md
179c10dfc702bbaa97b9d0c75143e753600db135ae46fc5a03511d4fe5812b3a  skills/standards/references/shell.md
9499c1d8ecd451607cf9c16c1b1f1be5ddf4bf38fed8a8d8f1fbb42e5366d12e  skills/standards/references/skill-structure.md
2318dcc08070166e572fe2e5217b6821bcbfdefdb46c1e1a16fa8ed790ec379d  skills/standards/references/sql-safety-checklist.md
b705b46dd235b720c74730ea7118e87c3aa98c91a2c3cb09a103a44ae9c98e64  skills/standards/references/test-pyramid.md
b700a1d6396a3636568144cbdb069165e4710bcd7a2e756139ee0eaabf8188a1  skills/standards/references/typescript.md
fc1a18346749556c00f69ccfd629eebe78790956e7e5ee148fd89710f92f821a  skills/standards/references/yaml.md
a4ed1adfb7d2cb6086cc804b4fd973bc65c9e4d367f46095e11b8ddd55263a60  skills/toil-mining/SKILL.md
ae24d537778432a356d032581eb938313906ebcf662f22280d7a07de6b79147e  skills/toil-mining/scripts/recent_human.py
84a8d83ba3d3d04b9ecee3f0fbe22a634265b607f3c47bfe428215b7ea83d4d1  skills/toil-mining/scripts/validate.sh
45cc2a75535b4089e3ac76543b26999c26c8a0dcadfc72788add4c3f706ccdd3  skills/toil-mining/tests/test_recent_human.py
```

**Row count: 106.** Package tally reading down the list: `automation-shape-routing` 2 · `craft-goal` 3 · `doc` 17 · `domain` 1 · `goals` 1 · `operationalize` 1 · `pattern-mining` 3 · `product` 1 · `security` 11 · `skill-builder` 46 · `standards` 16 · `toil-mining` 4 — **2 + 3 + 17 + 1 + 1 + 1 + 3 + 1 + 11 + 46 + 16 + 4 = 106.**

## 13. Closing state

```text
HEAD        : 0088c6e3824da201eabb1e751ac8e976599e0b5c   (unchanged)
HEAD tree   : c0c43eefb8042af5a6a7877c0f7f0de80149ffc6   (unchanged)
HEAD:skills : a264078d2795f3f7238e0c9ff5a8c15149a77c8d   (unchanged)
status      : clean — git status --porcelain=v1 returned 0 lines
```

**What the v13 correction did and did not do.** v13 is an **exact four-site attribution repair** over v12, on the two defect classes the fresh v12 Sol review (`4522542fe9ec83ea22ce3b7b04de6d8c47a88da422848b61154f4c774ba02d68`, **REQUEST_CHANGES**) left open. It (1) named the **v8 Sol review** (`c57b71d4…`) as the originating execution of `goals measure --json --help` in §5.1; (2) replaced the blanket §9.1 sentence that assigned the 106-row re-derivation, the sweeps, the validator reads, and “every skill validator, gate, and test” to the v8 pass and the v8 Sol review with the **heterogeneous origin table** — the v6, v7, and v8 author passes for the structural re-derivations, the **v7 Sol review** (`bb650549…`) for the twelve-skill `audit.sh` sweep and `test-mutation-boundaries.sh`, the **v8 Sol review** (`c57b71d4…`) for the migration-readiness ledger and the last `goals` help execution, and the **v9 Sol review** (`c22ce975…`) for the remaining carried validators, tests, gates, and fallback checks; (3) made §11 item 8 truthful about v10 having *not* closed the mapping; and (4) replaced the close-footer v8/v9-only hygiene description with the complete **v6–v9** author/review origin map. **That is all it did.** v13 **re-ran no repository evidence** — no command, validator, gate, test, or witness — and changed **no finding, severity, count, classification, per-skill analysis, improvement, witness result, limitation, or manifest row**. The **106 manifest rows are byte-identical to v12's** and remain the product of **five** independent recomputations (§12). No repository file, Git state, tracker record, or predecessor artifact was created, edited, or deleted.

*(v12's own statement of what it did, retained as lineage:)* v12 was a report-integrity **attribution mapping correction** over v11: it enumerated the complete v6/v7/v8/v9 origin set for carried executions (B1), replaced every non-identifying *Sol's fresh run(s)* / *Sol's fresh reviews* label with the exact originating review pass and its pinned digest (B2), and corrected the §12 manifest narrative from *three* to *five* recomputations to agree with its own enumeration and summary (B3). It re-ran no repository command, validator, gate, test, or witness, and changed no finding, count, classification, or manifest row. **The v12 Sol review confirmed B3 repaired and every substantive twelve-skill criterion PASS, and found B1 and B2 still open at the four sites v13 repairs.**

*(v11's statement of what it did, retained as lineage:)* v11 corrected footer and close-state wording only: it removed the stale atomic-write sentence that named the v9 path, reconciled the close-state contradiction by attributing every inherited probe to its originating pass, dropped the unverified `read-only` mode claim, and reset the provenance chain onto the measured v10 identity.

**v13, like v12, executed only a read-only observation of repository identity** — `git rev-parse HEAD`, `HEAD^{tree}`, `HEAD:skills`, and `git status --porcelain=v1` — reported in the close block above. **That is identity observation, not evidence execution.** It re-derives no finding, re-runs no witness, and supports no criterion. **Complete origin set for every substantive probe in §9 and §10 — the exact map, not a collapse:** the structural re-derivations belong to the **v6, v7, v8, and v9 audit-author passes** (§9.1–§9.4), with the 106-row manifest recomputed **five** times across the v6 pass, the **v6 Sol advisory** (`dfc1cc41d1e22ba8f6d5496e6c8af0b00d1b4f2723f70fb086593066992c4bc4`), the v7 pass, the **v7 Sol review** (`bb650549…`), and the v8 pass (§12); the executable validators, tests, gates, and fallback/readiness probes are carried from the **v7, v8, and v9 Sol reviews** and pinned at each point of use — the twelve-skill `audit.sh` sweep and `test-mutation-boundaries.sh` from the **v7 Sol review** (`bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76`), the last to run either; the migration-readiness ledger and the last `goals measure --json --help` execution from the **v8 Sol review** (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`); and the remaining carried executables from the **v9 Sol review** (`c22ce97509b9642610e2449dc7ef1914ed05c124e015c8ca2b2bb4db3efad046`), the most recent review to run them. `security/scripts/validate.sh` was run only by the **v5 Sol advisory** (`d20611ae0067a1d894f9db33522031b66e0f585230547e4c9641471352a161b0`), in a scratch copy (§9.4). **No probe in §9 or §10 belongs to v10, v11, v12, or v13; none of those passes re-ran any of them.**

*(v10's own statement of what it did, retained as lineage:)* v10 corrected report-integrity wording only: it replaced every main-body current-pass execution label with the exact originating pass, corrected two flattened nested paths to `context.intent.mode` and `context.sections.exclude`, and reset the provenance chain and footer. **v10 reran no repository command and no witness.** Every finding, count, witness result, per-skill judgment, improvement, and limitation is carried unchanged from v9; the behavioral-domain classification remains **9 owner-only / 9 template-only / 7 shared**.

**v13 did not rerun any witness or any substantive evidence.** **v13 is independently unreviewed** — the v9 Sol review binds v9; the **fresh v12 Sol review** (`4522542fe9ec83ea22ce3b7b04de6d8c47a88da422848b61154f4c774ba02d68`, 256 lines / 13,385 bytes) returned **REQUEST_CHANGES** on **v12** (`8fe38af42a681ae9eafea0af8f776831d7cd03dbf4a0158e90675b3ecbd02bcd`, 1,319 lines / 188,877 bytes), confirming **B3 repaired** and every substantive twelve-skill criterion and all **106** manifest rows **PASS** while leaving open the **B1/B2 attribution** defects v13 repairs; and **no review exists for these v13 bytes.** **No `PASS | FAIL | NOT_PROVEN` runtime verdict is claimed, issued, or implied by v13**, no validation is asserted, and **v13 confers no implementation authority** — it authorizes no repository remediation and binds nothing.

No repository file was created, edited, or deleted by v13, v12, v11, or v10. No generation, commit, merge, push, tag, tracker mutation, or verdict by any of them, and no predecessor artifact was edited or re-sealed. No productive mutating skill was run in any pass of this lineage.

*(Inherited probe hygiene, attributed exactly — the complete v6–v9 origin map, not a v8/v9 summary:)* the probes that produced this report's evidence were run across **four author passes and four reviews**, never one. Author passes: the **v6 pass** ran the first 106-row recomputation, the ADR-0016 ratchet at both scopes, the corpus/`effects`/closed-layout sweeps, and the `skill-builder` and `toil-mining` source re-reads (§9.4); the **v7 pass** re-derived and re-hashed the manifest and read all three template surfaces with whole-file occurrence counts (§9.3); the **v8 pass** re-derived the manifest a third time, re-ran the ratchet and the sweeps, and read all six package validators in full (§9.2); the **v9 pass** ran the §6/§0.1.1 diff and the standards-path measurements (§9.1). Reviews: the **v6 Sol advisory** (`dfc1cc41d1e22ba8f6d5496e6c8af0b00d1b4f2723f70fb086593066992c4bc4`) cross-checked the embedded manifest against the live corpus; the **v7 Sol review** (`bb650549bc100a9df61ee2ccee38fa5392cbf8da873d979dd7f41c59895c2c76`) ran the twelve-skill `audit.sh` sweep and `test-mutation-boundaries.sh`; the **v8 Sol review** (`c57b71d410b6249e4b94481bec78ee61e84c1c40b6256ab338111aba47aa2a66`) ran the migration-readiness ledger and the last `goals measure --json --help` execution; the **v9 Sol review** (`c22ce97509b9642610e2449dc7ef1914ed05c124e015c8ca2b2bb4db3efad046`) ran the remaining validators, gates, tests, and fallback checks; and the **v5 Sol advisory** (`d20611ae0067a1d894f9db33522031b66e0f585230547e4c9641471352a161b0`) ran `security/scripts/validate.sh` in a scratch copy. This is the same map published at §9.1 and in the origin set above, with no site deferring to a different one. Every author-pass probe was read-only with `PYTHONDONTWRITEBYTECODE=1` set and `PYTHONPYCACHEPREFIX` outside the repository, scratch output confined to directories outside it, and a post-run check confirmed **zero** repository bytecode was created. **v10, v11, v12, and v13 ran none of them.** Both v13 primary inputs are preserved unchanged at their stated digests, and the sealed v6 through v12 artifacts were left untouched.

**This report is advisory.** It issues no `PASS | FAIL | NOT_PROVEN` verdict, authorizes no repository remediation, and binds nothing. Its findings are claims supported by the evidence disclosed in §9 and §10 and bounded by the limits disclosed in `not_checked`; the context that authored it is not author-distinct from a Validate over the same subject, so it cannot judge its own subject.

*v13 was assembled in a scratch directory from the exact v12 bytes with only the four attribution repairs above applied, then published once to `/tmp/agentops-opus5-verified-skill-audit-outcomes-12-v13.md` and set to mode `0444`; no predecessor file was written, edited, or re-sealed. Its whole-file SHA-256, line count, byte count, and final mode are computed after the last byte is written and the file is set read-only, and are reported out of band — a file cannot contain its own digest. No claim is made about write-count, rename atomicity, or authoring process beyond what the sealed bytes themselves show.*
