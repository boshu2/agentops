# Dueling Idea Wizards — Phase 6: blind-spot probe

The proposals, blind scores, reveal reactions, rebuttals, and steelmen are now
available. Look beyond the shared framing.

## Identity and write boundary

- FABLE writes only `raw/WIZARD_BLINDSPOTS_FABLE.md`.
- SOL writes only `raw/WIZARD_BLINDSPOTS_SOL.md`.
- Paths are relative to
  `docs/audits/skill-system-overhaul-duel-2026-07-24/`.
- Read both steelmen and all prior duel artifacts completely before writing.
- Do not edit any other file and do not delegate.

## Blind-spot probe

Find three to five consequential ideas or risks that neither original proposal
contained. Do not rename an existing proposal.

For each:

- explain why both wizards missed it;
- identify the affected layer, seam, skill set, CLI surface, or migration
  control;
- give the failure mechanism and earliest observable signal;
- give a falsifier;
- give the smallest non-churning mitigation;
- classify it as release blocker, tranche blocker, T7 convergence blocker, or
  post-migration watch item.

At least one blind spot must concern:

- exact identity/evidence comparability;
- migration operations or rollback;
- skill portfolio usability/routing after the metadata overhaul;
- proof that retirement/rename does not erase real use.

End with the single unproven assumption the final plan still depends on.

When complete and verified, reply only `BLINDSPOTS_WRITTEN`.
