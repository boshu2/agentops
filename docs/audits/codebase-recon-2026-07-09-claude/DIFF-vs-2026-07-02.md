# Findings Diff — 2026-07-09 Claude sweep vs. 2026-07-02

> Finding-by-finding diff of the fresh Claude recon (repo tip `2c2bfc3fb`, reports in this
> directory) against the prior landed sweep of **2026-07-02** (base `fd9f38e26`,
> `docs/audits/codebase-recon-2026-07-02/`). Cross-family baseline: the same-day
> Codex-native sweep (base `fbba8af5`,
> `docs/audits/codebase-recon-2026-07-09/` in the toqd worktree). Every claim cites the
> fresh report that carries the evidence; where the fresh Claude sweep did not look,
> that is said explicitly rather than silently merged.

## Headline

The 2026-07-02 sweep's thesis — *fail-open seams in the repo's own enforcement machinery* —
got its code-fixable core **fixed within one week**, and the fix pattern precisely confirms
the repo's own second-order law: the items with a mechanical gate converged; the items
enforced only by audit-report memory did not move or **regressed**. Meanwhile the fresh
sweep finds the risk has migrated *outward*, to boundaries no in-repo gate covers: the Go
toolchain itself (a High CVE weakening `os.Root`, the repo's containment primitive), the CI
scanner supply chain, and — per the same-day Codex run — the governance state of the trunk
itself.

---

## Fixed since 2026-07-02

| 2026-07-02 finding | 07-09 status | Fresh evidence |
|---|---|---|
| **A1 (High/P0)** — `ao gate check` fails OPEN when a blocking check's script is missing (`GateStatusUnknown` excluded from failure) | **FIXED** | [codebase-pattern-extraction.md](codebase-pattern-extraction.md) Part 1 #4: `cli/internal/gates/report.go:26-41` — `ExitCode` now documents and enforces "a blocking check fails on FAIL, **UNKNOWN**, an empty/unrecognized status, or an evaluation error (fail-closed)". Cross-family corroboration: Codex diff table row A1 ("Fixed", `orchestrator.go:191`, targeted tests pass). |
| **A2 (High/P1)** — local pre-push gate orphaned by `core.hooksPath` → `.beads/hooks`; installer writes where git doesn't look | **FIXED (incl. the recommended hijack detection)** | [codebase-archaeology.md](codebase-archaeology.md) Entry Points: live gate is `scripts/hooks/pre-push.local` installed into `$(git-common-dir)/hooks` by `scripts/install-pre-push-gate.sh`; the tracked `.githooks/pre-push` is now an explicitly labeled historical shim that *itself warns* about the hooksPath hijack, and "installer detects and refuses". The 07-02 rank-3 recommendation landed as specified. |
| **A4/A5 (High)** — ~60-69 docs teach removed `ao rpi`/`orchestrate`/`evolve` as live; retired-tech gate regex omits the verbs | **SUBSTANTIALLY FIXED — one residual** | [codebase-audit.md](codebase-audit.md) L-7: the fresh audit checked `docs/first-value-path.md:176`, `docs/knowledge-flywheel.md:24`, `docs/MIGRATION.md` — all now correctly say "removed"; the only present-tense residue found is `docs/documentation-index.md:72` (ADR-0013's consumer). Codex confirms the gate regex now covers the verbs (`check-docs-no-retired-tech.sh:48`). |
| **A6 (Medium)** — `safety/doc.go` + tests describe deleted-hook enforcement; false-green tests | **FIXED** (Claude evidence indirect) | [codebase-audit.md](codebase-audit.md) security scorecard treats `cli/internal/safety/doc.go` as correctly "stating the doctrine" of the current hookless trust lanes — no false-green flag raised on a security-primary pass. Codex diff confirms directly: doc rewritten per-control, the `simulateRunRestricted` mirror test file deleted. |
| **A8 (Medium)** — `verify-buildtags.sh` (ADR-0012) wired to nothing automated | **FIXED (per Codex; Claude saw tags present, didn't verify wiring)** | Codex diff: `scripts/ci-local-release.sh:1100` now invokes it. Fresh Claude audit only notes "Build tags (ADR-0012 archive lanes) verified present in Makefile". |
| **07-01 M-1 (carried watch item)** — `bestEffortRefreshFindingCompiler` / `bestEffortPruneAgents` execute cwd-relative scripts without the `aoBinaryInside` guard | **FIXED** | [codebase-audit.md](codebase-audit.md) Continuity section: both now route through the trusted-script chokepoint (`cli/cmd/ao/findings.go:380-384`, `session_end_maintenance_helpers.go:91-94`). |
| **07-01 L-3 (carried)** — tar extractor implicit absolute-path handling | **FIXED** | [codebase-audit.md](codebase-audit.md): explicit `filepath.IsAbs` rejection + realpath containment in `cli/cmd/ao/corpus_snapshot.go:388-405`. |
| **07-01 P2/P3 (carried)** — preamble.sh 0-adoption and JSONL-scanner sprawl with no enforcement | **RATCHETED** (frozen / melting, respectively) | [codebase-pattern-extraction.md](codebase-pattern-extraction.md) N1/N2: `scripts/.preamble-grandfather` + `check-new-scripts-use-preamble.sh` (new-files-only → debt frozen, 9 adopters); `scripts/.jsonl-scanner-grandfather` + `check-jsonl-scanner-ratchet.sh` (shrink-only → 44 grandfathered files, list melting). |

## Still open / recurred — **recurrence is the signal**

| Finding | Sweeps seen | 07-09 state |
|---|---|---|
| ⚠️ **Atomic-write adoption gap** (26 private `os.Rename` writers vs `storage.AtomicWriteFile`; `pool.atomicMove` still fsync-less) | **06-24 → 07-02 → 07-09, third consecutive sweep** | **STALLED — and it is the only prior adoption gap with NO ratchet gate** ([codebase-pattern-extraction.md](codebase-pattern-extraction.md) Part 1 #7). Crash-safety debt. This is the loudest carry. |
| ⚠️ **Hash-chain duplication, no `cli/internal/hashchain`** (48 `payload_hash` refs across 8 non-test files) | 07-02 → 07-09 | **NOT DONE, unchanged** (pattern-extraction Part 1 #1). Security-critical ledger code. Codex's pattern lane independently adds a sharpening: the 4 implementations differ on whether `prev_hash` is inside the payload hash, so a naive consolidation would be *unsafe* — versioned codecs + golden chains required. |
| ⚠️ **`ExitError` unwrap sprawl — REGRESSED** | 07-02 → 07-09 | Grew **9 → 12** concrete unwraps in `root.go:85-172` (`verifyPrePushErr`, `landErr` added); still no shared interface (pattern-extraction Part 1 #2). The only backlog item that got *worse*. |
| ⚠️ **Doc scale/contract drift** (overview says ~77 checks / 73 skills / ~139 bats / ~280 scripts; disk says **101 / 59-60 / 257 / 345**) | 06-24 → 07-01 → 07-02 (A10/A11) → 07-09, **4-sweep theme** | Measured independently by BOTH [codebase-archaeology.md](codebase-archaeology.md) and [codebase-report.md](codebase-report.md); Codex F-13 concurs. The 07-02 meta-observation stands: the recon suite is drifting against its own prior output. |
| **Capabilities contract incompleteness** (07-02 A7) | 07-02 → 07-09 | Partially fixed (per-command exit maps exist now) but recurs in new form: [codebase-audit.md](codebase-audit.md) L-1 — `ao capabilities` documents **3 env vars while the binary honors ~77** (incl. `AGENTOPS_CANON_VERIFIER_CMD`, executed via `sh -c`). Codex F-12 corroborates (strict HOLD exit 5 + verify entry still missing). |
| **`cli/cmd/ao` main-package concentration** | 06-14 → … → 07-09 | ~300 non-test command files ([codebase-report.md](codebase-report.md)); PRIOR-SWEEPS tracks 633→669 total files, no peeling into subpackages. |
| **Bash pre-push monolith / triple gate orchestration** (07-02 A3) | 06-14 → 06-24 → 07-02 → 07-09 | Still acknowledged migration debt ([codebase-archaeology.md](codebase-archaeology.md) Data Flow); reachable via `AGENTOPS_GATE_BASH=1`; no parity mechanism verified this sweep. Codex demotes to "residual, lower priority". |
| ⚠️ **The fail-open/degrade-rather-than-hold CLASS itself** | 07-02 (A1/A2/A6) → codex-07-09 (F-01/02/03/07/11) | A1/A2 instances are fixed, but the *class* recurred in five new places found by Codex (trunk verdict binding report-only, skill.schema red at base, council scanner overflow, changelog SKIP, verifycfg fallback). Per PRIOR-SWEEPS §3 this is the single highest-priority recurrence — the fresh Claude sweep flagged it as a Watch item but did **not** systematically re-verify it (SYNTHESIS "Missing reports"). |

## New since 2026-07-02 (unique to the fresh Claude sweep)

- **H-1 — GO-2026-4970: `os.Root` symlink escape in the pinned toolchain** (go1.26.3/.4; fixed in .5). The repo's own sandbox primitive is weakened for exactly the trees agents write into (`.agents/`, vendor/runtime, seed templates). [codebase-audit.md](codebase-audit.md) H-1; `govulncheck`-confirmed. **P0: one toolchain bump clears it plus M-1.**
- **M-1 — GO-2026-5856 crypto/tls ECH SNI leak** (same bump; low practical impact today).
- **M-2 — no `govulncheck` lane in CI** — precisely why H-1 sat undetected one week after 07-01's clean scan; a one-off scan goes stale in a week. [codebase-audit.md](codebase-audit.md) M-2.
- **M-3 — CI security scanners installed unpinned** (`@latest`, bare pip) — the tools gating the release are TOFU on upstream's morning, violating the repo's own pin-external-contracts doctrine. [codebase-audit.md](codebase-audit.md) M-3.
- **L-2 — stale `3.2.0-rc` fallback version** observed live on both installed binaries after v3.2.0 shipped — feeds the recorded "stale ao binary" footgun class.
- **L-3 — `eval "$gate_cmd"` in `check-pawl-pre-push.sh`** missing the trust-boundary comment convention its siblings carry; **L-5 — AGENTS-RUNTIME.md vs `check-no-tracked-agents.sh` allowlist contradiction**; **L-6 — dormant tracked nightly audit-truth lane** (single snapshot, 2026-05-07); **L-8 — ~19/90 `exec.Command` sites without context/timeout**.
- **Patterns N1/N2 — the shrink-only grandfather ratchet as the repo's highest-value extraction** (8 in-tree instances, zero shared code → `scripts/lib/ratchet.sh`), and the three-enforcement-levels law (none → grows; new-only → frozen; shrink-only → melts) proven by this week's own delta. [codebase-pattern-extraction.md](codebase-pattern-extraction.md).

## Cross-family: Claude sweep vs the same-day Codex sweep

Verdict per Codex promoted **High** and **Medium** finding (from `findings.json`):

- **F-01 (High, trunk lacks bound verdict): NOT-EXAMINED.** The fresh Claude sweep did not grep the ledger for a tip-bound verdict; SYNTHESIS carries it explicitly as a Watch item ("verify rather than assume").
- **F-02 (High, trunk gate-red on `skill.schema`/goal-design): NOT-EXAMINED at the Codex base** — with an indirect later-tip signal: the Claude audit's live gate run at `2c2bfc3fb` (8 commits later) passed 45/45 fast-scope, suggesting the failure was fixed between `fbba8af5` and the Claude tip, but fast-scope routing means skill.schema may simply not have run. Not a contradiction; unverified.
- **F-03 (High, council scanner overflow hides trailing FAIL in `tick.go`): NOT-EXAMINED** at the specific site. Adjacent-class corroboration only: pattern-extraction confirms the 64KB-truncation class is real and being ratcheted (`storage.ScanJSONL` + shrink-only grandfather), but nobody re-checked `tick.go:739-945`.
- **F-04 (Medium, `bin/ralph` invokes `claude -p` outside Door 9's boundary): NOT-EXAMINED.** The Claude report only notes Door 9 exists (`always.door9-no-claude-p`, seed.go:241) — it did not scan tracked shell executables for violations.
- **F-05 (Medium, legacy Codex dispatcher lexical symlink containment): NOT-EXAMINED.** Claude verified the ADR-0012 archive tags exist (consistent with Codex's legacy-only reachability demotion) but audited none of the tagged code.
- **F-06 (Medium, legacy required-commands bypass sandbox / ignore non-zero exit): NOT-EXAMINED** (same tagged surface).
- **F-07 (Medium, missing changelog clears blocking gate as SKIP): NOT-EXAMINED.** Sits squarely in the fail-open class the Claude sweep listed as Watch but did not probe.
- **F-08 (Medium, install.sh/nightly-evolution.sh call removed commands; whole-file baseline keeps the gate green): NOT-EXAMINED — with a telling lens split.** The Claude pattern report catalogued the very same `.scripts-ao-invocations-baseline` as a *positive* instance of the ratchet pattern (N1) without auditing its contents; Codex read the same artifact as the escape (whole-file grandfathering). Same evidence, opposite valence — worth resolving deliberately.
- **F-11 (Medium, malformed `.aoverify.yaml` falls back to weaker defaults): NOT-EXAMINED.** Claude's archaeology lists `verifycfg` in the config table without probing its failure mode.
- **F-12 (Medium, capabilities/REBOUND contracts disagree with executable truth): CORROBORATED (partially).** Claude audit L-1 independently measured the env-var half (3 documented vs ~77 honored); the REBOUND-schema-prose half was not examined.
- **F-14 (Medium, MCP surface shells literal `ao` from PATH): NOT-EXAMINED** — despite the repo's recorded stale-binary footgun class, which Claude's L-2 (stale fallback version) touches from a different angle.
- **F-15 (Medium, `make docs-check` exits 127 on a deleted validator): NOT-EXAMINED.**

**Blind spots, family vs family:** The Claude sweep looked *outward and longitudinal* — toolchain CVEs, CI supply chain, adoption-debt trajectory, ratchet mechanics — and found the only High neither prior sweep nor Codex found (GO-2026-4970), but it leaned on PRIOR-SWEEPS to avoid re-derivation and consequently re-verified almost none of the specific fail-open code paths, deferring to Codex's findings as "Watch" items. The Codex sweep looked *inward and adversarial* — executing repros against gate/council/verify code paths and auditing the live trunk's governance state — but ran no dependency/toolchain scan and missed the stdlib CVE sitting under its own base. The two lenses are near-perfectly complementary; the F-08 valence split (same baseline file read as strength by one family and as escape by the other) is the cleanest single illustration of why cross-family sweeps are worth the cost.

## Verdict trajectory: 06-14 → 06-24 → 07-01 → 07-02 → today

**06-14** read a disciplined consolidation-and-hardening window whose residual risk was doctrinal (quorum family→context weakening) plus a harsh factory-shape critique. **06-24** scored the codebase mature and self-honest — 0 Critical/High — with DRY atomic-write debt as the headline. **07-01** confirmed strong health and named the repo's governing law: *only a gate changes behavior; everything ungated decays.* **07-02** found what that law predicts — fail-open seams in the enforcement machinery itself (gate Unknown→pass, orphaned hook, ~60 docs teaching removed commands). **Today**, both families confirm the 07-02 seams were closed within a week — the law working in the positive direction — while the ungated backlog (atomic-write, hashchain) sat still or regressed (ExitError 9→12), and the risk frontier moved to the places no in-repo gate can see: the pinned toolchain (H-1), the unpinned scanner supply chain (M-2/M-3), a report-only verdict backstop, and a trunk that per Codex briefly shipped unverified and gate-red. The arc across five sweeps is consistent: architecture and honesty stay excellent; every escape lives at the boundary of enforcement coverage, and the boundary keeps moving outward exactly as fast as the repo gates the previous frontier.

---

*Written 2026-07-09 by the Claude recon delta writer. Sources: this directory's four reports + PRIOR-SWEEPS.md; `docs/audits/codebase-recon-2026-07-02/{SYNTHESIS.md,DIFF-vs-2026-06-24.md}`; the Codex-native `docs/audits/codebase-recon-2026-07-09/{SYNTHESIS.md,findings.json,DIFF-vs-2026-07-02.md}` (toqd worktree). No findings re-derived; every row cites its evidence source.*
