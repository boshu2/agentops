# age-3va.4 Pawl Pre-Push Review

Verdict: CONFIRMED

Target: `d8f2a1eedceda4b425271e12cefa6642aaafcf7b`

Reviewer stance: independent fresh-context cross-family review of the R4 escape-emit wire, with the prior invalid-JSON defect treated as the critical regression risk.

## Contract Check

The executable `gate-verdict` contract requires the fields that the prior version omitted:

- `cli/internal/yieldledger/yieldledger.go:102` defines `GateVerdictBody` with `pawl_verdict_ref`, `head_sha`, `mode`, `author_family`, and the SoD/evidence fields.
- `cli/internal/yieldledger/yieldledger.go:238` requires a `PawlVerdictRef` with non-empty `bead_id` and `head_sha` length at least 7.
- `cli/internal/yieldledger/yieldledger.go:205` allows `mode:"deterministic"`.
- `cli/internal/yieldledger/yieldledger.go:361` rejects an invalid `pawl_verdict_ref`.
- `cli/internal/yieldledger/yieldledger.go:367` rejects a top-level `head_sha` shorter than 7.
- `cli/internal/yieldledger/yieldledger.go:373` rejects invalid modes.
- `cli/internal/yieldledger/yieldledger.go:379` rejects a missing `author_family`.
- `cli/cmd/ao/yield.go:256` parses the JSON as `GateVerdictBody`, and `cli/cmd/ao/yield.go:261` appends through the validating writer.

The workflow now emits the valid full body:

- `.claude/workflows/operating-loop.js:389` documents that the agent substitutes `git rev-parse HEAD`.
- `.claude/workflows/operating-loop.js:392` defines `emitVerdict`.
- `.claude/workflows/operating-loop.js:393` commands `SHA=$(git rev-parse HEAD)` and sends the full JSON body with `pawl_verdict_ref{bead_id,head_sha}`, top-level `head_sha`, `mode:"deterministic"`, `author_family:"operating-loop-gate"`, `author_ne_reviewer:true`, and `evidence_present:true`.
- `.claude/workflows/operating-loop.js:398` emits `CONFIRMED` at attempt 1 for the upstream slice gate.
- `.claude/workflows/operating-loop.js:402` emits `REFUTED` at attempt 2 for the downstream acceptance roll-up catch.

## Live Proof

Built a fresh `ao` from this worktree, as required because the system `ao` may be stale:

```text
cd cli && go build -o /tmp/ao-rev ./cmd/ao
exit 0
```

In a fresh temp directory, `/tmp/age-3va4-review.XIaDGF`, using real 40-character SHA `d8f2a1eedceda4b425271e12cefa6642aaafcf7b`, I ran the exact emit shape generated by the workflow:

```text
/tmp/ao-rev yield emit gate-verdict --bead age-3va.4-review --run operating-loop-age-3va.4-review --json '{"difficulty":2,"pawl_verdict_ref":{"bead_id":"age-3va.4-review","head_sha":"d8f2a1eedceda4b425271e12cefa6642aaafcf7b"},"disposition":"CONFIRMED","head_sha":"d8f2a1eedceda4b425271e12cefa6642aaafcf7b","attempt":1,"mode":"deterministic","author_context_id":"operating-loop-slice-gate","refuter_families":[],"author_family":"operating-loop-gate","cross_family":false,"author_ne_reviewer":true,"evidence_present":true}'
exit 0

/tmp/ao-rev yield emit gate-verdict --bead age-3va.4-review --run operating-loop-age-3va.4-review --json '{"difficulty":2,"pawl_verdict_ref":{"bead_id":"age-3va.4-review","head_sha":"d8f2a1eedceda4b425271e12cefa6642aaafcf7b"},"disposition":"REFUTED","head_sha":"d8f2a1eedceda4b425271e12cefa6642aaafcf7b","attempt":2,"mode":"deterministic","author_context_id":"operating-loop-acceptance-rollup","refuter_families":[],"author_family":"operating-loop-gate","cross_family":false,"author_ne_reviewer":true,"evidence_present":true}'
exit 0
```

Ledger readback in that temp directory showed exactly 2 JSONL rows: the attempt-1 `CONFIRMED` and attempt-2 `REFUTED` for the same bead/run/SHA pair. That is the pair consumed by `DetectEscapes`, which is run-scoped and requires a `CONFIRMED` followed by a `REFUTED` at a strictly higher attempt for the same bead (`cli/internal/yieldledger/escape.go:48`, `cli/internal/yieldledger/escape.go:57`, `cli/internal/yieldledger/escape.go:94`, `cli/internal/yieldledger/escape.go:107`).

The slow-loop consumer accepted the pair:

```text
/tmp/ao-rev membrane derive-checks --run operating-loop-age-3va.4-review
exit 0
Derived 1 membrane check(s) from 1 escape(s) in run operating-loop-age-3va.4-review:
  age-3va.4-review -> .agents/pre-mortem-checks/escape-age-3va-4-review-operating-loop-age-3va-4-review-d8f2a1eedced-d8f2a1eedced.md [written]
```

The derive command loads the yield ledger and calls `DetectEscapes` at `cli/cmd/ao/membrane.go:91` and `cli/cmd/ao/membrane.go:95`; its human report prints the "Derived ... from ... escape(s)" line at `cli/cmd/ao/membrane.go:281`.

## Requested Confirmations

1. Doc idiom is valid: `docs/architecture/workflow-conformance-pattern.md:104` states the full validated requirements, and `docs/architecture/workflow-conformance-pattern.md:109` through `docs/architecture/workflow-conformance-pattern.md:114` show the full body, not the old minimal shape.

2. R4 header is honest: `.claude/workflows/operating-loop.js:28` marks R4 checked, and the live proof above shows the emit genuinely appends a consumable `CONFIRMED`/`REFUTED` escape pair.

3. Emit/derive boundary holds: `.claude/workflows/operating-loop.js:393` explicitly tells the emit agent not to run `ao membrane derive-checks`; the workflow only logs that the slow loop derives later at `.claude/workflows/operating-loop.js:404`. The derive implementation is a separate CLI path in `cli/cmd/ao/membrane.go:82`.

4. Required gates:

```text
node --check .claude/workflows/operating-loop.js
exit 0

bash scripts/check-workflow-governance.sh
exit 0
```

## Known-Risk Context

`ao lookup --query "code review patterns age-3va-work" --limit 3` returned a shared-state coordination learning. I applied it by running the emit/derive smoke in a fresh temp directory rather than the repo runtime ledger, so the proof is not contaminated by existing `.agents/yield` state.

## Verdict

CONFIRMED
