{
  "name": "agentops-core-gemini",
  "version": "1.0.0",
  "description": "AgentOps 61-skill CORE image packaged for Google Antigravity (AGY) / Gemini. Portable SKILL.md set (identical to the Claude image — zero conversion) wrapped in an Antigravity plugin, with Agent Mail MCP and AgentOps guard/evidence hooks.",
  "skills": "./skills",
  "agents": "./agents",
  "rules": "./rules",
  "hooks": "./hooks/hooks.json",
  "mcpServers": {
    "agent-mail": {
      "command": "am",
      "args": [
        "serve-stdio"
      ]
    }
  }
}
